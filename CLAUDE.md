# DECO Training Evaluator — project context

Read this first, every session. It replaces the memory that used to live in the "Training" Claude Project.

## Who you're working with

Jordan Evans (jordan.evans@decorepair.com), DECO Windshield Repair. **Not a developer** — no coding background, no command-line experience. Explain things in plain language, avoid jargon, and don't assume familiarity with git, terminals, or web tooling. When something needs to happen outside this repo (in Google Apps Script, Google Cloud Console, GitHub's website), give click-by-click steps.

Always write the company name as **DECO** or **DECO Windshield Repair** — in caps, never "Deco".

## What this app is

A web app that grades DECO field-training sessions. A manager uploads an audio recording of a training session; the app sends it to Google's Gemini API, which scores it against DECO's real training rubric and returns section-by-section scores, a total, a certified Yes/No, and written feedback. It also tracks where each field manager stands on their certification path.

**Live at:** https://development-dashboard.github.io

**The app is called "Development Dashboard"** — that's what the header, the browser tab title and the panel pills say. "DECO Training Evaluator" survives only as the name of this local folder.

**The rename HAPPENED on 2026-08-14.** Jordan renamed the GitHub organisation and the repo, and both are now `development-dashboard` — confirmed two ways: a `git push` reported `This repository moved` and named the new location, and the new address serves the app. Notes for whoever is here next:
- **The git remote is correct — it was set by hand, and that step is still required after any future rename.** It reads `https://github.com/development-dashboard/development-dashboard.github.io.git` because `git remote set-url origin …` was run on 2026-08-14, not because anything fixed itself. **Git does not rewrite a stored remote URL.** When you push to a repo that has moved, GitHub answers with a permanent redirect, git honours it, prints `warning: redirecting to …` and the push succeeds — leaving the old URL sitting in `.git/config`. That's why a push can report the new location while the remote still points at the old one, which is exactly what made this look self-healing. Don't record it as automatic; a later rename will need the same command run again.
- **The Google OAuth origin was added in time — sign-in on the new address is CONFIRMED WORKING** (screenshot, 2026-08-14: header reads "Signed in", and the roster loaded 18 people across 8 regions, so the Sheets reads work too). This was the step with no symptom to diagnose — a missing origin doesn't error, the sign-in button just does nothing — so it's worth recording as settled rather than leaving anyone to re-check it. Still the first thing to look at if sign-in ever breaks after a domain change.
- The old address does **not** forward, and the old org name `deco-training-evaluator` is released for anyone to claim.

Built as a hackathon project for DECO leadership (judged on Complete / Critical Thinking / Useful, 1–3 each).

## How it's wired together

```
index.html (this repo, served by GitHub Pages)
   │  Google sign-in (Google Identity Services), Sheets + Drive REST APIs
   │
   ├── reads/writes ──> Google Sheet "DECO Training Tracker - Roster"
   │                      1LAqMWl_sSv0NSvzh2Z4YefgxpAr2RZtjvepYvful66s
   ├── writes ────────> Google Sheet "DECO Training Tracker - Grading Log"
   │                      1TsfoRDTZa73ruDGC1uDaSJJ7WKr4STQRTNNbz7S2H2M
   ├── uploads audio ─> Drive folder "DECO Training Tracker - Audio Uploads"
   │                      1Gj4Z8GeNzThuHXTUY0chEEvQixg2Zr8z
   │
   └── POSTs to ──────> Google Apps Script Web App ("Training AI Grader")
                          bound to the Grading Log sheet; holds the Gemini
                          API key server-side in Script Properties
                          TWO OR THREE calls per session, because Apps
                          Script's 6-minute cap is per execution:
                            1. action:'prepareAudio' — short clip stays
                               inline; a real session is chunk-uploaded to
                               the Gemini Files API and comes back as a URI
                            2. action:'transcribe' (V16, LONG RECORDINGS
                               ONLY) — Gemini writes the session out as
                               timestamped text with speaker labels and
                               delivery notes, saved to Drive beside the
                               audio. ONE call up to ~3h47m; past that the
                               page drives BOUNDED WINDOWS (V17), one call
                               each, so no single call meets the 6-minute
                               wall — 2 windows for 4h33m, 3 for 8h. Still
                               resumable inside a window if it stops short.
                               Hard ceiling ~9h01m, and it is Gemini's
                            3. grade — 3 consensus passes fired in PARALLEL
                               via UrlFetchApp.fetchAll, reading EITHER the
                               audio (short clips, unchanged) or that
                               transcript (long sessions)
                             │
                             └──> Gemini API (audio or transcript + prompt
                                  in, JSON out)
                                  model: gemini-3.5-flash (GEMINI_MODEL property)
```

Apps Script Web App URL (the `AI_GRADING_ENDPOINT` constant in `index.html`):
`https://script.google.com/a/macros/decorepair.com/s/AKfycbyWTE5fjMMSr2pExK0EuyuExZhXT0Por7k2Hh6vXeSrXRbAlDT7qrOsErKz6W_SnY3I/exec`

The official *human* evaluations live in Smartsheet, not in this app: **"Training Evaluations"** (Training Day) and **"Site Visit Evaluations"** (Team On-Site Day). Those are the source of truth for calibrating the AI's accuracy.

## Repo layout

- `index.html` — the entire front end. Single self-contained file: HTML, CSS, and JS inline. Eight tabs: **Dashboard** (certification roster), **Follow-Ups** (see below), plus one per session type — **Training Day**, **Team On-Site Day**, **Conversion Call**, **Stage 1 Interview**, **Reference Checks**, **Stage 2 Interview**. The Reference Checks tab covers two session types (candidate and parent) behind one picker — see its `variants` array.
  - **Follow-Ups (added 2026-08-13)** is the coaching queue: every graded session whose AI verdict came back as anything other than `Certified: Yes`, across all session types, in one filterable list that drains as people tick rows off. Jordan's ask, and the first piece of Phase 4 to actually exist. Design decisions worth not re-litigating:
    - **What pulls a session in is the AI's own `certified` value (column K), nothing re-derived.** So the list can never disagree with the grade on the card.
    - **The reason column splits three things a single "failed" label would flatten:** *scored under 6*, *never came up on the recording* (the audio-only blind spots — counts as 0 by design, not a performance problem), and *the AI wasn't sure* (`needs_human_review`). Keep them separate; that distinction is most of the tab's value.
    - **It's framed as coaching, not a verdict, and must stay that way** while the AI is uncalibrated — same rule as the rest of the app up to Phase 5.
    - **A person's region comes from the Roster, looked up by name** (`regionForPerson`). Nothing region-related was added to the Grading Log; a name that doesn't match the Roster shows as "Region not set" rather than disappearing from the filter.
    - **Someone already certified for that session type gets an "Already certified" flag** — a certified person slipping is a different situation from a trainee who was never expected to pass yet.
    - `fetchSubmissions()` is shared with Recent Submissions so there's one place that knows how sheet rows map onto sessions.
  - **Compare (added 2026-08-14)** is Phase 2 calibration on screen: pick a person and a session type and every AI grade they have lines up side by side, section by section, against a human grade typed in beside them. Jordan's ask, after V16 made it possible to grade one recording two different ways — *"see the difference between those two"* and then *"all 4 AI runs and the human comparison"*. He also said outright *"we just might not use it"*, and several decisions below follow from that rather than from the feature being load-bearing. Decisions worth not re-litigating:
    - **It handles N runs, never a fixed number.** The count is whatever happens to have been graded — two today, six next week — and a table built for four breaks on the first session that isn't. Verified at 2, 4 and 6.
    - **The human column is the REFERENCE, not a fourth opinion.** Phase 2 is "refine the prompt until the AI lines up with the district manager", so every figure here measures AI *against* human and never averages the two together.
    - **Audio vs transcript is read off column X, not guessed.** A session Code.gs wrote a transcript link for was transcript-graded; anything without one was graded from audio, which is also correct for every session predating V16. Gated through `audioLinkOf()` so a note typed into that column by hand can't be read as a transcript. It's a labelled chip on each column because that distinction is the entire question the tab was built to answer.
    - **A section with no confident score renders "never came up", never `0`.** It counts as 0 in the total by rubric rule, but drawing it as a zero turns "the audio never covered this" into "they did this badly" — the same distinction Follow-Ups exists to preserve. `compareScoreOf()` mirrors `followUpReasons()`'s test exactly and has to keep doing so.
    - **The human total is the SUM of what's typed, never separately entered.** Two sources for one figure is how they drift apart. A part-filled column says "3 of 11 filled in" rather than showing a total that reads like a grade.
    - **Deltas are signed.** "The AI is consistently generous" and "consistently harsh" are different findings, and a bare magnitude hides which one you're looking at.
    - **The typed human grades USED to live in `localStorage` and now live in the Grading Log at column Y — see "Saving the typed human grades" below.** The original note here said promoting them to a column was the upgrade if the tab earned its place; it did, and Jordan asked for it on 2026-08-14. Storage is still used, but only as a crash mat for unsaved typing. **The one thing from that note that still stands: storage access is wrapped because it THROWS** rather than returning null in a private window — same lesson as `saveToken` and the theme toggle.
    - **Loaded on open, not on sign-in**, and it reuses `submissionsCache` when something else already filled it. Refresh re-reads rather than clearing the cache — the Dashboard badges read that same cache and blanking it mid-flight would empty them.
    - Front-end only. **No `Code.gs` change, no redeploy.**
    - **Verified in a real browser against a stubbed Sheets backend, both schemes** (no screenshot — the Browser pane still wasn't compositing). Two mistakes in the *testing* worth recording, because both would have passed a careless review: **the first overflow checks were measuring nothing**, since the whole app sits inside `#app-content` which is `hidden` until sign-in, so every width read 0 and "no horizontal overflow" was trivially true — reveal it first. And **the contrast sweep reported five false failures at ~1.2:1** because `color-mix()` computes to `color(srgb 0.95 0.61 0.33)` — **0–1 floats, not 0–255** — which a naive rgb parser reads as near-black. Parse both forms and composite the semi-transparent tints, or this sweep will "fix" things that were never broken.
    - **Two real defects found and fixed by that verification:** `.cmp-table` at `width:100%` with no `min-width` **squeezed** 8 columns into a 375px viewport instead of handing the overflow to `.cmp-scroll` — `min-width: max-content` is what makes it scroll inside its own box (measured: 1072px table in a 267px box, page still 375px). And `.cmp-col-when` / `.cmp-cell-note` measured **4.09** in light mode, the known `--text-muted`-on-`--sunken` value, now `--text-secondary` at 7.47 light / 10.55 dark. Full sweep of all 17 new classes: **zero failures in either scheme.**
  - **Saving the typed human grades — the Grading Log, column Y (2026-08-14).** Jordan, on the Compare table: *"Can take these arrows out, as well as save option on the typed in scores."* Asked where they should be saved, he chose the Grading Log over this browser — which is exactly the upgrade the original localStorage note said to make if the tab earned its place. Decisions worth not re-litigating:
    - **ONE GRADE PER RECORDING, NOT PER PERSON, and this is the decision everything else follows from.** The old `localStorage` key was person + session type, so a single column of boxes covered every run. That only held because the runs so far were **the same call graded four ways**; the moment somebody has two *different* conversion calls, one shared grade silently claims to describe both. A Sheet row is one session, which is the honest unit — so the score boxes moved **into each run's own column** and the standalone "Human" column is gone. Jordan was given this choice explicitly and picked per-recording knowing it means typing the numbers again on each of his three duplicate submissions of the same call.
    - **On the AI row, unlike the sections tick at W.** A session is up to two rows and the *manual* one need not exist at all (an audio-only submission has no manual grade), but Compare only ever shows sessions that **have** an AI row, since that's where the score being compared lives. So the AI row is the one guaranteed to be there. Read from either, though, so a value typed into the manual row's cell by hand is still found. Same reasoning that put the follow-up state on the AI row.
    - **JSON, like column S, because it has to round-trip exactly.** A readable `Opening: 9; Discovery: 7` would have to be re-parsed out of free text somebody can edit in the Sheet, and a mis-parse silently changes a calibration figure. `parseHumanScores` is defensive for the same reason: a cell somebody typed a sentence into comes back as *no grade*, never a partial one — **a partial human grade is worse than none**, because the totals row would then report a number nobody stands behind.
    - **Nothing writes until Save is pressed**, because a Sheets write per keystroke is eight writes for one grade. Typing updates a draft that is mirrored to `localStorage` **as a crash mat only** — so closing the tab mid-grade loses nothing, which is what stops the move to a shared store from costing "type eight numbers, navigate away, lose all eight".
    - **The save READS THE WHOLE LOG BACK and only drops the drafts that verified.** Same guard the delete path and `writeRosterCell` already use. Anything that didn't land stays dirty, stays on screen and stays in this browser — a partial failure costs a second press, never the numbers. A hard failure deliberately **does not claim nothing was written**, since a batch that throws part-way may still have landed rows.
    - **An error outranks the "unsaved" hint in the status line, and that ordering is load-bearing.** Checking dirty first — which is what shipped and was caught in testing — replaced the reason with *"Unsaved — press Save"*, i.e. the one message that makes a failure look like the user simply hadn't pressed the button yet. Editing anything clears the error, which is when the plain hint comes back.
    - **The Save button lives in the panel's static markup, OUTSIDE `#compare-wrap`.** Not tidiness: leaving a score box and clicking Save fires that box's `change` first, and if that rebuilt the button, mousedown and mouseup would land on different elements and **the click would never fire at all**.
    - **Re-rendering is deferred by a `setTimeout(0)` so a Tab has already landed on the next box**, whose focus is then captured and restored. Without it, typing eight numbers means eight lost tab stops. `scheduleCompareRerender` is that; don't collapse it back into a direct call.
    - **The spinner arrows are gone** (`appearance: textfield` + the WebKit rules). They stole width from a column with none to spare, and a stray scroll over a focused number input silently changes a score. Kept as `type="number"` for the numeric keypad and the 0–10 bounds.
    - **Grades found under the OLD person+type key are shown, never auto-filled.** There is no way to tell which recording each score was about, and filling them into a column would be the app asserting something it can't know — on the wrong recording that's a fabricated calibration figure. The notice lists them and offers "Forget these".
    - **Compare now says Pass / Fail for a Conversion Call** via the same `verdictWords()` the results card uses, instead of Certified / Yes. The row label is the neutral "Verdict", so nothing has to invent a heading per session type.
    - **`GRADING_LOG_LAST_COLUMN` moved X → Y in the same change.** A delete stopping at X would leave a calibration grade behind for a recording that no longer exists — the same failure the W and X notes already warn about, one column along.
    - Front-end only. Code.gs appends A–S and never reads past S. **No `Code.gs` change, no redeploy.**
    - **Verified in a real browser against a stubbed Sheets backend, both schemes** (no screenshots — the Browser pane wasn't compositing). Checked: the save writing `Y2` then the header, and the round trip back through `fetchSubmissions`; a write that **reports success and is discarded** caught by the read-back and named ("Saved 0 of 1. Row 2 didn't read back"); a 500 keeping the drafts and the button pressable; the crash mat recovering after the in-memory draft was wiped; focus surviving a tab between boxes; saved scores reloading from the Sheet with nothing dirty and Save disabled; the legacy notice and its Forget button; Training Day still reading "Certified"; contrast **7.47–16.7 light / 9.9–17.91 dark, zero failures**; no page overflow at 1280 or 375, where the table scrolls inside its own box (493px in 270px). **The `.btn` reading 1.25:1 in that sweep is the KNOWN false positive** this file already records — it's gradient-filled, so `backgroundColor` is transparent and the walk-up measures the page behind it.
  - **The "Manual feedback" box was WRITE-ONLY, and now isn't (fixed 2026-08-14).** Jordan, looking at the textarea on the grading form with a word typed in it: *"Does this go anywhere?"* It did — straight into **column F** of the manual row — and then nowhere else ever again: `fetchSubmissions()` read A–X but skipped index 5, so the sentence explaining a manual grade was invisible on the results card, in Recent Submissions, in the expanded roster row, in Follow-Ups and on Compare, while the *grade* it explained showed in four of those. The only way to read it was to open the Grading Log Sheet. Decisions worth not re-litigating:
    - **One helper draws it everywhere** (`managerNoteBlock`), for the same reason `certifiedCellFor()` is shared: four copies of a block this small is how they drift apart.
    - **The label names WHO wrote it** — "What Grace Hopper wrote about this session", off column D, falling back to "the grader" for a blank or `Did not attend`. This block sits *directly above the AI's own card*, and unattributed prose in that position reads as something the AI produced. Don't reduce it to a bare "Note".
    - **A manual-only grade with a note is now EXPANDABLE.** Both lists gated the chevron on `hasAi`, which would have kept the note hidden on exactly the sessions that have nothing else to show. `canExpand = hasAi || note`, and the empty `.ai-result-box` is hidden by the pre-existing `:empty` rule, so a note-only row shows no bare panel underneath.
    - **The roster row's note and AI card share one `.ps-detail` wrapper.** The toggle used to add/remove `hidden` on `.ai-result-box` itself; leaving the note outside that would have made it the one thing not revealed by opening the card.
    - **Whitespace is nothing** (`manualNoteOf` trims), the same guard `audioLinkOf` puts on column G — a note of three spaces must not open an empty panel headed "what the grader wrote".
    - **On Compare it's a row under the RUN columns, not the human one.** Each column there is one session, and the note came in on that session's form; it's also the closest thing on that tab to *why the human graded it that way*, which is the question the tab exists to answer. Drawn only when at least one session has one — an empty row of dashes earns nothing on a table already that wide — and capped at `max-width: 240px` so one long note can't stretch the table past what `.cmp-scroll` hands off.
    - **`pre-wrap`, so line breaks somebody typed into the textarea survive.**
    - Read-only. Nothing new is written, the sheet is unchanged, and `GRADING_LOG_LAST_COLUMN` doesn't move. Front-end only — **no `Code.gs` change, no redeploy.**
    - **Verified in a real browser against a stubbed Sheets backend, both schemes** (no screenshots — the Browser pane still wasn't compositing). Checked: the note reaching `fetchSubmissions` on both the merged and the `noLink` path; all four row shapes (note+AI, note-only, nothing, whitespace-only) in Recent Submissions and in the expanded roster row; the note rendering above a real AI card in Follow-Ups; Compare with 2 runs where one has no note; the 240px cap wrapping a 290-character note to 8 lines with the table still 1133px inside its 1135px box; no page overflow at 1280 or 375; contrast **6.97 light / 9.45 dark** on the label and **15.58 / 16.06** on the text, **16.7 / 7.47** on Compare. **One testing mistake worth recording, and it's the one CLAUDE.md already warns about:** `#app-content` is hidden by a **class**, not the `hidden` attribute, so setting `.hidden = false` reveals nothing and every measurement comes back 0 — `classList.remove('hidden')`, and unhide the tab section too.
  - **Emailing a graded session to the person it's about (added 2026-09-08).** Jordan, thinking through how feedback actually reaches a field manager: *"it goes to the manager to review, and then to send it off ... when they get the feedback and the manager approves it, they can click the send button."* Every expanded session — Recent Submissions, Follow-Ups, and the expanded roster row — now carries a **Send this feedback to …** panel that drafts a plain-text email, lets the manager edit it, and sends it **from their own Google account**. Decisions worth not re-litigating:
    - **THE MANAGER PRESSES SEND. NOTHING SENDS ITSELF, and that is the whole safety property** — the advisory-only rule one step further out than the Roster. Nothing in this app is calibrated, and on the one Conversion Call with a per-category human grade the AI came in **above** the human on every run. Feedback that let itself out of the building would be DECO telling a field manager they failed on the authority of a number nobody has checked. Auto-sending on a finished grade was considered and rejected; **don't build it.**
    - **The draft is a TEXTAREA, not a preview.** Jordan chose (asked and answered, 2026-09-08) to include the AI's section-by-section scores in what the employee sees, and full editability is what makes that safe enough to do: a manager who reads a section score that doesn't match the session they ran can delete the line, rather than choosing between sending something wrong and sending nothing. **The manager's grade is the headline and the AI block is explicitly second**, carrying its own sentence saying it is a second opinion and the grade above is the one that counts. That ordering is not decoration — this email is the first thing in the project that shows an uncalibrated AI score to the person it is ABOUT rather than to somebody grading them.
    - **It sends via the Gmail API with a new `gmail.send` scope, as the manager.** Routing it through Apps Script was considered and rejected: that would send every manager's feedback **from the script owner's account**, i.e. all feedback in the company appearing to come from Jordan. Cost of the choice, and it is real: **everyone signed in gets one fresh consent screen.** `gmail.send` is send-only — nothing here can read, search or delete a mailbox. **A copy is Cc'd to the sender**, so it's in their own sent mail and an accidental send is visible to whoever made it.
    - **The granted scopes are tracked (`grantedScopes`), never assumed from `SCOPES`.** Google's consent screen lets somebody untick one permission and approve the rest, so *asking* for `gmail.send` is no evidence of having it. Stored with the token so a refresh knows. `requestGmailPermission()` asks with **`prompt:'consent'`, not `''`** — someone who unticked the box has a valid token that simply lacks the scope, and `prompt:''` hands that same token back without ever asking again. It waits 120s, not `SILENT_TIMEOUT_MS`, because a person has to read the screen; and it deliberately does **not** set `silentRenewMayBeOnScreen`, which exists to hold back popups nobody asked for. **This one was asked for, by a click.**
    - **A verdict word is only printed where the row can settle it.** `submissionPassedManually()` is deliberately NOT used — it answers "could this have certified them", so it returns false for a session type with no certification column and would print **Fail on a passing Networking Activity**, and it folds a blank sections tick into a yes. So: under the bar → the no word; at/above it **with the tick recorded** → the yes word; at/above it with the tick **never recorded** → the number and no verdict. Same three answers `manualCertifiedCell` draws, and this email must not invent the half that was never written down.
    - **"Never came up on the recording (counts as 0)" survives into the email**, the same distinction Follow-Ups, Compare and the results card all preserve. Flattening it to a zero would tell somebody they did badly at something the microphone never caught.
    - **Roster `Email` column, offered on BOTH boards.** Deliberately **not a column in the roster TABLE** — both boards are `table-layout: fixed` and already split their width between four fixed columns and five or six badges, so a ~33-character address would squeeze the badges rather than scroll. It renders as a second line under the name (same shape as the "also BC1, BC5" line), ellipsised, full value on the tooltip, and **turns into an input when clicked** — writing through `writeRosterCell`, so it is read back before the screen changes. Optional everywhere: a blank address stops only the sending, and the panel says which address is missing and where to add it rather than the button quietly being absent.
    - **Column Z, `Feedback Sent`, and `GRADING_LOG_LAST_COLUMN` moved Y → Z.** One readable string, never parsed back apart — it answers "has this gone out, and where to?", which is a sentence, unlike column Y's JSON. **Written to the AI row where there is one, falling back to the MANUAL row** — the one extended column that cannot assume an AI row exists, since feedback is perfectly sendable on an audio-less paper evaluation. **Z is the last column this pattern can take:** every range built from that constant is a single-letter A1 reference, so a 27th column means teaching `columnLetter()` and those ranges about AA.
    - **A second send is recorded but never BLOCKED.** A manager may want to resend a corrected note. The panel says what already went and when, so a resend is a decision instead of an accident.
    - **A successful send that fails to record does NOT claim it wasn't sent.** Saying "couldn't send" over an email already in somebody's inbox is far worse than an unrecorded send, so the failure is reported as exactly what it is.
    - **Every session with a grade, a note or an AI read is now EXPANDABLE** (`feedbackWorthSending`). A bare manual score used to have no chevron, which would have hidden the one thing that could be sent.
    - Front-end only. **No `Code.gs` change, no redeploy.** **PUSHED AND CONFIRMED LIVE 2026-09-09** (commit `9a79915`) — verified by fetching the served file with a cache-buster and finding `gmail.send`, `renderFeedbackSender` and `GRADING_LOG_LAST_COLUMN = 'Z'` in it, rather than assuming Pages had caught up. **Two things Jordan has to do before it can be used: press "Add it to the Roster" on the Email-column bar, and accept the one-time Google permission prompt** (so must everyone else, once each).
    - **Verified in a real browser against a stubbed Sheets and Gmail backend, both schemes** (no screenshots — the Browser pane wasn't compositing again). Checked: the address write landing on **K4** for a person below a blank Roster row, an apostrophe name round-tripping, a bad address and Escape both issuing **zero** writes, a discarded write reverting the cell and naming it; the draft for a manual pass, a manual fail, an unrecorded sections tick, and an AI-only session, with `stage2_`/`networking_` prefixes stripped and **DECO** capitalised; the MIME message decoding back byte-identical including an em-dash subject through RFC 2047, base64 wrapped at 76 and URL-safe; the **edited** body being what went, not the draft; `Z1` + `Z3` written to the right row; a 500 keeping the draft with zero sheet writes; a declined consent issuing **zero** Gmail calls; a delete now clearing **A:Z**; the panel rendering in all three contexts; no page overflow at 1280 or 375; no app console errors.
    - **One real defect found by measuring, not by review:** the "+ Add email" line at `--text-muted` measured **4.55 resting and 3.94 on a HOVERED row** — under AA, and hovered is the normal state, since this sits in a click-to-expand row somebody's pointer is over on the way to clicking it. Now `--text-secondary` with italic carrying "nothing here yet": **7.87 / 7.20 light, 11.09 / 9.74 dark.**
    - **Still open, and worth knowing: nothing checks that the person on the form is the voice on the recording.** A mis-filed grade would now be emailed to the wrong person — the same unguarded failure mode this file already records from the 2026-08-14 Conversion Call, with a wider blast radius.
  - **Dashboard rows expand into that person's recordings (added 2026-08-13).** Clicking anyone on the roster opens their sessions inline — every session type, newest first — and each one opens further into the same AI result card (audio player, evidence timestamps) that Recent Submissions and Follow-Ups render. Points worth knowing:
    - **No extra read of the Grading Log.** `fetchSubmissions()` stores its result in `submissionsCache`, and Follow-Ups' load — which already runs on sign-in, on Refresh, and after a grade finishes — then calls `refreshRosterRecordingCounts()` to paint the "N recordings" badges. That's why the badges appear a moment *after* the roster does, and why a row with no badge is still clickable.
    - **`refreshRosterRecordingCounts()` skips the re-render while a row is open**, so a background refresh can't shut a row under someone reading it.
    - **A delete is the one repaint that can't be skipped (fixed 2026-08-13).** Deleting a session from Recent Submissions left it on the Dashboard, because the Dashboard reads `submissionsCache` rather than the sheet and the guard above skipped the repaint — Jordan hit this with a row already expanded. The delete now (a) drops the cleared row numbers out of `submissionsCache` itself instead of waiting on `loadFollowUps()`'s re-read, which can be slow or fail, and (b) calls `renderDashboardKeepingOpenRow()`, which repaints and then re-opens the same person. **The open row is found by name (`data-person`), not by row index** — the index only means something against the filters in effect when the row was drawn — and by comparing the attribute rather than with a CSS selector, since a name can contain an apostrophe.
    - **A delete now proves it worked before the row leaves the screen (2026-08-13).** Jordan deleted a session, watched it disappear from Recent Submissions, and it was still on the Dashboard after a full page reload — so it was still in the Sheet. **What the Sheet actually showed when we finally looked: the manual row empty and the AI row intact.** A session is up to two rows sharing an audio link, and only one of them went; the surviving AI row carries the score and the link, so the session looked untouched. Whether the app cleared one row and reported success on both, or cleared nothing and a later by-hand edit emptied the manual row, was never settled — Jordan resolved it in the Sheet before we could reproduce. Two things made it hard to see, both worth remembering: **the Drive connector serves a *cached* copy of a Sheet** (its `read_file_content` didn't show a row added minutes earlier, while `get_file_metadata`'s `modifiedTime` was live — use metadata to sanity-check freshness, and don't diagnose live data from it), and **the Grading Log had a filter on Trainee Name**, so rows were hidden from the person looking while the API read them all. The delete now (a) renews the token first, (b) reads *every* row back after clearing and treats any surviving data as a failure, keeping the session on screen, and (c) writes the reason into a red bar above the list (`showLogDeleteError`) instead of a button label that says "Delete failed" for three seconds and reverts. A half-deleted session is exactly what the read-back catches, so **the next occurrence will name itself.**
    - Sessions are matched to a person by the **name in column B**, the same name the upload form picked off this roster. A name typed into the Sheet by hand won't match.
    - The person's sessions are **divs, not a nested table** — `#dashboard-table-wrap table` sets `table-layout: fixed`, which would hit any table nested inside it.
  - **A Team On-Site Day now takes THREE passing recordings to certify (added 2026-08-14, `SESSION_PASSES_REQUIRED`).** Jordan, thinking about why the long-recording work kept hurting: *"on a team on-site day, you would do about three to four … why don't they get three recordings? And to be certified, you need to pass at least three recordings … if you did two passes, one fail, you'd have to do another."* **The premise checks out and it is the good kind of fix — it makes an engineering problem go away by correcting a modelling error.** A Team On-Site Day was never one session; it is three to four separate site visits, which is exactly why the whole-day file ran 4h33m and needed six bounded transcription windows at $4.27. Recorded per visit, each file is short enough for a single pass. **It is also rubric-compatible, which was the thing most likely to sink it and was checked rather than assumed:** all five section keys (`pre_visit_preparation_arrival`, `engage_setup_compliance_rapport`, `reading_operator_leadership_diagnosis`, `sales_and_coaching`, `technical_development_wrap_up`) are per-visit behaviours, so every visit legitimately exercises all five. Had any of them been a once-a-day thing, every recording would have scored 0 on it by the "no confident score counts as 0" rule and nobody could ever have passed. Decisions worth not re-litigating:
    - **Three PASSES, not three in a row.** A fail doesn't wipe the passes already banked — two passes and a fail means one more visit to record. Jordan's call, and the forgiving reading is right while the AI is uncalibrated.
    - **It counts MANUAL grades only, and that is the whole safety property.** An AI grade still never moves the Roster. Counting AI verdicts here would have quietly handed the certification decision to the AI, which is Phase 5. `passingManualGrades()` is the one place that counts, and it counts what a human signed off.
    - **Consequence Jordan should know and hasn't been asked about: a strong first day can certify on its own.** Three of the four visits in one day passing is three passes. That follows directly from what he asked for ("if you pass three team on-site days, then you should be good"), so it's built that way — but if the intent was three passes *on separate days*, that's a different rule and the app can't tell days apart anyway (the log stores upload time, not visit time).
    - **The count comes from a FRESH read of the Grading Log at submit time, never from `submissionsCache`.** The row for the grade being submitted was appended seconds earlier and isn't in the cache, and the cache can be minutes stale besides. One extra Sheets read on the highest-stakes write in the app is a cheap price for not signing somebody off on an out-of-date number. **If that read fails, nothing is certified and the message says so** — the safe direction, and it names what to do rather than leaving a badge mysteriously unchanged.
    - **`manualGradeCertifies` was renamed `manualGradePasses`, and the rename is the point.** "Is this recording a pass?" and "does that certify them?" are now two questions with two answers. Collapsing them back into one test is how a single good visit starts signing people off again.
    - **Training Day is untouched, by construction rather than by special case** — it has no entry in `SESSION_PASSES_REQUIRED`, so `passesRequired()` returns 1 and the old path runs byte-for-byte, including **zero extra log reads**. A Training Day *is* one two-day session; there is nothing to split. Verified as a regression check, wording included.
    - **The delete → un-certify offer counts the same way, and the old test would have missed the main case.** `stillSupported` was a `.some()` — "is there any passing grade left?" — which on a three-pass type answers "yes, still supported" when someone drops from 3 passes to 2, i.e. precisely when the offer should appear. It's a count against `passesRequired` now, and the bar says *"that leaves only 2 of the 3 passing recordings it takes to certify"* rather than "that was the last passing grade on file".
    - **The Dashboard badge carries the progress ("2 of 3 passes") and is deliberately BLANK at zero.** On a real roster most people have no team on-site grades at all, so "0 of 3" would add a second line to nearly every row to repeat what the Not-yet badge beside it already says. Same rule the "N sessions" badge in that table already follows. Blank also when the person is certified, and when the Grading Log hasn't been read yet — a zero drawn mid-load would tell somebody with three passes they have none. The rule itself lives in the note above the table and in the button's own tooltip.
    - **Clicking a badge still certifies on the spot, count or no count.** A human deciding outranks the tally, same permission the badge toggles have always had; the tooltip just says the count exists so it doesn't look like the rule was forgotten.
    - Front-end only. **No `Code.gs` change, no redeploy** — the backend grades one recording and has no opinion about how many make a certification.
    - **Verified in a real browser against a stubbed Sheets backend** (no screenshots — the Browser pane wasn't compositing again). Checked: `passesRequired` for all four session types; the pass test at the 38/37 boundary and unticked; nine counting cases (blank column W counts, a recorded "No" doesn't, under-the-bar doesn't, Training Days don't cross-count, names match case-insensitively, null vs 0 for an unread log and a blank name); all seven cert-preview branches plus Training Day's three unchanged; the submit path issuing **zero Roster writes on passes 1 and 2 and exactly one (`G2=Yes`, right column, right row) on pass 3**; Training Day certifying on one pass with zero extra reads; the failed-read path writing nothing; already-certified short-circuiting before the read; the un-certify offer at 3→2 (raised) and 4→3 (silent); the badge at 1133px with no table or page overflow at 1280 or 375; an apostrophe name round-tripping; contrast **8.42 light / 9.9 dark**; clean console.
    - **Two real defects found by running it, both invisible to review.** `.badge-toggle` is an `inline-flex` **row**, so the progress line became a second flex item and sat *beside* the badge — 149px of button in a 121px column, silently overlapping the next column because `table-layout: fixed` means nothing widens and nothing scrolls. Hence the `.cert-stack` wrapper: one flex item that stacks internally. And `certProgressLabel` read the certification value off the person in memory while `paintRosterToggle` is deliberately called with values that *disagree* with memory (the new value while a write lands, the old one when a write fails) — so the badge read **"Certified · 3 of 3 passes"** for as long as the two were out of step. The value is passed in explicitly now.
  - **Auto-certifying from a manual grade (added 2026-08-13).** A passing manual grade ticks that person's certification column on the Roster and flips the Dashboard badge, with no retyping into the Sheet. This is allowed *because the grade is a human's decision* — an AI grade still never writes to the Roster, and mustn't until Phase 5. Decisions worth not re-litigating:
    - **Both halves of the Smartsheet formula, or nothing.** The form captures one total, and the rule is total ≥ the bar **and every section ≥ 6** — a total can't answer the second half (88/110 with a 4 in one section is not a pass). So there's a **"Every section scored 6 or higher" checkbox** next to the manual grade, and `manualGradeCertifies()` requires it. No tick, no write. The alternative considered and rejected was certifying on the total alone, which would silently certify people the Smartsheet wouldn't.
    - **It only ever writes "Yes".** A grade under the bar leaves the cell alone — a weak session doesn't revoke a certification somebody already holds, and un-certifying is a manager's call, not a form's.
    - **Deleting the grade that certified someone offers to take the certification back — it never does it silently (2026-08-13).** Jordan deleted a passing manual grade and the Dashboard went on saying Certified, because the tick lives on the Roster and the grade lives in the Grading Log; nothing connected the two. Now a successful delete runs `offerUncertifyAfterDelete()`, which rules out every case it can and otherwise puts an amber bar above Recent Submissions with one button (`uncertifyOnRoster` → writes `"No"`). Decisions worth not re-litigating:
      - **It asks rather than writes, because the app cannot know where a "Yes" came from.** It might have been typed onto the Roster by hand, or earned on a session graded before this app existed. Auto-wiping it because an unrelated row was deleted is a worse failure than the one being fixed — and it would contradict the rule directly above.
      - **It's still an offer even though the sections tick is now recorded (column W, below).** W settles one half — a grade recorded as *not* ticked certified nobody, so it no longer produces a pointless offer, and it no longer counts as a remaining grade that holds a certification up. What W can't settle is where an existing "Yes" on the Roster came from: hand-typed, or an older session. `submissionPassedManually()` therefore treats a **blank** W (any row from before 2026-08-13) as *unknown* and still asks. Don't collapse unknown into either answer.
      - **Four gates decide whether the bar appears at all:** the session type has a certification column, the person currently reads certified, the deleted grade was at or above the pass mark, and **nothing left in the Grading Log would certify them on its own** (checked against `submissionsCache` *after* the delete has pruned it, and scoped to the same session type — a Team On-Site Day pass doesn't hold up a Training Day certification).
      - **It writes `"No"`, not a blank.** `certBadge()` reads the two identically, but a written "No" says somebody decided this where an empty cell reads as never filled in.
      - **A failed Roster write keeps the offer on screen with a "Try again" button** and the reason underneath. The delete itself already succeeded and still stands — the two are different Sheets, and the message must not imply the delete came undone.
      - **The offer was working and still failed, because nobody saw it — fixed 2026-08-13, same day it shipped.** Jordan deleted the grade that had certified Brady Van Someren and reported that the Dashboard went on saying Certified. **The four gates were fine.** A local harness (fake Sheets behind a stubbed `fetch`, driving the real `loadRecentLog` → delete → offer path) reproduced his exact state and the bar appeared correctly, in both the audio-backed and manual-only-with-blank-W variants, and the button wrote `"No"` and flipped the badge. What was actually wrong is that **the thing you look at (the Certified badge, on the Dashboard tab) and the thing that fixes it (the bar, at the top of Recent Submissions on the session-type tab) are on different tabs** — so the natural move after deleting is to go check the Dashboard, and never see the offer. Three changes, all to discoverability, none to the ask-don't-write rule:
        - **The bar scrolls itself into view.** Deliberately `scrollIntoView({block:'center'})` with **no `behavior: 'smooth'`** — smooth scrolling is a silent no-op in some browsers (measured: it did nothing at all in the test browser, while plain scroll landed correctly), and a notice whose entire job is to not be missed must not ride on an animation that might not run.
        - **The offer survives a re-render.** It lives in `pendingUncertify` and is re-drawn by `restoreUncertifyOffer()` after every paint of Recent Submissions, because `wrap.innerHTML = html` used to destroy it — a Refresh, a finished AI grade, or switching between the two live tabs would silently take away the only button that can undo the certification. It's scoped to its own session type, so a Training Day offer doesn't appear over the Team On-Site Day list, and it comes back when you return to its tab.
        - **"Leave it as is" exists.** Declining is a real answer to a question the app is asking; before this the only ways out were to reload (losing the button permanently) or to leave the bar sitting there.
        - **The bar was still too quiet to read as an interruption — fixed 2026-08-14 (`.alert-bar`).** Jordan, on seeing it: *"make this a more obvious message so it is not hard to miss when they pop up."* Everything about *when* it appears was already right (it scrolls itself in, it survives a re-render); what was wrong was that it carried the same visual weight as a hint under a form field — a 1px border, a 12% tint, `--shadow-sm`, and 12px buttons. It now shares one shell with the failed-delete bar: a 7px solid tone stripe down the left edge, a 34px icon disc, a headline in the tone's own colour (**"Action needed — the Dashboard still says certified"**, which names the *other tab* before you read a word more), full-size buttons, and `--shadow-pop`. `showLogDeleteError` was rebuilt on the same shell (`--tone: var(--critical)`, headline "That delete didn't go through") and now scrolls itself into view too — the delete button can be a long way down the list. Decisions worth not re-litigating:
          - **Neither button was promoted to a primary orange one.** The bar asks a question the app genuinely can't answer (see the ask-don't-write rule above), so the loudness belongs to the notice, not to one of its two answers. Declining stays the quieter of the two and looks it — only the size changed.
          - **All of the weight is in the RESTING state.** The entrance slide and the twice-then-stop ring pulse are decoration behind `prefers-reduced-motion: no-preference`, on exactly the rule that governs the scroll: a notice whose whole job is to not be missed must never depend on motion that might not run. The pulse rides on a `::after` so it can't fight the resting shadow.
          - **`is-new` and `role="alert"` are set only on a *fresh* offer, not on the restore path.** A Refresh or a finished AI grade re-draws this bar; re-pulsing and re-announcing to a screen reader on every paint would turn it back into wallpaper. This is why `showUncertifyOffer`'s third parameter is now `announce` rather than `scrollIntoView` — it gates all three.
          - **The headline colour is `color-mix(in srgb, var(--tone) 78%, var(--text-primary))`, not the tone itself.** Measured: full-strength `--warning` on its own 14% tint is **4.09:1**, and 15.5px bold doesn't qualify as WCAG large text. The mix darkens on light and lightens on dark — 5.62 / 5.53 / 5.21 (warning / critical / done) in light, 9.04 / 6.67 / 7.69 in dark. Don't "clean this up" back to `var(--tone)`.
          - Verified programmatically in both schemes (the Browser pane still wasn't compositing frames, so no screenshot): computed contrast as above, no horizontal overflow, the bar at 1132×179 at 1280px wide, and the failure path, the dismiss path, and `restoreUncertifyOffer()` all re-run. **Worth an actual look the first time a session can screenshot.**
            - **Partly answered later the same day.** The Browser pane *was* compositing when the prerequisite gate went in (see below), and that bar uses this exact shell — screenshotted in both schemes, and the resting weight reads as an interruption as intended. That's the shell confirmed, **not this bar**: the un-certify offer's own copy, tone and two-button row still haven't been looked at. The screenshot pass did turn up one measured defect in the shared shell — `.notice-err` failing AA on the warning tint — which is now fixed for both bars.
        - **Still true and worth knowing: the offer is only raised by a delete.** Once it's answered or the page is reloaded, there is no way back to *the offer* — but as of 2026-08-14 a stranded "Yes" no longer has to be fixed by hand in the Sheet, because the Dashboard's certification badges are directly clickable (see "Changing a roster status by hand"). What was considered and *not* built, and still isn't, is a **standing un-certify prompt** on the Dashboard: a certified person with no passing grade on file is the normal case right now (most certifications predate this app), so a prompt would nag on almost every row. A badge somebody chooses to click is not a prompt.
      - Front-end only. **No `Code.gs` change, no redeploy.**
    - **The sections tick is now written to the Sheet — column W, `All Sections 6+` (2026-08-13).** Jordan looked at a manual-only row reading 89/110 and saw the Certified column showing a dash: *"even though they passed the constraints it does not say certified."* The column only ever reported the AI's verdict (column K), and the log only stored the total — so a grade that certified somebody and a grade that missed on one section were indistinguishable after the fact. The fix was to record the thing the app already knew at submit time. Points worth keeping:
      - **W, not one of the free columns before it.** Code.gs owns A–S; T/U/V are the follow-up state. `GRADING_LOG_LAST_COLUMN` exists so the three ranges that must agree — the read in `fetchSubmissions`, the delete's clear, and the delete's read-back — can't drift apart. **A delete that stops at V leaves an orphaned "Yes" in W.**
      - **The header is written on every submission** (like the follow-up columns write theirs), in its own `try`/`catch`. The grade is already saved by then, and an unlabelled column must never be reported as a failed submission.
      - **The Certified column has three answers for a manual grade, not two** (`manualCertifiedCell`): at/above the bar *and* ticked → **Yes**; under the bar, or recorded as not ticked → **No**; at/above the bar with a **blank** W → **"Bar met"**, a neutral badge whose tooltip says the second half wasn't recorded and points at the Dashboard. Saying "Yes" off the total alone is exactly the shortcut the two-part rule exists to prevent, so **don't make old rows read as certified.**
      - **A session that was AI-graded still shows the AI's verdict** — that column's job during Phase 2 calibration is to show what the AI concluded. The manual verdict fills the cell only when there's no AI grade to show. `certifiedCellFor()` is the one place that decides, shared by Recent Submissions and the expanded roster row so the two can't disagree.
      - Front-end only. **No `Code.gs` change, no redeploy** — Code.gs appends A–S and never reads past S.
    - **Confirmation is inline and *before* submit, not a dialog after it.** `renderCertPreview()` shows what pressing Submit will do to the Dashboard while the score is still on screen and editable — the moment a typo that crosses the bar would actually get caught. A modal a minute later, after the upload, arrives too late to be a real check. It also spells out the *below-the-bar* case, which answers "why didn't it certify?" before it's asked.
    - **The Roster row number is stamped on each person before blank rows are filtered out** (`ROSTER_ROW_NUMBER` in `loadRoster`). Position in `rosterRows` is not the row in the Sheet; one empty row anywhere above someone would shift the write onto the wrong person — and for a certification column, that means signing off somebody who wasn't graded.
    - **The certification column is found by header name, not a hardcoded letter**, so inserting a column in the Roster can't redirect the write onto its neighbour.
    - **The Roster write is a separate `try`/`catch` from the grade write.** They're different Sheets; if the Roster write fails the submission still stands, and the status line says which name to go tick by hand rather than implying the whole submission failed.
    - Only **Training Day** and **Team On-Site Day** have certification columns (`SESSION_CERT_COLUMN`), so the feature is inert on the preview tabs by construction rather than by a special case.
    - Front-end only — the app already holds the `spreadsheets` scope. **No `Code.gs` change, no redeploy.**
  - **Audio is optional on the grading form (2026-08-13).** It used to be `required`, which meant that the moment a passing manual grade could certify somebody, there was no way to actually *use* that — certifying from a paper evaluation with no recording was blocked by the form (Jordan hit this immediately). Now the rule is **audio, a manual grade, or both — but not neither**, since a submission with no recording and no score is an empty row. Consequences that were checked rather than assumed:
    - **Link-less rows don't merge.** `fetchSubmissions()` groups the manual row and the AI row of one session by their shared Audio File Link (column G). Rows with a blank link already went down a separate `noLink` path that keeps each as its own submission — so several audio-less manual grades can't collapse into one. This was the thing most likely to break, and it didn't.
    - **No recording means no AI call**, and the status line says so outright rather than leaving someone waiting for a result that was never coming. No Drive round trip either — the row just carries a blank link.
    - **The Dashboard badge counts "sessions", not "recordings".** Once a session can exist without audio, the old "3 recordings" label would have been overstating what's there to listen to.
    - Every render path (Recent Submissions, Follow-Ups, the expanded roster row) already guarded on a blank `audioLink` and shows N/A or hides the Listen link.
    - **"No audio" is now said out loud, not left blank (2026-08-13).** Hiding the link left an empty gap that reads as "something failed to load" rather than "there is nothing to listen to" — Jordan asked for it after seeing an "Open in Drive" that opened onto nothing. One helper, `audioLinkOf()`, decides it in both views: **only a real `http(s)` link counts as a recording**, so whitespace or a note typed into column G by hand renders as "No recording" (expanded roster row) / "None" (Recent Submissions) instead of as a link with nothing behind it. It also gates `driveFileIdFromLink`, so junk text can't build a player. Note this can't detect the *other* dead-link case — a link whose Drive file was deleted or belongs to an account the viewer isn't signed into — which would need a Drive call per row and isn't built.
  - **Grading somebody the Roster says isn't ready yet — a warning with an override (added 2026-08-14).** Jordan, looking at the Dashboard: *"For the people that have pending they should not be able to upload their team on site clip or team on site or get graded, can you have a warning that pops up and the ability to override it if need be."* Pick a person on either live tab who still has training steps showing **Pending** and a loud bar appears under the picker; Submit stops until one tick inside that bar overrides it. Decisions worth not re-litigating:
    - **Which steps gate a session is one constant, `SESSION_PREREQUISITES`, and both live types share the same three:** `Day 1 - Meeting Attended`, `Day 1 - Training Watched`, `Day 2 - Ops/District Manager Training`. **Confirmed by Jordan on 2026-08-14**, including that the gate applies to **Training Day as well as Team On-Site Day** — a manager delivering a Training Day shouldn't be graded on it before they've watched it themselves.
    - **`Team On-Site Day Completed` is deliberately NOT a prerequisite**, and neither are the two Certified columns. That column is ticked *after* the day happens, so requiring it would warn on every genuine first Team On-Site Day upload — the recording **is** that day. Requiring a Certified column would block the entire roster, since nobody is certified before the session that certifies them. (On the live roster this is the difference between warning on Shan Brar and Ishandeep Luthra, which is right, and also warning on Karanveer Sanghera, which would be wrong.)
    - **It's a warning with an override, not a block.** The Roster is maintained by hand and can lag reality, and somebody who genuinely needs a session graded out of order shouldn't have to go and edit a Sheet before the app will let them. Equally it isn't advisory-only: Submit actually stops, because a warning that can be ignored by reflex is the one that gets ignored.
    - **Inline and before Submit, not a popup after it** — the same call `renderCertPreview()` makes, for the same reason. A dialog after the fact arrives once the audio is already in Drive and the Gemini passes are already paid for.
    - **The gate is checked FIRST in the submit handler, ahead of "add a recording or a manual grade".** "This person shouldn't be graded yet" is an objection that filling the form in can't answer, so raising it first avoids sending someone off to do work they're about to be stopped on.
    - **The override is scoped to the person AND the session type it was granted for** (`prereqOverride = {name, sessionType}`), held outside the checkbox because the bar is re-drawn constantly. Ticking "grade anyway" for one person must not carry to the next person picked, to the other tab, or past a successful submission — every one of those re-draws the gate, which drops a stale override. Verified for all four.
    - **A ticked override goes to `.acknowledged`, not to green.** It keeps the warning tone and keeps listing the Pending steps, losing only the lift. Nothing about the training path got fixed by ticking a box, and `.alert-bar.done` (green) means *resolved* everywhere else in this file.
    - **"Submit stopped —" lives in the bar, not in `#grade-status`.** The button is at the bottom of the form and the bar at the top; putting the reason next to the override that clears it is the only place both are visible at once. The bar scrolls itself in on a blocked submit, **plain, with no `behavior: 'smooth'`** — same recorded reason as the un-certify offer.
    - **A warning the app can't stand behind is worse than none**, so `missingPrerequisites()` returns *nothing* (not everything) for a name that isn't on the Roster, and skips any required column the Roster doesn't actually have. Renaming a Roster column would otherwise flag all 30 people with no tick anywhere that could clear it.
    - **`ROSTER_SHORT_LABELS` was hoisted out of `renderDashboardTable`** so the warning names a step word-for-word as the Dashboard column header someone is being sent to go look at. Two copies of those labels would drift.
    - The trainee dropdown marks affected people `· training not complete`, so the mistake can be avoided before it's made. `populateTraineeDropdown()` now preserves the current selection, because it re-runs on a Refresh and on a tab switch.
    - Front-end only. The override is **not** recorded in the Grading Log — that would mean a column X and bumping `GRADING_LOG_LAST_COLUMN`, and Jordan asked for a guardrail, not an audit trail. Worth offering if "who overrode this" ever comes up. **No `Code.gs` change, no redeploy.**
    - **Verified in a real browser, both schemes, with screenshots** — the Browser pane *was* compositing frames this session, unlike 2026-08-14's earlier work. Checked: the classification for all four roster shapes, the submit block (0 calls past the gate) and the pass-through once overridden, override scoping across person/tab/reset, the Dashboard unchanged after the label hoist, no page overflow at 1280 or 375, clean console. **One real defect found and fixed in passing:** `.alert-bar .notice-err` at full-strength `--critical` measured **4.04:1** on the warning tint in light mode — under AA. It now uses the same `color-mix(… 78%, var(--text-primary))` treatment as `.alert-bar-title` (5.63 light / 6.08 dark). That fix also applies to the un-certify offer's failure message, which had the same problem.
  - **Changing a roster status by hand, from the Dashboard (added 2026-08-14).** Jordan, on the roster table: *"can you update this so that you can also manually change these sections on the website? from meeting to training certified?"* All six status columns — the four training steps and both certification columns — are now editable in place: clicking a badge writes `Yes`/`No` into that person's row on the Roster sheet. `rosterToggleCell` renders them, `wireRosterToggles` drives them, `writeRosterCell` does the write. Decisions worth not re-litigating:
    - **This is allowed because a HUMAN is clicking it** — the same permission a passing manual grade already has (see "Auto-certifying from a manual grade"). **An AI grade still never writes to the Roster, and mustn't until Phase 5.** Don't let this feature be cited as precedent for that.
    - **A certification arms and takes a second click; a training step flips on the first.** Getting a training step wrong is one click away from being right, where signing somebody off — or taking a sign-off away — deserves the second look. Same arm/confirm idiom as the delete button in Recent Submissions, so it's already familiar. The armed label says which way it's about to go (`Certify?` / `Remove?`), lapses after 5s, and arming a second certification disarms the first.
    - **The cell is READ BACK before the badge changes.** A write that quietly doesn't stick has bitten this app before (it's why the delete reads its rows back), and here it would leave the Dashboard confidently showing a certification the Sheet doesn't have. Verified against a stubbed Sheet that accepts the write and then discards it: the badge reverts and the bar names the cell.
    - **It never calls `renderDashboardTable()`.** A repaint would shut an expanded row and throw away an armed button, so only the one badge is redrawn and the four dependants are refreshed by name: the stat strip, the trainee dropdown's "training not complete" marker, the out-of-order warning, and the certify preview. `refreshRosterRecordingCounts()` gained a matching guard (`rosterEditInProgress()`) so a background refresh can't repaint mid-write or mid-confirmation.
    - **Turning something off writes `"No"`, not a blank** — same reason `uncertifyOnRoster` does. The badges read the two identically, but a written "No" says somebody decided this.
    - **A column the Roster doesn't have renders as a plain badge, not a button.** Renaming a Roster column shouldn't offer a control that could only fail.
    - **Undoing a certification is now permanently available here**, which is a genuine change from the un-certify offer's note that "a stranded Yes has to be changed by hand on the Roster Sheet". That note's *reasoning* still holds — the app still never un-certifies on its own initiative, and the post-delete offer still exists because a delete is the moment the question gets raised. What's changed is only that answering it no longer means leaving the app.
    - Front-end only. The app already holds the `spreadsheets` scope. **No `Code.gs` change, no redeploy.**
    - **Verified in a real browser against a stubbed Sheets backend, both schemes** (no screenshots — the Browser pane wasn't compositing this session). Checked: the write lands on the right cell for a person *below a blank Roster row* (`I6`, not `I5` — the `ROSTER_ROW_NUMBER` hazard), a name containing an apostrophe round-trips through `data-person`, the first click on a certification issues zero writes, both failure paths revert the badge and raise the bar, an expanded row survives a toggle, ticking a step from the Dashboard clears the grading form's warning and the dropdown marker while preserving the selection, no page overflow at 1280 or 375, the table still fits in one view (1132px, no scroll), clean console.
    - **One defect found and fixed in passing:** the in-flight badge was `.badge.pending`, the same tone as the "Pending" badge it replaces — so "Saving…" was nearly invisible, and measured **1.91:1** in light mode. It now has its own neutral `.badge.saving` (16.4 light / 19.3 dark).
    - **Found but deliberately NOT fixed — the Dashboard badge palette fails AA in light mode**, and always has: measured at 11px bold, `Done` **3.38**, `Not yet` **3.70**, `Pending` **1.91**, against the 4.5 needed. This is pre-existing and app-wide, not something this feature introduced (dark mode is fine — 8–11:1), and fixing it means restyling badges Jordan signed off on a day earlier. Raised with him rather than changed unilaterally. The established fix if it's wanted is the `color-mix(… %, var(--text-primary))` treatment already used on `.alert-bar-title` and `.notice-err`.
  - **Adding a person to the Roster, from the Dashboard (added 2026-08-14).** Jordan, on the roster table: *"Can you add + button or something like that and looks good that you can add a person directly from it?"* An orange **+ Add person** button sits in the Dashboard toolbar next to Refresh and opens an inline form (`#add-person-form`) that appends a row to the Roster sheet: `submitAddPerson` validates, `addPersonToRoster` writes and reads back. This completes the loop the badge toggles started — the Dashboard could change every status on a person but couldn't create the person, so a new hire still meant leaving the app. Decisions worth not re-litigating:
    - **Same permission as the badge toggles: a HUMAN is adding this row.** Nothing about an AI grade writing to the Roster has changed, and this mustn't be cited as precedent for it — that's still Phase 5.
    - **The row is built against the HEADER ROW, never by position** (`rosterHeaders.map`), so a column inserted into the Roster moves its value with it instead of putting a name into somebody's region. Every column the form doesn't collect is left **blank on purpose** — blank reads as Pending / Not yet, which is the honest starting point, and the six status badges then fill it in one click each. The form says so out loud, because "why is everything Pending?" is otherwise the first question.
    - **A duplicate name is REFUSED, not warned about.** `rosterPerson`, `submissionsForPerson` and every certification write join the two Sheets on the name, so two people sharing one collapse into whichever the Sheet lists first — including for a write that signs somebody off. Matched case-insensitively with whitespace collapsed (`" mathew   marshall "` is caught), and the name is stored collapsed for the same reason.
    - **Role and Region are pickers over what the Roster already uses**, with an explicit `+ A role that isn't listed…` option that reveals a text box. Free text alone creates `"BC1 "` and `"Field manager"` as separate groups on the stat strip and quietly drops the person out of the filter they belong in. The pickers are refilled by `populateFilterOptions` on every roster read, so a role added this session is on the list next time.
    - **The append is READ BACK before anything on screen says it worked** (same reason `writeRosterCell` reads its cell back), by re-reading the row Google reports in `updates.updatedRange`. **If the read-back disagrees, the message does not claim nothing was written** — an append that reports success and reads back wrong may well have left a row behind, and this can't tell, so it says to check that row in the Sheet before retrying. A confident "nothing was added" is how a duplicate gets made.
    - **Afterwards the roster is RELOADED rather than the new person being pushed into `rosterRows` by hand.** `loadRoster` is what stamps `ROSTER_ROW_NUMBER`, and a guessed row number on a certification column signs off the wrong person. Verified: someone added below a blank Roster row gets their badge writes at `I6`/`D6`, not `I5`/`D5`.
    - **A successful add scrolls to the new row and tints it green for 6 seconds** (`.just-added`), because a sorted roster can put a new name a screen away from the button that added them. The tint is a **resting style, not an animation**, and the scroll is plain with no `behavior:'smooth'` — the recorded reason both times.
    - **The entrance slide animates `transform` only, never `opacity`.** Anything an animation controls is whatever frame that animation is on, so a browser that doesn't advance animations would leave a panel somebody just opened sitting at opacity 0. Caught for real in the harness (the pane wasn't compositing, so the form was invisible until this was changed). Off by seven pixels is a survivable worst case; invisible isn't.
    - **The filter row's buttons are wrapped in `.filter-actions`.** `.filters .btn { margin-left: auto }` on two buttons splits the free space between them and strands one mid-row.
    - Front-end only. `sheetsAppend` already existed. **No `Code.gs` change, no redeploy.**
    - **Two fixes made in passing, both pre-existing:**
      - **A Refresh silently reset the Dashboard's region and role filters** (`populateFilterOptions` rebuilt the options without keeping the selection, while the search box beside it kept what was typed). They're preserved now — which is also what makes the "the filters you have set are hiding their row" note reachable, since without it an add always widened the table back out to everyone.
      - **`.edit-status` (the green "Saved —" line every Roster write uses) measured 4.45:1 in light mode**, a hair under AA. Now `color-mix(in srgb, var(--good) 82%, var(--text-primary))` → 5.87 light / 8.4 dark.
    - **Verified in a real browser against a stubbed Sheets backend, both schemes** (no screenshots — the Browser pane wasn't compositing this session; worth an actual look next time one can). Checked: every validation path issues **zero writes** (blank name, duplicate in three spellings, apostrophe duplicate, missing role, missing region, "not listed" left empty); the happy path's append row, its read-back, the roster reload, the flash, the status line, and the new role/region reaching the filters, the trainee dropdown and the stat strip; the row-number hazard above; both failure paths (append 500, read-back mismatch) keeping the form open with its values and raising the bar without a status line; the filters-hiding-the-row note; Cancel, Escape and a second button click all closing and clearing; contrast of every new string (title 16.4/19.3, sub 6.6/11.4, error 5.9/8.5, buttons 7.7–8.7); no page overflow at 1280 or 375, where the grid collapses to one column; clean console.
    - **One bug caught only because it ran in a browser:** the filter-persistence fix declared a second `keepRegion` in `populateFilterOptions`, which already had one for the Follow-Ups select — a `SyntaxError` that killed the entire script and left the app stuck on the sign-in screen. It reviewed as fine.
  - **A manager who covers more than one region — the `Also Covers` column (added 2026-08-14).** Jordan: *"A district Manager Braydon might look over individually BC3 ... and then be the direct report for BC1 and BC5 ... an operations manager looks over a state or province ... BC and AB. how would you classify those under region so they are picked up when pulling a region?"* Every region check in the app was an exact match on one cell, so a district manager could only be filed under one region and a province-level person sat in the dropdown as if `BC` were a sibling of `BC1`. The Roster now has an **`Also Covers`** column, and `personInRegion()` is the one place that decides whether somebody belongs in a region's view. Decisions worth not re-litigating, **both of them Jordan's calls on 2026-08-14**:
    - **Two columns, because "mine" and "I oversee it" are different facts.** `Region` stays the one region that is actually theirs (Braydon = BC3); `Also Covers` holds the rest (BC1, BC5). The option to make `Region` itself a list was offered and turned down. One thing genuinely depends on the split: **`regionForPerson()` is still primary-only**, so a graded session belongs to exactly one region — otherwise the same recording appears in the Follow-Ups queue once per region its owner covers, and the queue reads as three sessions needing three conversations. **Covered regions widen who you FIND, not how many sessions exist.**
    - **Matching is LITERAL in both directions.** `BC` is *not* treated as containing `BC1`–`BC7`: filtering BC1 lists the people whose Region is BC1 plus anyone whose `Also Covers` names BC1 outright, and a province-level person appears under `BC`, which is its own view. A province-rolls-down-to-its-territories rule was explicitly offered and declined ("Only in the BC view"), so **don't add code that parses `BC1` into `BC` + `1`** — it would be a quiet reversal of a decision, not a cleanup.
    - **A covered region is drawn in the table, not just used by the filter** (`.region-also`, "also BC1, BC5" under the primary). Filtering BC1 and getting back a row whose Region column reads BC3 looks like a bug; the cell explains itself instead. It's `--text-secondary` at 10.5px (8.4 light / 9.9 dark) — `--text-muted` measures ~3.2 and would fail AA at that size.
    - **The region dropdowns are built from both columns** (`allRosterRegions`), so a province that only exists in an `Also Covers` cell is still filterable — that's how `AB` becomes selectable. Sorted with `localeCompare(…, {numeric: true})` so `BC10` lands after `BC2`.
    - **Per-region figures now double-count a multi-region manager on purpose.** Somebody covering three regions is counted once in each of the three, because the stat strip has to agree with the table below it. So these figures can't be summed across regions. Noted in the code at `renderDashboardStats`.
    - **If the Roster has no `Also Covers` column, none of this exists** — the filter falls back to `Region` alone and the add-person form drops the field entirely, the same principle as a missing certification column rendering a plain badge rather than a control that could only fail. **Verified as its own case** (`?nocovers=1` in the test harness): no "also" lines, no invented column in the appended row, no console error. **Jordan has to add the column header to the Roster sheet himself** — the app deliberately doesn't create it, since guessing where a column goes in his sheet is riskier than one manual step.
    - The add-person form's grid is `repeat(auto-fit, minmax(205px, 1fr))` precisely because it is four fields with the column and three without, and neither should strand a lone field on a second row.
    - Front-end only. **No `Code.gs` change, no redeploy.**
    - **Verified in a real browser against a stubbed Sheets backend, both schemes** (still no screenshots — the pane wasn't compositing). Checked: all seven region views against hand-counted expectations, including `BC` returning *only* the province-level person; the stat strip agreeing with the table for a covered region; `regionForPerson` staying primary-only; the covers field's parsing (`" bc2 ,BC7;  BC 9 , , BC2"` with region BC2 → `"BC7, BC 9"` — own region dropped in both spellings, blanks dropped, inner whitespace collapsed); a **seven-region** covers list wrapping to three lines inside the 102px column with the table still exactly 1132px and no page or table scroll; the no-column variant; contrast of every new string; 375px collapsing to one column; clean console.
    - **One defect found and fixed in passing:** `.field-optional` measures **4.09:1** on this form's sunken panel at 12px. Fixed **scoped to `.add-person-form`** rather than globally, because a parallel session was mid-way through a palette pass on the same file and the class also sits on white panels where it clears (4.62). If that pass restyles `.field-optional`, this override can go.
  - **The Grader field is a picker, not a text box (2026-08-14).** Jordan, on the grading form: *"Have this as a drop down menu of trainers, so those would be district managers or operations managers on the dashboard tab."* `#g-grader` is now a `<select>` filled by `populateGraderDropdown()` from the Roster. Decisions worth not re-litigating:
    - **Who's on the list is decided by the Roster's own Role column, never a hardcoded list of names** — the same principle as the Dashboard's role counts. `GRADER_ROLE_KEYWORDS` holds `district manager` / `operations manager` / `ops manager`, and `isGraderRole()` matches **lower-cased, whitespace-collapsed, as a substring**, so `District Manager`, `district manager ` and `Senior District Manager` all land here. Role is typed into a Sheet by hand; an exact match would silently drop somebody for a trailing space.
    - **There is an escape hatch, and it's needed.** `+ Someone not listed…` reveals a text box (`#g-grader-other`), the same idiom as the add-person form's "a role that isn't listed" boxes. The Roster lags reality — a newly promoted district manager shouldn't have to go and edit a Sheet before the app will let them record a session. The box is cleared and its `required` dropped whenever it isn't showing (`syncGraderOtherField`), so **a hidden required field can never block a submit with an error nobody can see** — verified, including the DNA-over-"not listed" path.
    - **An empty Roster list falls back to listing everyone, and says why.** No district or operations managers found — a renamed Role column, or roles spelled some other way — and the picker lists the whole Roster with a hint line explaining it, rather than being a control that can't be answered. Same principle as a missing certification column rendering a plain badge.
    - **"Did not attend" is a hidden option, set programmatically.** The checkbox needs the field to *read* as `Did not attend` (that string is what Recent Submissions checks to draw an em-dash instead of "None"), and a `Did not attend` option sitting in the visible list would read as a person. It's built at page load with an empty roster too, so ticking the box before the Roster arrives can't set a value with no option behind it and quietly record a blank grader.
    - **The name is read at submit time, before the upload** (`graderNameValue()`, alongside where the session type is captured), not after it. The form is editable for the whole of a multi-minute upload.
    - The picker keeps its selection across a roster re-read, for the same reason `populateTraineeDropdown` does. Nothing joins on the grader name — it's display-only in column D — so free text there stays low-risk.
    - Front-end only. **No `Code.gs` change, no redeploy.**
    - **Verified in a real browser (the file loaded as a page, with `rosterRows` driven directly), both schemes — no screenshots, the Browser pane wasn't compositing again.** Checked: the trainer filter against a roster containing field managers, two spellings of district manager, an operations manager, a `Senior District Manager`, a blank name and a blank role; alphabetical order; an apostrophe name round-tripping through an option value; the reveal/clear/`required` behaviour of the typed box; whitespace collapsing; both refusal paths issuing **zero writes** (empty selection blocked by the select's own `required` with focus landing on it, whitespace-only typed name caught by the handler); the grader name landing in column D for a picked name, a typed name and "did not attend"; the form reset after a submit and on a tab switch; the fallback list and the empty-roster case; contrast (picker 16.7 light / 17.9 dark, hint 8.4 / 9.9); no page overflow at 1280 or 375; clean console.
  - **A second roster board — the Recruiting Dashboard, and the Dashboard tab renamed to Training/TOSD (2026-08-14).** Jordan, pointing at the roster's certification columns: *"Can you add in this part here Conversion call certified... Actually can you take the dash board tab and put the name to Training/TOSD and then make a new tab that is similair to it of Recruiting Dashboard and then add certifications for all the recruting components, also not it would only be Regional Managers and upwards who would be on it because Field Managers do not Recruit."* Decisions worth not re-litigating:
    - **There is ONE board renderer, taking a config (`ROSTER_BOARDS`), not two copies of the table.** `renderDashboardTable`, `renderDashboardStats`, `populateFilterOptions`, `refreshRosterRecordingCounts` and `renderDashboardKeepingOpenRow` all take a board; the board carries its element IDs, its status columns, who it includes and which session types it's about. Two hand-maintained copies of a table this size is exactly the drift this file warns about everywhere else. The **Training/TOSD board's output is unchanged** — same headers, same colspan, same stat cards — verified against the pre-refactor values.
    - **Element IDs still say "dashboard" for the Training/TOSD board.** Only the visible label moved, so nothing else in the file had to. `#dashboard-table-wrap`'s CSS became **`.roster-table-wrap`** (a class on both wraps) for the same reason — one set of rules, two tables.
    - **Five certification columns, one per recruiting SESSION TYPE, in process order:** `Certified - Conversion Calls`, `- Stage 1 Interviews`, `- Reference Checks`, `- Parent Reference Checks`, `- Stage 2 Interviews`. **Reference checks get TWO columns** because the candidate and parent checks are two session types with two rubrics — the same reason they need two `sessionType` strings in `Code.gs`, even sharing one tab.
    - **These were deliberately NOT in `SESSION_CERT_COLUMN` — and Conversion Call now IS (Jordan, 2026-08-14, see the entry below).** That map is what lets a passing manual grade write a certification onto the Roster and what raises the un-certify offer after a delete. The reasoning here — four of the five types have no rubric loaded at all, so there is no score to certify off — **still governs the other four and they stay out of it.** Conversion Call was the one this note said should be wired in only as a deliberate decision rather than as a side effect of adding a column; Jordan made that decision.
    - **Who appears is decided by EXCLUDING field managers (`NON_RECRUITING_ROLE_KEYWORDS`), never by listing the senior roles.** An include-list silently drops a title nobody has invented yet, and a certification you can't see is one you can't record; the exclusion errs the other way, where a wrong row is visible and one word to fix. Someone with a **blank Role appears** for the same reason — an empty cell isn't proof they're a field manager. Matched lower-cased, whitespace-collapsed, substring, like `isGraderRole` — verified that `Field Manager`, `field manager ` and `Senior Field Manager` all land together.
    - **A board only counts and only opens the sessions it's about.** `submissionsForPerson(name, types)` takes an optional type filter, so the Recruiting board's "N sessions" badge and its expanded row show recruiting calls, not the person's Training Days, and its follow-up card can't report a Training Day. A **blank Session Type reads as Training Day** (the same default everything else uses), so legacy rows stay off the recruiting board. Element IDs for the expanded rows carry the board key — both boards are in the DOM at once, and two elements sharing an id renders one person's card into the other board's row.
    - **The app OFFERS to add the missing Roster columns, which reverses the Also Covers precedent on purpose.** That was one header typed by hand; this is five, each then looked up by exact text — five chances to create a column nothing can find. `addRecruitingColumnsToRoster` **appends** to the right of the last header in use (never inserts, so nothing in the sheet moves), reads the headers back, and **refuses outright past column Z** because that's as far as `loadRoster` reads. A read-back mismatch **does not claim nothing was written** — something may have landed, and a confident "nothing happened" is how a column gets added twice. Until the columns exist every badge is a plain un-clickable one (`rosterToggleCell`'s existing rule) and the bar above the table says which headers are missing.
    - **`rosterHeaders` is now trimmed on read.** Every column in this app is found by exact header text, so a header typed with a trailing space read as a column that isn't there — and rendered as a badge nobody could click. One line, and it removes a whole class of silent failure.
    - **Add person stays on the Training/TOSD board only.** It lists the whole Roster, so that's where a new person belongs; the Recruiting note says so rather than leaving somebody hunting for the button.
    - Front-end only. The app already holds the `spreadsheets` scope. **No `Code.gs` change, no redeploy.**
    - **Verified in a real browser against a stubbed Sheets backend, both schemes** (no screenshots — the Browser pane wasn't compositing again). Checked: the role classification for all six shapes; per-board filter independence, including BC1 returning Deacon (own region) **and** Braydon (Also Covers) with the stat strip agreeing; every stat figure hand-counted; the toggle writing to **L12** (right column, right row) with the first click issuing **zero** writes; a discarded write reverting the badge and raising the bar on the **recruiting** board only; the add-columns happy path (one write, `L1:P1`, notice cleared, 30 toggles live) and both failure paths keeping the button usable; session-type scoping on counts, expanded rows and the empty state; no duplicate element IDs across boards; the open-row and armed-toggle guards; the Training/TOSD board byte-comparable to before; contrast of every new string (5.0–18.8 light, 5.2–16.8 dark, zero failures); tables fitting 1132px at 1280 with no page scroll.
    - **One real defect found and fixed by that verification, and only a browser would have caught it:** the notice's button pushed the **page** into horizontal scroll at 375px (388px against a 375px viewport). `.alert-bar .btn` is `white-space: nowrap` — which the two-button bars need — so a long label can't wrap. The label is now "Add them to the Roster" (196px) with the long form on `aria-label`. **Don't lengthen it back.**
  - **A Conversion Call now certifies from a passing MANUAL grade (added 2026-08-14, `SESSION_CERT_COLUMN`).** Jordan, looking at Recent Submissions on the Conversion Call tab: *"Can you add certified here?"* — and, asked what that should mean: *"That if they got a passing grade they are now certified, only for manual entries."* One entry added to `SESSION_CERT_COLUMN` and the whole existing certification path came with it: the sections tick, the before-submit preview, the Roster write, and the un-certify offer after a delete. **This is the decision the recruiting-board entry above said should be made deliberately rather than fall out of adding a column.** Decisions worth not re-litigating:
    - **Manual grades only, and that is the entire safety property — it is unchanged, not weakened.** `certifyOnRoster` is only ever reached from `manualGradePasses`, which reads the number a human typed and the box a human ticked. **An AI grade still writes nothing to the Roster on any session type**, and mustn't until Phase 5. The AI card still says **Pass / Fail** rather than Certified here (`VERDICT_WORDS`), which matters more now than it did before: the word "Certified" over an AI score would claim precisely the authority this change withholds.
    - **The other four recruiting types stay out**, and the reason is not caution about wiring — they have **no rubric loaded**, so there is no score to certify off. Those five badges remain a manager's tick and nothing else.
    - **One pass, not three.** No entry in `SESSION_PASSES_REQUIRED`, so `passesRequired` returns 1 and the Training Day path runs — which is right: a conversion call is one call, not a day made of three site visits. Bar is **48/80 and every section ≥ 6**, the same two-part rule as everywhere else.
    - **"The Dashboard" stopped being an unambiguous phrase, and every sentence that used it was fixed.** A Conversion Call tick is on the **Recruiting Dashboard**; the other board is labelled **Training/TOSD**. `certBoardLabel()` names the right one, off `ROSTER_BOARDS` so the wording matches the tab's own label. It matters most on the un-certify bar, whose whole job is to say *a badge on another tab is now wrong* — sending somebody to the tab where the person genuinely isn't certified reads as the app being confused. Also applied to the cert preview, `certifyOnRoster`'s status sentences and the submit-path messages.
    - **A missing Roster column is caught BEFORE submit, not after.** Unlike the two training columns, `Certified - Conversion Calls` is one the Roster may not have yet (it's added on request from the Recruiting board), so a passing grade can easily arrive before the header exists. `certColumnMissing()` makes the preview say which column is absent and where the button is, rather than promising a certification the submit would then walk back. **An unread Roster is deliberately not reported as missing** — same null-vs-0 distinction `passingManualGrades` makes; claiming the column is absent mid-read would send someone to add one they already have.
    - **The Recruiting Dashboard's on-screen note was rewritten**, because "These are tracked, not graded" had become false for one of its five columns. It now states the 48/80 rule for Conversion Calls, says the other four are hand-ticked only, and keeps the AI-never-ticks-anything line as its own sentence.
      - **AND IT WENT STALE AGAIN WITHIN A DAY — rewritten a third time on 2026-08-15, and this is now a standing hazard rather than an incident.** As Stage 2 and both reference checks joined `SESSION_CERT_COLUMN` the note was left saying *"the two interviews are ticked by hand"* and *"the two interview rubrics aren't loaded yet"*, while Stage 2 was in fact certifying at 54/90 — **user-facing copy telling somebody a badge won't move, over an app that moves it.** It now reads "Four of these certify from a grade; Stage 1 Interview is ticked by hand", names all four thresholds, and says outright that a Networking Activity is graded but has no column here so it signs nobody off. **Two internal comments carried the same stale claim** (at `RECRUITING_CERT_COLUMNS` and above `SESSION_CERT_COLUMN`, both still saying the other four had no rubric loaded) and were corrected in the same pass. **The rule to take from this: adding a session type to `SESSION_CERT_COLUMN` is not done until that note and those two comments have been re-read** — the map is three places, not one, and the one people actually see is the note.
    - Front-end only. **No `Code.gs` change, no redeploy** — the backend grades one call and has no opinion about certification.
    - **Verified in a real browser against a stubbed Sheets backend, both schemes, 49 assertions** (no screenshots — the Browser pane wasn't compositing again, though the viewport was real this time). Checked: the pass test at the 48/47 boundary and unticked; `certColumnMissing` for present / absent / unread; all six preview branches naming the right board; the Roster write landing on **F7** (right column, right row) with the missing-column path issuing **zero** writes; `submissionPassedManually` for ticked / under-bar / recorded-No / blank; the un-certify offer firing for a Conversion Call and naming the **Recruiting Dashboard** tab, staying quiet when another passing grade is still on file, and a Training Day offer still naming **Training/TOSD**; the Passed column reading Yes / No / Bar met for a manual-only row; no stray "of 1 passes" progress line on the recruiting badge; Training Day and Team On-Site Day previews unchanged; contrast 5.28–15.65 light, 7.04–12.36 dark; no page or table overflow at 1280 or 375; clean console.
  - **BOTH REFERENCE CHECKS WENT LIVE — two session types behind one tab (2026-08-15, `Code.gs` V19).** Jordan handed over both rubrics and asked for them wired up, restating the design he set back in August: *"both should be under the same tab and there is just a box or something that you choose which one it is."* He also gave the reason the parent call exists at all: *"we do the parent reference check for people under 23 to get ahead of helicopter parents and get them on board because we have lost candidates to this."* Decisions worth not re-litigating:
    - **ONE TAB, TWO SESSION TYPES, and `variants` is the whole mechanism.** This is the first tab in the app that covers more than one session type. They are genuinely separate rubrics — different categories, different weights, different automatic fails — so they get two `sessionType` strings, two Recent Submissions lists, two certification columns and two entries on the Compare picker. The tab is the *only* thing they share.
    - **`currentSessionType` is derived from the picker, not from the tab** (`sessionTypeForTab`). Everything downstream already keyed off that one variable — the manual-grade cap, the labels, the pill, the cert preview, which Recent Submissions rows show — so pointing it at the picker made all of it follow for free. It falls back to the tab's declared `sessionType` for any unexpected value, so a stale one can never land the app on a type Code.gs has no rubric for.
    - **THE PICKER LIVES OUTSIDE THE `<form>`, and this is load-bearing rather than tidy.** `resetGradingPanel()` calls `form.reset()`, which silently returns every control to its first option. Inside the form, the picker would snap back to "Reference Check" while `currentSessionType` still said "Parent Reference Check" — a form labelled one rubric and submitting the other, with nothing on screen wrong. Same class of reason the Compare Save button sits outside `#compare-wrap`.
    - **Switching variant resets the panel, exactly like switching tabs**, because it is the same event: a different rubric on a different scale with a different list underneath. Verified that a half-filled form does not survive it.
    - **The pill and the Recent Submissions heading name the SESSION TYPE, not the tab.** Identical output on every other tab (label === sessionType there), and on this one it stops the list being headed "Reference Checks" while filtering to a single variant.
    - **THE ONE RULE WITH NO EQUIVALENT ANYWHERE ELSE IN THIS APP: a negative reference is not a low score.** Both source rubrics say so in a callout box. It is the mistake a general-purpose grader makes by default — a call full of concerns *feels* like a bad call — and getting it backwards would train DECO's managers to fish for praise. The prompts state it in rule 2 and again in the rubric body. **The Conversion Call's version of that bullet is about ADVANCEMENT, which is a different claim; there are tests that neither leaks into the other.**
    - **Flattened to 70 with a 42 bar**, the same decision Jordan made for the Conversion Call: 60% of 70 *and* exactly 7 × the section pass mark, so the threshold can never contradict the every-section rule. Consequences (the app is stricter than the source rubrics, and the automatic fails are reported rather than scored) are recorded in `docs/reference-check-rubrics.md`.
    - **BOTH CERTIFY FROM A PASSING MANUAL GRADE** — Jordan's explicit choice when asked, 2026-08-15 — writing to `Certified - Reference Checks` and `Certified - Parent Reference Checks`. **Two columns, never one:** passing the candidate check certifies nobody for the parent check. The safety property is untouched: only a manual grade a human signed off ever reaches `certifyOnRoster`, and **an AI grade still writes nothing to the Roster on any session type.** The AI card says Pass / Fail for both, for exactly that reason.
    - **The `parent_` key prefix is a storage concern that was leaking onto the screen, and only rendering a card caught it.** `SECTION_LABELS` is one flat map across every session type, and both rubrics have a "Quality of Reference Questions" category at different numbers — so the parent keys need prefixing. Left alone, every section on a Parent Reference Check card read "Parent Accurate Role Explanation", "Parent Communication Tone", the word repeated down a card already entirely about a parent call. `SECTION_KEY_PREFIXES` strips it at display time. **The fix also collapsed three copy-pasted label transforms into the one `sectionLabel()` they all claimed to be** — a prefix stripped in two of the three would have had the corrections list disagreeing with the section it points at.
      - **THE SAME LEAK SHIPPED AGAIN ON THE NEXT TWO RUBRICS, and the entry above is why it was easy to miss — it reads as a fix, not as a rule (found and fixed 2026-08-15, front-end only).** `SECTION_KEY_PREFIXES` was left holding `parent_` alone while Stage 2's nine keys all begin `stage2_` and the Networking Activity's eight all begin `networking_`, so those cards read **"Stage2 Leadership Assessment"** and **"Networking Building The List"** — the identical defect, on cards where the word repeats down all nine and all eight rows. It is now a three-entry list with a note saying to check for a prefix whenever a rubric is added. **Only rendering a card catches this**, both times: the keys look fine in `Code.gs`, the labels are derived, and nothing fails.
      - **`sectionLabel()` now forces the company name back to capitals**, because title-casing a key is right for every word in a section name except one: `networking_deco_referral_identification` rendered as **"Deco Referral Identification"** on the card, in the Compare table, in the corrections list and in the Follow-Ups reasons. DECO is capitalised everywhere else in this app, and this was the one place generating it from a key rather than typing it.
    - **The under-23 trigger is context, NOT a criterion, and is deliberately absent from the prompt.** The caller is never graded on the candidate's age, and naming it would invite the model to weigh it. It lives in the Code.gs comment, in `docs/`, and in the on-screen hint under the picker. There is a test asserting it stays out.
    - **The refactor that made room for these rewrote constants three LIVE prompts are built from**, one of which (Conversion Call) has four measured AI runs against a human grade. So the tests pin the exact **length and checksum** of every pre-existing prompt rather than checking for phrases — a stray character there invalidates a calibration baseline and presence tests would sail past it. All five came out byte-identical.
    - **What IS shared across the three recruiting-call prompts, and what deliberately isn't.** Shared: rule 1's phone-call carve-out, the front of rule 2, and `CALL_SCORING_CALIBRATION_BODY` — the calibration lever, so tuning it moves every recruiting rubric at once. Not shared: the **measured** upward-bias sentence, which is a real finding about the Conversion Call rubric and nothing else. The reference prompts say the bias was found on the other rubric, that it has not been measured here, and to correct for it anyway. **Don't "unify" that into one claim.**
    - **The anchor SHAPE differs between the rubric families** — Conversion Call writes discrete 10/8/6/4/2/0 standards, both reference rubrics write 9-10/7-8/6/3-5/0-2 bands — so the headline of rule 2 is per-family. Pointing the model at a scale its own rubric doesn't use costs accuracy on every category.
    - **Verified in a real browser against a stubbed Sheets backend, both schemes (no screenshots — the Browser pane wasn't compositing again).** 592/592 in the Apps Script harness. Checked: both variants' max/threshold/labels/subject label; the picker's options, hint, and survival across a tab round trip; the form clearing on a variant switch; Training Day and Conversion Call unchanged; the pass rule at the 42/41 boundary and unticked; all six cert-preview branches including the missing-column path naming the right column and the right tab, and an unread Roster **not** reported as missing; Recent Submissions scoping with no cross-variant bleed and a blank Session Type still reading as Training Day; the Passed column's Yes / No / Bar met; the catch-up bar listing both variants independently and dropping one when certified; both results cards with no stray "Parent"; contrast **4.62–18.82 light / 6.12–17.91 dark, zero failures**; no page overflow at 1280 or 375.
    - **Two harness mistakes worth recording, because both produced convincing false results.** Assigning `window.rosterHeaders` does **not** rebind a module-scope `let`, so `certColumnMissing` was reading an empty array and the "missing column" case looked broken when it wasn't. And a `fetchSubmissions` stub left bound from an earlier probe kept returning simplified rows into a later test, which failed inside the renderer and looked like a product bug. **Stub at the lowest layer (`sheetsGet`) and let the real parser run.**
  - **Certifying from grades that were already on file — the catch-up bar (added 2026-08-15, `certifiableFromGrades` / `renderCertCatchupNotice`).** Jordan, on the Recruiting Dashboard with every Conversion Call badge reading Not yet: *"Have this turn to certify if there is a manual entry for conversion calls that it is certified."* The wiring for that existed already (`SESSION_CERT_COLUMN`, 2026-08-14) — but it only ever fires **at the moment a grade is submitted**, so it did nothing for grades submitted before the session type had a certification path, before the Roster had the column to write into, or on a run where the Roster write didn't land. **Nothing ever went back and looked.** This looks: a bar above either roster table listing everyone whose passing manual grades say they should be certified when the Roster doesn't, one Certify button each. Decisions worth not re-litigating:
    - **It OFFERS; it never writes on its own initiative** — the same rule as the un-certify bar after a delete, and for a stronger reason here. The permission that makes certifying legal at all is *a human decided this*; a page load that quietly signed several people off would be the app deciding, which is the one thing every note in this file forbids. Auto-writing on load was considered and rejected.
    - **Manual grades only, through the same `submissionPassedManually` / `passesRequired` path the submit form uses.** An AI grade still never moves the Roster; counting AI verdicts here would hand the certification decision to the AI by the back door. Verified: a session with an AI grade and no manual one is not listed.
    - **A row whose sections tick was never recorded is listed WITH THAT SAID ON IT**, not dropped and not silently counted as a Yes. `submissionPassedManually` treats a blank column W as *unknown, could have* — the right answer for raising a question and the wrong one for signing somebody off without the person seeing it. So the row reads `graded 55/80 · by Jordan Evans · the "every section reached 6" tick wasn't recorded on that row`.
    - **It reads the WHOLE board, never the filtered table.** A region filter or a name typed in the search box must not be able to hide a sign-off that is waiting to happen. Verified: filtering the Recruiting board to BC1 leaves one person in the table and still lists both waiting certifications.
    - **`writeRosterCell`, not `certifyOnRoster`.** The former reads the cell back before anything on screen moves, which is what stops the badge claiming a certification the Sheet hasn't got; `certifyOnRoster` (the submit path) doesn't read back. Verified against a stub that accepts the write and discards it: the badge stays Not yet, the offer stays on screen, and the message names the cell.
    - **The button arms and takes a second click**, like every certification control in this app, and arming a second offer disarms the first. Its armed label is **"Confirm?"**, deliberately short: `.alert-bar .btn` is `white-space: nowrap`, and a long label in this bar has already pushed the page into horizontal scroll at 375px once. The full sentence is on the tooltip and the `aria-label`.
    - **`certCatchupBusy` and `armedCertCatchup` are in `rosterEditInProgress()`**, so a background `refreshRosterRecordingCounts()` can't repaint a half-confirmed offer away.
    - **A success repaints through `renderDashboardKeepingOpenRow()`**, not `renderDashboardTable` — the row has to leave the list, the badge has to turn green and both stat strips count the column, without shutting a row somebody has open.
    - **It is general, not scoped to Conversion Call.** It runs off `SESSION_CERT_COLUMN` + `passesRequired`, so a Team On-Site Day with three passing recordings on file appears on the Training/TOSD board the same way. On the live data as of 2026-08-15 that board's bar is **empty** (Mathew Marshall's three passing Training Days are already ticked) and the Recruiting one shows exactly one row: **Dante Furlan — Conversion Calls, 64/80**.
    - Front-end only. **No `Code.gs` change, no redeploy.**
    - **Verified in a real browser against a stubbed Sheets backend, both schemes** (no screenshots — the Browser pane wasn't compositing again). Checked: all eight classification cases (three passing conversion calls listed; under-the-bar, tick recorded No, already certified, and AI-grade-only all correctly absent; three-of-three Team On-Site Days listed and two-of-three absent; a name with a **trailing space** on the Roster matching the log, and an apostrophe name); the filter not hiding a row; the first click issuing **zero** writes; the write landing on **K13/K15** (right column, right row); the discarded-write and hard-failure paths both leaving the badge, the memory copy and the offer untouched with the reason underneath, and the button pressable; recovery afterwards; the row leaving the list, the badge turning green, the status line and the stat strip going 2→3 of 6; contrast **5.62–15.65 light / 5.43–12.36 dark**, armed button 5.03 / 6.58; no page overflow at 1280 or 375, where the button drops onto its own line; no console errors from the app.
  - **The sections-tick header was labelling the WRONG COLUMN, and two of them are wrong in the live sheet (found and fixed 2026-08-15).** Row 1 of the Grading Log reads `All Sections 6+` in **W, X and Y**. The values were always right — only the labels moved. Cause: the header write used `` `${GRADING_LOG_LAST_COLUMN}1` ``, which was the same cell as W when W *was* the last column, and then silently followed that constant out to X (V16, the transcript link) and to Y (the Compare human grades). Both header writes now derive from their own index (`columnLetter(MANUAL_SECTIONS_IDX)` / `columnLetter(HUMAN_SCORES_IDX)`), as does the Compare value write, which had the same latent coupling. **Two cells in Jordan's sheet need fixing by hand and the app will not do it: `X1` should read `Transcript Link`** (that column is Code.gs's, so the front end deliberately doesn't label it) — `W1` and `Y1` correct themselves on the next manual submission and the next Compare save.
  - **Visual rebuild #2 — the design-token layer (2026-08-14).** Jordan asked for the site to "look 10 times better and like a 1 billion dollar company app". The brand palette, logo and fonts were already right (see Phase 2); what was missing was everything *underneath* them. Almost all of it is CSS, and the one structural addition is the Dashboard stat strip. Decisions worth not re-litigating:
    - **The surface ladder is the whole fix.** `--page` and `--surface-1` used to differ by 5% of cream, so a card was an invisible region of the page and no shadow could rescue it. Now: `--page` is a warm off-white, `--surface-1` is pure white, and a new `--sunken` is for wells *inside* a card — inputs, the filter bar, table headers, the results card. Dark mode mirrors it. **Three levels, and every new surface has to pick one of them** rather than inventing a fourth. (The exact hex values here — `#f7f3ec`/`#f4efe6` and `#0b0a08`/`#16140f`/`#100e0b` — were **superseded by rebuild #3 below**; the ladder is what survives, not the numbers.)
    - **Shadows are warm-tinted and two-layered** (`--shadow-sm/md/lg/pop`): a tight contact shadow plus a wide soft one. A neutral grey shadow over a cream page reads as dirt.
    - **In dark mode depth comes from a top-edge highlight, not shadow.** A shadow on near-black does nothing, so `.panel` and `.stat-card` get a `linear-gradient` highlight along their top ~100px instead. Don't "fix" dark mode by scaling the shadows up — that just makes muddy halos.
    - **Header and tab nav are sticky** (`top: 0` and `top: 66px`), the nav with a backdrop blur. **If the header's height changes, the nav's `top` has to change with it** or the two overlap.
    - **The active tab's underline is a `::after` that scales from the centre**, not a `border-bottom` that snaps on. Same reason buttons got a press state: the app never acknowledged input, and that is most of what "feels cheap" is.
    - **Table headers are sticky, uppercase micro-type on `--sunken`, and every `td` is `tabular-nums`.** Numbers that don't line up column-wise make a data table read as sloppy no matter what else is done to it.
    - **Badges gained a 1px ring in their own hue.** A tint alone goes soft on a white card; the ring is what keeps Done / Not yet legible at the 11px the Dashboard squeezes them to.
    - **The Dashboard stat strip (`renderDashboardStats`) is the one new thing on screen, and it adds no Sheet read.** Four cards — roster size, certified on Training Days, certified on Team On-Site Days, sessions awaiting follow-up — all counted from `rosterRows` and `followUpItems`, which are already in memory. Two rules it must keep: **the search box never changes a figure** (only the region and role dropdowns do — superseded 2026-08-14, see the next bullet; it used to be drawn before the filter check entirely); and it **re-states what the table below already says** rather than deriving anything new — the two-part certification rule lives in the Smartsheet and in the note above the strip, and giving it a third home is exactly how those drift apart. `updateFollowUpCount()` calls the render again, which is what fills the follow-up card in once that list loads a moment after the roster. **Centred, not left-aligned (Jordan, 2026-08-14)** — four cards of very different text lengths ragged down the left read as four unrelated boxes. Centring means the card is a flex column, which is also what lets `.stat-card-sub { margin-bottom: auto }` push the progress bars onto a shared floor line; without that the bars float mid-card on the shorter ones and the alignment centring bought is lost again. The bar track is `--gridline`, not `--sunken` — in dark mode `--sunken` is near enough the card's own colour that the empty part of the bar vanishes.
    - **Verified programmatically, not by screenshot** — the Browser pane wasn't compositing frames that session, so the check was computed styles, token resolution, stylesheet rule count, no horizontal overflow, and a full re-run of the functional paths (roster expand, Follow-Ups, results card, and the whole delete → un-certify offer → Roster write flow) in both colour schemes, with a clean console. If a later session can screenshot, it's worth an actual look.
    - **The strip now answers for a slice of the roster, not the whole thing (2026-08-14).** Jordan: *"Break this down even more, Total Managers, Total Field Managers, Total District Managers and other manager roles if they come up, Then have it adjust when you select the region same with the other like certified training days."* Two changes, and the first **deliberately reverses the "drawn before the filter check" decision written directly above** — so don't restore that as a cleanup:
      - **The region and role dropdowns narrow all four figures; the name search still doesn't.** A region or a role is a slice of the company somebody is asking about, so "1 of 2 certified in BC1" is the number they want. Typing a name is a lookup, and watching the roster total tick down per keystroke tells nobody anything — that half of the original reasoning was right and is kept. Because the two behave differently, a **scope line above the strip names what's being counted** ("Counting BC1 + Field Manager — 2 people. Searching a name doesn't change these figures."), since a number whose scope isn't stated is a number waiting to be misread. When a filter combination matches nobody the strip and the scope line both clear themselves — the table below already says "No matching people", and four cards reading "0 of 0" would only say it worse.
      - **`renderDashboardStats` reads the two dropdowns itself rather than being passed them**, because `updateFollowUpCount` calls it too and two readings of the same filters could drift apart.
      - **The roles are counted off the Roster's own Role column, never a hardcoded list** — that's what "other manager roles if they come up" needs, and a role appears here the day somebody is given it. Sorted biggest group first; someone with no role lands under **"Role not set"**, which is pinned last wherever its count would have put it (a gap in the sheet isn't a role). **Deliberately not pluralised:** the count sits in its own right-aligned column beside the role, so each row reads as a category rather than a sentence, and the page never has to guess the plural of a job title typed into a Sheet.
      - **A graded session inherits its region and role from the person it's about**, so the follow-up card scopes on `item.region` (already looked up that way) and on a Roster lookup for the role. A name that isn't on the Roster has neither, so it drops out of a narrowed count rather than being counted against a region it isn't in. **The Follow-Ups tab badge stays roster-wide** — it's a global nav count, not a Dashboard figure.
      - **Measured, both schemes, with screenshots** (the pane was compositing). One real defect found and fixed before shipping: the scope chip at `var(--accent)` on its own 13% tint measured **3.85:1** in light mode, and bold 11.5px is nowhere near WCAG large text — it now uses the same `color-mix(… 78%, var(--text-primary))` treatment as the alert-bar titles (5.33 light / 7.77 dark). The search caveat at the end of the scope line was also going to be `--text-muted` at **3.25:1**; the whole line is one colour instead. Also checked: the seven filter combinations (none / region / role / both / search-only / empty-combination / blank-role people) against hand-counted expectations, no page overflow at 1280 or 375, and a clean console.
    - **Two wording/figure changes on Jordan's ask, 2026-08-14.** The roster card's title is **"Managers"**, not "People on the roster" (his words: *"change this to Managers in the title"*) — the scope line below still says "N people", which he was shown and hasn't asked to change. And each certification card now carries **"N% certified"** (`.stat-card-pct`) directly under its "N still to certify" line, because the bar alone can't be read off to a precise figure. Two things worth keeping:
      - **It has its own rounding (`pctLabel`), not `pct()`'s.** A bar drawn 99.7% full is fine; the words "100% certified" while somebody is still to certify are not. So a flat `0%` / `100%` is reserved for genuinely nobody and genuinely everybody, and the in-between extremes render `<1%` / `>99%`. Only reachable on a roster in the hundreds, but this is the one figure on the card people quote out loud.
      - **The percentage sits inside `.stat-card-sub`**, so `margin-bottom: auto` still puts both bars on the shared floor line (measured: identical at 1280 and 375). It's `--text-secondary`, deliberately not `--text-primary` — the big "2 of 18" above is the card's hero and a second bold figure in the same colour competes with it. Measured 8.42 light / 9.9 dark.
  - **Visual rebuild #3 — brightening the theme and the contrast pass (2026-08-14).** Jordan: *"Can you brighten up the theme a little bit and add more contrast on the things that are important or should stand out."* Entirely CSS — no markup, no JS, no `Code.gs`, no redeploy. Two halves, and the second is where the real work was.
    - **Brightening is the surface tokens, not a wash over everything.** Light: `--page` `#f7f3ec` → `#faf7f1`, `--sunken` → `#f5f1e8`, and the body's orange wash from 7%/6% to 10%/9%. Dark went further because it was the worse of the two — `#0b0a08`/`#16140f`/`#100e0b` → `#131110`/`#201d17`/`#191712`, which sat so near black that the app read as switched off. **The ladder from rebuild #2 is preserved in both, and that constraint is what sets the ceiling:** a brighter page narrows the white-card-to-page step (1.106 → 1.069), so `--border` went 0.09 → 0.12 and `--border-strong` 0.16 → 0.22 in the same edit to keep a card's edge doing the work the lost tone used to. **Don't brighten `--page` further without paying the borders back.**
    - **Every Dashboard status badge failed AA in light mode and now doesn't.** This is the item the 2026-08-14 add-person session found, measured, and deliberately raised with Jordan rather than changing unilaterally — his ask is the answer to that question, so it was fixed. Each badge's text is now its tone pulled 22% toward `--text-primary` (`color-mix(… 78%, var(--text-primary))`, the same treatment as `.alert-bar-title` and `.notice-err`), with the tints and rings a few points stronger: **Done 3.81 → 5.01, Not yet 4.08 → 5.37, Pending 2.42 → 5.01**, and 5.8–6.5 in dark. Checked on a **hovered** row too, which darkens the ground under them — all still ≥ 4.5. One rule serves both schemes because the mix darkens on light and lightens on dark; **don't "clean it up" back to a bare `var(--tone)`.**
    - **`--pending` is no longer `var(--brand-taupe)`.** The brand taupe is a decorative neutral and made the worst-contrast thing in the app (2.42:1) out of the status people most need to read. It's `#7a715f` now — same hue, darkened until it holds up. **The taupe token itself is untouched** and still used where it's genuinely decorative (the preview-tab dot, the signed-out status dot).
    - **`--accent` darkened `#b5590d` → `#a44e0b`.** Not cosmetic: the old value measured 4.77 on a white card but **4.46 on the page itself** — already under AA — and it had no headroom for the active tab's new tinted seat, where it fell to 4.17. Now 5.72 / 5.35 / 5.00 across those three grounds. Dark mode is unaffected (`--accent` is `var(--brand-orange)` there).
    - **`--text-muted` `#8f8676` → `#7d7463`** (3.60 → 4.62 on white). It carries table headers, footnotes and the "of 12" denominators, and was under AA everywhere. **It is still not enough on `--sunken`** (4.09), which is why the add-person session's scoped `.add-person-form .field-optional` override **must stay** — that note says it can go "if the palette pass restyles `.field-optional`"; it didn't, and measurement says it can't be removed. `thead th` and `.stat-card-label` were moved to `--text-secondary` outright rather than relying on muted at 10px uppercase.
    - **Three other pre-existing AA failures fixed in passing, all on lines that matter:** `.cert-preview.will-certify` (3.91 — the line saying a submission is about to certify somebody), `#grade-status.ok` (4.45 — the whole feedback for a three-minute upload; `.err` was 4.86 and got the same treatment for consistency), and the header quote's attribution at `rgba(255,255,255,0.42)` (4.10 on the header's dark end, 3.66 in its orange-washed corner → 0.58).
    - **`.note` needed a dark-mode rule of its own.** Its background is `color-mix(--brand-cream 38%, --surface-1)`, and mixing a *light* neutral into a dark card produces a mid-tone box that neither the dark theme's text nor its background belongs on — 3.19:1, the worst thing in dark mode, on the box carrying most of this app's explanations. Dark mode now tints with the brand **orange** instead (stays dark, same warm "this is a note" cast): **8.38:1**. Don't collapse the two rules back into one.
    - **The active tab got a tinted seat, and deliberately NOT a font-weight change.** The tab row is a horizontal scroller; 600 → 700 re-measures the active tab and shifts every tab beside it a pixel or two on every switch. Colour and a 13% tint cost no width.
    - **Verified programmatically in a real browser, both schemes — no screenshots, the Browser pane still wasn't compositing.** A full-DOM sweep (every element with its own text, against its true composited ground, at its own size/weight threshold) returns **zero real failures in either scheme**. Two classes of false positive worth knowing before someone re-runs this: **gradient-filled elements** (`.pill`, `.btn`, the `header`) report ~1.1:1 because `backgroundColor` is transparent and a walk-up can't see a gradient — measured against their actual fills they are 7.7:1 (black on orange) and 8.8–18.9 (header). And **the scheme emulation leaves descendants' custom properties stale**: the active tab and `.tab-preview` first measured 2.37/2.71 using dark-mode values under a light root. Forcing a recalc (`display:none` + reflow) before measuring is what makes the numbers real — without it this pass would have "fixed" two things that were never broken.
    - **One AA gap left, deliberately:** `.lock-tagline` — "Think Orange. Save Your Windshield.™" on the sign-in screen — is `var(--brand-orange)` at 26px and measures **2.72 against the 3.0** large-text threshold. It's decorative trademarked brand copy, and the codebase explicitly reserves brand orange for exactly this (large decorative headlines). Raised with Jordan rather than changed; the one-line fix is `--accent`, which keeps it orange.
  - **A light/dark toggle in the header, and the theming rewrite it needed (2026-08-14).** Jordan: *"add an option to toggle the page so you can view in two different types."* The ask came out of a real failure — he switched Windows to light mode to look at the day's contrast work and **the app stayed dark**, so the light palette everything had been measured against was unreachable from his machine. Decisions worth not re-litigating:
    - **The tokens had to move off the media query, and that is the actual fix.** Dark values used to live only inside `@media (prefers-color-scheme: dark)`, guarded by `:root:where(:not([data-theme="light"])))`. That shape can force **light** and can never force **dark** — on a light-set machine the block simply doesn't match, so there is nothing to override. All five dark blocks (the token set plus `.panel`, `.note`, `.stat-card`, `.ai-verdict`) are now `:root[data-theme="dark"]`. **Adding a toggle without this would have produced a button that worked one way only.**
    - **There is deliberately NO duplicate media-query copy of the dark tokens.** The usual reason to keep one is a no-JavaScript fallback, and this app has nothing to fall back to — sign-in, the roster, grading and every panel are JS. Two copies of forty values would be forty chances to drift while guarding a page that never renders. (This is a considered exception to the define-it-three-ways convention, not an oversight.)
    - **Theme resolution is an inline script at the top of `<head>`, above the stylesheet.** Run from the main script at the bottom instead, the browser paints the default theme and then repaints — a white flash on every load for anyone who chose dark. It must stay inline and stay first.
    - **`localStorage` is wrapped in `try`/`catch` because it THROWS rather than returning null** in a private window or with site data blocked — the same lesson as `saveToken`. A blocked store costs only the memory of the choice between reloads and must never break the page. Confirmed for real: the test harness runs on a `data:` URL where storage throws, and the toggle still worked.
    - **The app follows the OS only until someone picks a theme, then stops listening.** Otherwise changing Windows to light would silently undo a manager's decision to work in dark — the mirror image of the bug this exists to fix. `DECO_THEME.explicit` is what separates the two, and it's the single source of truth; nothing re-reads storage or re-queries `matchMedia` to find out.
    - **The button names its destination, never its current state.** "☾ Dark" means *press for dark*. A lone sun/moon icon leaves people guessing which of the two it means, and this is used by managers, not designers. Below 700px the word hides and the `aria-label` carries it, because the icon is `aria-hidden`.
    - **`onSchemeChange` reads the query object, not `event.matches`.** Both work in a real browser, but the query is the actual state, so the handler behaves correctly however it's invoked. It's also exposed as `DECO_THEME.__onSchemeChange` **purely so this branch can be tested** — DevTools/CDP colour-scheme emulation changes what `matchMedia` reports *without dispatching a `change` event*, so there is otherwise no way to prove the branch runs at all. Don't delete it as dead code.
    - Front-end only. **No `Code.gs` change, no redeploy.**
    - **Verified in a real browser against the actual edited file, both schemes — no screenshots, the Browser pane wasn't compositing again.** Checked: the head script resolving from the OS in both directions; the toggle forcing light on a dark-set machine **and dark on a light-set machine** (`--page: #131110` with the OS reporting light — the case that was previously impossible); `color-scheme` following so form controls and scrollbars flip too; all four converted component rules firing (`.panel` gains its dark top-edge gradient only in dark, `.note` flips cream → orange-tinted); OS-change followed while `explicit` is null and ignored once set; **the button adds 0px to the header height** (72px with and without — it matters because `nav.tabs` is pinned to the header, and the sign-in button is already the same 39px); no horizontal overflow at 1280 or 375, where the label correctly hides; clean console. `.stat-card` and `.ai-verdict` aren't in the DOM when signed out, so those two conversions are proven by pattern rather than measured — worth a glance on a signed-in session.
    - **Found but NOT fixed, and pre-existing: `header` is 72px tall while `nav.tabs` sticks at `top: 66px`**, so the nav can ride 6px over the header's bottom edge (and its 3px orange rule) when scrolled. Measured with the toggle removed — 72px either way — so this predates the toggle and is not something it introduced. Raised rather than changed, since it's a visual call on work Jordan signed off on; the fix is one number.
  - **The verdict hero — the top of an AI result card (`verdictHero`, 2026-08-14).** Jordan sent a screenshot of the old header ("0 / 110", a small Not-certified badge, left-aligned) and asked for it centred and "more obvious in your face", with a percentage, how much score was missing to pass, and how many sections came in under 6. It replaces the old `.ai-summary-row` / `.ai-stat` strip entirely. Decisions worth not re-litigating:
    - **The badge is still `result.certified`, never re-derived from the total.** Certification is the two-part rule (total at or above the bar **and** every section ≥ 6), so a score can clear the bar and still be a No — and the hero has to be able to say exactly that. Everything else in it is arithmetic on numbers already shown further down the same card, so it can't disagree with them.
    - **Sections under 6 are split, not flattened** — "4 scored low · 3 never came up on the recording (counts as 0)". Same distinction the Follow-Ups tab makes, for the same reason: the two need different actions, and the second isn't a performance problem. `verdictHero`'s classification must stay in step with `followUpReasons()`.
    - **The pass mark is drawn, not just stated.** A meter with the score as fill and the bar as a tick answers "how far off was it" faster than the number does. It's only drawn when the session type has a max to draw against — no invented scale on a preview session type.
    - **The bigger it got, the more the framing line mattered.** A 92px score and a 15px badge push hard toward sounding authoritative, which is exactly what Phase 2 says not to do — so the hero ends with "This is the AI's read of the recording. It's here to inform your grade, not to replace it." Don't drop it to save the two lines.
    - Sizing is `cqi`-based off `container-type: inline-size` on the hero itself, because the same card renders in the narrow expanded rows of Recent Submissions and the Dashboard (measured: 92px score at 1084px wide, 56px at 370px, no overflow either way). The meter track is `--gridline`, not `--sunken` — in dark mode `--sunken` is near enough the card's own colour that the empty half of the bar disappears.
    - **Screenshotted this time**, light and dark, plus the zero / mixed / pass / bar-met-but-not-certified / Team On-Site Day / no-max cases.
  - **Conversion Call went LIVE as a third graded session type (2026-08-14, `Code.gs` V18 — DEPLOYED AND CONFIRMED, both halves).** Jordan produced the rubric (`DECO_Conversion_Call_AI_Grading_Rubric.docx`, v1.0) and asked for it wired up. **This reverses the 2026-08-13 "recruiting side is post-project" note FOR THIS SESSION TYPE ONLY** — Stage 1, Reference Checks and Stage 2 are still preview with no rubric loaded, and that note still governs them. Decisions worth not re-litigating:
    - **It is loaded FLATTENED, and Jordan chose that knowingly.** The source rubric scores 8 categories 0–10 and combines them as a **weighted average out of 10** (pass at 6.0), with **5 of 8 marked critical** and **3 automatic-fail conditions that override the score outright**. He was shown that, and the option he picked was "flatten it to match the other tabs" — a flat sum out of 80, every section ≥ 6, pass at 48. The alternative (build the weighted model) was offered and declined. Three consequences, all recorded in `docs/conversion-call-rubric.md` under "How the structural differences were resolved":
      - **The app's total will not match a hand-computed weighted score on some calls.** Worked example in that file: a 4 in a 15%-weighted category and a 9 in a 10%-weighted one gives 6.55 weighted vs 69% flat. **Don't reconcile them by "fixing" one — they are different formulas by decision.**
      - **The app is STRICTER than the rubric, not looser.** "Every section ≥ 6" applies to all 8 categories here, where the rubric's floor covers only 5. A 4 in Opening, Communication or Tailored Selling now fails a call the rubric would have passed.
      - **48 is not an arbitrary bar.** It is 60% of 80 (matching the rubric's 6.0/10) *and* exactly 8 × the section pass mark, so the threshold can never contradict the every-section rule.
    - **The automatic-fail conditions are REPORTED, not scored, and that is the whole reason they still exist.** Nothing overrides the number. Instead prompt rule 10 makes the model raise any of the three as a `must_fix` correction **and** open `notes_for_human_reviewer` with the literal string `AUTOMATIC FAIL CONDITION:`. It also forbids zeroing unrelated categories to force a failing total, which would corrupt the section scores calibration depends on. **A flattened score with the conditions silently dropped was the one outcome worth avoiding**, and it cost no scoring logic to avoid it.
    - **It has its OWN rule set, and the separation is the point (`conversionRules`, `promptRules`).** Reusing the field-training rules would have been wrong three ways, and the worst is subtle: shared rule 1's visual-criteria carve-out ("many criteria are inherently visual") is **false on a phone call**, and would have handed the model a standing excuse to mark a category `insufficient_evidence` — which scores 0 — when the truth is the caller simply didn't do it, which is a low score with a reason attached. Same number, completely different meaning on the card. The conversion rule 1 says outright that everything on the call is verbal and redefines `insufficient_evidence` as *the recording failed you, not the caller*. The prompt also carries **none** of the pricing ladder, ICBC rebuttals, glass-shop data or Replacements 101 — a recruiting call has no use for them and feeding them in invites grading against them.
    - **`SHARED_RULES_MIDDLE` and `SHARED_RULES_TAIL` were SPLIT so rules 5, 6 and 8 keep one copy**, not two. That split rewrote constants the two live prompts are built from, so it is the riskiest part of V18 — guarded by the pre-existing `sharedRules("audio") === SHARED_RULES` assertion plus new ones pinning the transcript medium and both re-joins. **Training Day and Team On-Site Day prompts are byte-identical to V17.2.**
    - **No certification column and no prerequisites, both by construction rather than special case.** `SESSION_CERT_COLUMN` and `SESSION_PREREQUISITES` have no Conversion Call entry, so the auto-certify path and the training-not-complete gate are inert — a conversion-call pass signs nobody off, and the Day 1/Day 2 training steps have no bearing on making a recruiting call. The results card says **Pass / Fail**, not Certified / Not certified (`VERDICT_WORDS`), for the same reason.
    - **The corrections list means something different here and says so.** A phone call is over before it's uploaded, so there is no "fix it before the session ends". The heading is "To follow up on with this candidate", the intro copy is session-type-specific, and rule 9 asks for what has to be put right with *this candidate* before they go further through the funnel.
    - **The person graded is the CALLER, never the candidate** — the form label reads "Caller being graded" and the prompt says the other voice is not being graded. A conversion call is made by a district or operations manager, so "Trainee" would have read as the wrong person to pick.
    - **Verified: 252/252 in the Apps Script harness** (191 pre-existing all still passing, 61 new, 3 stale ones updated — one had used `'Conversion Call'` as its example of an *unsupported* type). Front end driven in a real browser against the edited file: the tab live, max 80 / bar 48, all four session types' verdict cards, Pass/Fail wording with Training Day and Team On-Site Day unchanged, the automatic-fail note rendering, no page overflow at 1280 or 375, clean console. **No screenshots — the Browser pane still wasn't compositing.**
    - **FIRST REAL CALL GRADED, 2026-08-14, AND IT LINED UP WITH THE HUMAN GRADE — the first calibration signal this project has ever had.** Result: **68/80 (85%), Pass**, zero categories under 6. Jordan: *"This listened to it and it was pretty close to what i graded myself!"* Category scores were 8 / 6 / 8 / 8 / 10 / 10 / 8 / 10. Caveats on how much weight that carries:
      - **It is ONE call, and a strong one.** A call scoring 68/80 exercises the top of the scale and says nothing about whether the AI is calibrated near the pass mark, which is where a disagreement would actually change a decision. The rubric's own tricky cases — an automatic-fail condition, and a call correctly ended early — were both absent, so both remain untested.
      - **"Pretty close" was not recorded as numbers.** Jordan's own grade for this call wasn't captured alongside the AI's, so there is no per-category delta to look at. **Next real call: type the human grade into the Compare tab** — that is exactly what it was built for, and it turns "pretty close" into a figure.
      - The evidence timestamps looked plausible throughout (0:22 → 5:12 across a ~6 minute call) and several sections had all 3 passes landing on one moment, but **no citation was hand-checked against the audio**, so the accuracy question is still open for this session type.
    - **Pass count stays at 3 — Jordan's call, 2026-08-14, asked and answered.** He asked for "the three times run through" not realising consensus grading was already on for every session type; the "3 passes point here" markers on his own card were it running. Raising it to 5 *for Conversion Call only* was offered on the reasoning that a ~6 minute call costs ~$0.05 a pass against ~$1.42 for a 4h33m Training Day, and that passes run in parallel so it would add no waiting. He declined, and three passes is what produced the grade that matched him. **Worth not re-offering unless something changes.** The point made at the time still holds and is the thing to remember: **more passes buy CONSISTENCY, not CORRECTNESS** — the median of 5 wobbles less than the median of 3, but a systematic misreading of the rubric will simply be agreed on by more passes. `GRADING_RUNS_PER_SESSION` is global; making it per-session-type is the small change if this ever comes back, and it **must** be per-type, since 5 passes on a 4.5-hour recording is ~$7.12 against ~$4.27.
    - **THE SAME CALL GRADED THREE TIMES ON 2026-08-14 — twice from audio, once from transcript, against a human grade of 64/80. This is the best calibration data the project has, and it says the TRANSCRIPT route was the accurate one.**

      | | Open | Avail | Goals | Probe | Comms | Job | Sell | Next | **Total** |
      |---|---|---|---|---|---|---|---|---|---|
      | Human (Jordan) | — | — | — | — | — | — | — | — | **64** |
      | Audio run 1 | 8 | 6 | 8 | 8 | 10 | 10 | 8 | 10 | **68** |
      | Audio run 2 | 8 | 6 | 8 | 8 | 10 | 10 | 8 | 10 | **68** |
      | Transcript | 8 | 6 | 8 | **6** | 10 | **8** | 8 | 10 | **64** |

      - **The audio route is REPRODUCIBLE, and that is a real result.** Two independent 3-pass runs on the same recording returned **identical totals and identical per-category scores** — not close, identical. The standing "AI grading consistency is still not fully solved" caveat does not appear to apply to a ~6 minute call graded from audio. Do not generalise it to a 4-hour session, which has never been run twice.
      - **The audio route came in +4 over the human BOTH times.** Twice the same figure is a consistent lean, not noise — which is exactly the kind of thing Phase 2 exists to find, and exactly what a single run could never have shown.
      - **The transcript run hit the human total exactly.** The entire 4-point gap is two categories: **Probing (audio 8, transcript 6)** and **Job Overview (audio 10, transcript 8)** — both cases of the audio route being more generous. Plausible reading: hearing a warm, fluent call flatters it, and reading the words back shows what was actually asked and actually stated. **That directly contradicts this file's own assumption that audio is the better evidence**, and it is the single most interesting finding so far.
      - **DO NOT OVERSTATE IT — the human grade is a TOTAL, not per-category.** The manual grade box takes one number, so "the transcript matched the human" is true at the total only. Two different category splits both sum to 64, and there is no evidence Jordan would have scored Probing and Job Overview where the transcript did. **The next call must be graded per-category on the Compare tab** or this stays a coincidence that looks like a finding.
        - **THAT CAUTION WAS RIGHT, AND THE FINDING ABOVE IS NOW OVERTURNED — Jordan supplied his per-category grade later the same day, and a SECOND transcript run came back at 54.** Read the header of this item as superseded: **per category the AUDIO route is the closest to the human, and the transcript route is not reproducible.** See "PER-CATEGORY HUMAN GRADE" directly below.
      - **One call, and a strong one.** Nothing here says anything about behaviour near the 48 pass mark.
      - **Timestamps wobbled while scores did not.** The two audio runs cite slightly different moments (1:24 vs 1:26, 2:55 vs 3:07, 3:35 vs 3:49) and run 2 returned 2 clips where run 1 returned 3, yet every score matched. So citation moments are the less stable output, and a few seconds of drift between runs is normal — worth knowing before anyone reads a shifted timestamp as a changed judgement.
    - **PER-CATEGORY HUMAN GRADE, AND A FOURTH RUN — 2026-08-14, and it OVERTURNS the "transcript is the accurate route" finding above.** Jordan gave his own eight category scores for that same call — **9, 7, 8, 9, 9, 9, 7, 7 = 65/80** in rubric order — and a second transcript run of the same recording came back at **54/80, a FAIL**.

      | | Open | Avail | Goals | Probe | Comms | Job | Sell | Next | **Total** | Verdict |
      |---|---|---|---|---|---|---|---|---|---|---|
      | **Human (Jordan)** | 9 | 7 | 8 | 9 | 9 | 9 | 7 | 7 | **65** | Pass |
      | Audio ×2 | 8 | 6 | 8 | 8 | 10 | 10 | 8 | 10 | **68** | Pass |
      | Transcript #1 | 8 | 6 | 8 | 6 | 10 | 8 | 8 | 10 | **64** | Pass |
      | Transcript #2 | 8 | **4** | 8 | **4** | **6** | 8 | 8 | 8 | **54** | **FAIL** |

      Mean absolute delta per category: **audio 1.1, transcript #1 1.4, transcript #2 1.9.**

      - **THE TRANSCRIPT ROUTE IS NOT REPRODUCIBLE on this call — 64 then 54** — while the two audio runs were identical category for category. The two categories that swung (**Probing** and **Communication**) are exactly the two this run flagged **Double-check**, with pass spreads of 4·4·8 and 6·6·10; transcript #1's values for those (6 and 10) sit *inside* those spreads. That is a coin toss, not drift. **Don't quote "audio grading is reproducible" as if it covered both routes.**
      - **"The transcript matched the human exactly" was a COINCIDENCE, and it is now provable.** Transcript #1 hit 64 against 65 by scoring Hiring Process **+3** above the human and Probing **−3** below — the total matched while the categories didn't. This is the exact trap the caution above named, caught by the exact method it prescribed.
      - **Per category, AUDIO is the closest of the three to the human.** That reverses the direction of the earlier headline. **The first real calibration decision this project has to make is which route to trust, and the evidence now points at audio for a short call.**
      - **The verdict flipped for the first time.** 54 still clears the 48 bar, but two categories under 6 make it a Fail under the every-section rule, where the human grade passes with nothing below 7. **This is the first AI/human disagreement that would actually change a decision about a candidate.**
      - **The disagreements have ONE SHAPE and it is the actionable finding:** every large gap is the AI marking harder, all on "did they explicitly confirm X" categories — Availability −3, Probing −5, Communication −3, with the model's stated reasons being omissions (didn't confirm specific days, didn't probe sales comfort, asked a repetitive question). **The AI weights the checklist; the human weights the outcome.** That is a single prompt lever, and it is the thing to change first.
      - **CONFOUND, and it is not small: Jordan graded HIS OWN call.** The Smartsheet grades Phase 2 is trying to match are managers grading someone else, so "the AI is harsh on Probing" and "the grader is generous to himself on Probing" are both live and this run cannot separate them. **The next calibration call must be one the grader ran as the manager, not as the caller.**
      - Two loose ends: Jordan's eight numbers total **65** while the Grading Log's manual column reads **64**, and the same call was submitted **three times**, so Compare draws duplicate columns until they're deleted.
      - Confirmed in passing by the same screenshots: **evidence timestamps render on a transcript-graded session**, i.e. the V18.1 `parseClipSeconds` bracket fix is working in production.
    - **A REAL DATA-QUALITY CATCH, and it came from the reviewer notes:** *"The caller identified himself as Jordan, the BC operations manager, while the session context lists Dante Furlan."* The form had the wrong person selected, so the grade was filed against Dante Furlan for a call Jordan made. **Nothing in the app cross-checks the name on the form against the voice on the recording** — this only surfaced because the prompt asks the model to report anything ambiguous, and it happened to notice. Two things follow: that Grading Log row needs correcting before it is used for calibration, and **a mis-filed grade is a failure mode with no guard at all** on any session type. Worth considering a check if it recurs.
  - **The AI result card fails AA on ten `--text-muted` rules in LIGHT mode — pre-existing, found 2026-08-14, RAISED NOT FIXED.** Measured 4.09 on `.ai-verdict-eyebrow`, `.ai-verdict-max`, `.ai-verdict-rule`, `.ai-detail-label` (the Score / Listen at / Talked about / Missed / Why labels, 40 instances on one card), `.score-note-mark`, `.ai-section-footnote`, `.ai-corrections-intro`, `.ai-correction-tag`, `.ai-note-label`, `.ai-verdict-fact-note`. It is the known "`--text-muted` on `--sunken`" value this file already records. **Identical on all three session types and fine in dark mode (6.5+)**, so it is a light-mode palette decision across the whole card rather than a defect in any one rule — raised with Jordan rather than restyled unilaterally, the same call the Dashboard badge palette got. The established fix is `--text-secondary` (measured 7.47 light / 10.55 dark). It was missed by the 2026-08-14 contrast sweep because **an AI result card isn't in the DOM when signed out**, which that entry already flags as the limit of its own coverage.
    - **Fixed in passing, because it was a different and much worse root cause:** the four `.ai-callout-label` tones were bare `var(--tone)` and measured **Strong 4.45 / Weak 4.86 / Focus 4.92 / Corrections 2.72** in light. Now the standard `color-mix(… %, var(--text-primary))` treatment → **6.25 / 6.77 / 6.76 / 5.87** light, 7.39–10.99 dark. **The corrections label is mixed to 60%, not 78%, and that is measured rather than chosen** — `--brand-orange` is light enough that 78% still came out at 4.07, i.e. still failing. It was tried first and rejected.
  - **Tab order is the real process order, not alphabetical or convenient.** Reference Checks sits between Stage 1 and Stage 2 because that's when references actually get called (Jordan, 2026-08-13). If a new session type is added, put it where it falls in the process.
  - The tab row is generated from a single `SESSION_TABS` array. Each entry is either `status: 'live'` (Code.gs has a rubric for that exact `sessionType` string) or `status: 'preview'` (the tab exists and explains itself but does nothing).
  - **The two live tabs share one grading panel.** `showTab()` re-points `currentSessionType` at the panel rather than there being two copies of the upload form — so the manual-grade cap, the labels, the pill, and which Recent Submissions rows are shown all follow the active tab. Adding a live session type means adding an entry to `SESSION_TABS` and an entry to `SESSION_MAX_POSSIBLE`, nothing more on this side.
  - Because one panel is shared and an AI grade takes ~3 minutes, `gradeSessionWithAI` captures its session type at submit time and refuses to paint progress or results into a panel that has since switched tabs — it shows a "still grading in the background" note on the other tab instead and drops the finished grade into Recent Submissions.
  - **Don't flip a `'preview'` entry to `'live'` just by editing the string.** Code.gs rejects any `sessionType` it has no rubric for; it needs the rubric text, the section keys, the max score, and a redeploy first.
- `apps-script/Code.gs` — the backend. **This is a copy.** The version that actually runs lives inside the Apps Script editor bound to the Grading Log sheet. Editing it here does nothing until it's pasted back in and redeployed (see below).
- `docs/` — the rubrics and reference data the Gemini prompt is built from. **If a rubric or a policy number changes, `docs/` and the prompt text inside `Code.gs` both have to be updated to match.**

## Deploying changes

**Front end (`index.html`):** commit and push to `main`. GitHub Pages redeploys automatically in 1–2 minutes. That's the whole process.

**Backend (`Code.gs`):** editing the file in this repo is not enough. It must be pasted into the Apps Script editor (Grading Log sheet → Extensions → Apps Script), saved, and then **Deploy → Manage deployments → pencil icon → New version → Deploy**. Skipping the New Version step leaves the old code running at the same URL. Label the version to match the file's header (currently V15).

**When a change spans both, deploy the BACKEND FIRST.** The two halves ship by unrelated routes and there is always a window where they disagree. As of V15 the front end degrades gracefully in that window rather than breaking (see the pre-V15 fallback in the LONG RECORDINGS note), but that's a safety net, not the intended order.

> **V14 is deployed — Jordan pasted and cut a new version on 2026-08-14.** The live Web App runs V14, so the raw-JSON leak, the thrown-away pass and the stale model default are all fixed in production.
>
> **But `Code.gs` in this repo is once again AHEAD of what's live.** After that deploy, `CODE_GS_VERSION` and the richer `doGet()` were added (see below) and have **not** been pasted or redeployed. Nothing depends on them — they're a diagnostic — so this is not urgent, but the next person to touch `Code.gs` should fold them into that deploy rather than assume the file matches production.
>
> **V15 IS DEPLOYED — Jordan pasted and cut the new version on 2026-08-14, and BOTH HALVES ARE NOW LIVE.** Verified rather than assumed, which is the point of the `doGet` diagnostic: the endpoint returns `{"version":"V15","longAudio":true,"apiKeySet":true,"actions":["prepareAudio","checkAudio","grade"],"parallelPasses":3,"inlineAudioLimitMb":14}`. The front-end half was pushed in the same session (commit `a8a2b16`) and the live site was confirmed to be serving it — `prepareAudio`, `checkAudio`, the phase bar and the pre-V15 fallback are all present in the served file, and the old flat `ESTIMATED_GRADING_SECONDS` is gone.
>
> **This also cleared the two stale notes above:** `CODE_GS_VERSION` and the richer `doGet()` were folded into this deploy, so the repo copy and production now match. Re-authorisation was prompted on the first run, as expected — V15 calls the Drive REST API with `ScriptApp.getOAuthToken()`.
>
> **What is still NOT known: whether a full-length recording actually grades.** Deployed is not the same as proven. See the LONG RECORDINGS item under "Known quirks" for the one open question (the 6-minute wall on a single grading pass).
>
> **V16 IS DEPLOYED — BOTH HALVES ARE LIVE (confirmed 2026-08-14).** Verified rather than assumed: the Web App URL answers `{"version":"V16","transcription":true,"longAudio":true,"apiKeySet":true,"model":"gemini-3.5-flash","actions":["prepareAudio","checkAudio","transcribe","grade"]}`, and the front-end half is pushed (`main` matches `origin/main`, commit `662d95b`). The pre-V16 fallback in `index.html` — which grades the audio directly against an older backend — is now dormant safety netting, not a path in use.
>
> V16 is **transcribe-first grading** — see the dedicated item under "Known quirks". It does not change how a short clip is graded; the audio prompt is byte-identical to V15's and there's a test asserting exactly that.
>
> **V20 IS WRITTEN AND IS NOT DEPLOYED (2026-08-15).** It adds the **Stage 2 Interview** rubric as the sixth graded session type — 9 categories out of 90, pass at 54, flattened exactly like the Conversion Call and the two reference checks. **Deploy the backend FIRST** (paste `Code.gs`, New Version labelled V20), confirm the Web App URL answers `"version":"V20"` **with a cache-busting `?cb=` parameter** (see the note below — a clean GET can serve a stale version and has), then push `index.html`. The order matters for the usual reason: the front end flips the Stage 2 tab from preview to live, and a V19 backend would refuse `sessionType` after the audio is already in Drive.
>
> Things worth knowing before touching it:
>   - **The flattening bites harder on this rubric than on any other.** The source document leaves three of the nine categories non-critical — one weighted at just **5%** — and "every section ≥ 6" makes all nine a hard gate. An otherwise strong interview that is vague about how advancement is earned now fails on that alone. **This is the first thing to check on a real interview**, and the easiest thing to revisit if it produces a false Fail.
>   - **It grades the INTERVIEWER, never the candidate**, and the rubric is emphatic: a weak candidate who was properly probed is a HIGH score; a polished candidate accepted at face value is a LOW one. Getting that backwards would grade DECO's managers on the luck of who they interviewed.
>   - **It is an interview, not a phone call, and it does NOT reuse `callMediumIntro()`.** That function says PHONE CALL / CALLER in seven places and three live prompts are built from it. `interviewMediumIntro()` is separate, and states the audio carve-out about the **rubric** ("every criterion here is verbal") rather than about the medium — a Stage 2 interview may well have been in person.
>   - **`CALL_SCORING_CALIBRATION_BODY` is now a function of its two nouns** (`callScoringCalibrationBody(eventNoun, personNoun)`) so Stage 2 can say "interview"/"interviewer" without a second copy of the calibration lever existing. **The constant must still come out byte-identical** — V19's fingerprint tests cover it and all three pre-existing prompts, and they pass.
>   - **The candidate competency scales (Leadership / Precision / Introspection) are context and an advisory reviewer note only.** They describe the *candidate*, which is a later phase — recommendation only. Not a section, not in `total_score`, no column, and they can never move the Roster.
>   - **`docs/` has no growth figures, pay rates, bonus percentages or contract dates**, and the source rubric grades several of those against "current approved materials". The prompt says outright that they are not supplied and routes any specific figure to the reviewer notes rather than guessing. **This is the one genuine content gap** — see the open item at the bottom.
>   - **339 → 592/592 in the Apps Script harness** (the count includes V19's reference-check tests; 27 are Stage 2's). Front end driven in a real browser: the tab live, max 90 / bar 54, Pass–Fail wording, "Interviewer being graded", the cert preview correctly promising *nothing*, all four older live types unchanged, no page overflow at 1280 or 375, clean console. **No screenshots — the Browser pane wasn't compositing again.**
>
> **V23 IS WRITTEN AND IS NOT DEPLOYED (2026-09-08). IT ADDS A TONE & ENERGY READ TO ALL EIGHT RUBRICS AT ONCE, AND IT IS ADVISORY — IT CANNOT MOVE A SCORE.** Jordan: *"Could you add to the rubrics as well, all of them ... kind of a bonus section? Just because I don't wanna affect the brain, but just a section that is based on tone, energy, passion, enthusiasm. I think that would be important to have some data on."* Four dimensions, 0–5 each, returned as a **top-level `tone_and_energy` field**. Full write-up and every decision behind it: `docs/tone-and-energy-read.md`. Decisions worth not re-litigating:
>   - **"Bonus" is read as extra INFORMATION, never bonus points.** Nothing here is added to any total. Bonus points would have changed every certification formula in the app and broken Smartsheet parity — so the alternative was considered and rejected, not overlooked. **If Jordan actually meant points that adds up, that is a different build and this is the note to come back to.**
>   - **"Don't affect the brain" is honoured MECHANICALLY, not by asking the model nicely.** `gradeWithConsensus` computes `total_score`, the minimum-section test and `certified` by walking the **section keys only**, so a field outside `sections` has no route to any of them — nor to `followUpReasons()`, nor to a Compare column, nor to the Roster. **Proven rather than asserted:** all eight types graded twice, once at 5/5/5/5 and once at 0/0/0/0, identical totals and identical verdicts; a 40/80 Conversion Call still Fails with a perfect tone read; a 64/80 still passes with a flat one.
>   - **0–5 ON PURPOSE, not the 0–10 every scored category uses**, so an advisory number can never be mistaken for a graded one. Same reasoning as the existing `Leadership 3/5` candidate signal. The prompt says **3 is the honest answer for an ordinary competent delivery** and that "nothing went wrong" is not a 5 — the same upward-bias correction V18.2 needed.
>   - **THE SHARP EDGE: six of the eight rule sets already RESTRAIN tone** ("where tone is ambiguous, do NOT penalise", plus a ban on inferring personality or suitability from voice). Handing the model a tone section is exactly what could repeal that quietly and start colouring real scores with vibe, so the block makes the flow **ONE-DIRECTIONAL**: it is the only place a tone impression may be a judgement, and it may not be cited to move a category score either way.
>   - **A PRE-EXISTING GAP found while testing this, and LEFT AS FOUND: `SHARED_RULES` — Training Day and Team On-Site Day — has NO audio-medium tone restraint at all**, only a transcript-medium one. That is why the new block states the restraint **in full itself** instead of cross-referencing "the general rules" — a cross-reference would have pointed at nothing on exactly the two rubrics with the longest recordings. Not fixed, because adding it to `SHARED_RULES` moves pinned prompt fingerprints for a reason Jordan did not ask for; there is an assertion **pinning the gap** so it stays a known fact, naming the test to flip if it is ever closed. **Worth raising with Jordan.**
>   - **On the TRANSCRIPT route it is judged ONLY from the inline delivery notes** (`[rushed]`, `[hesitant]`, `[monotone]`, ...). **A transcript with no notes is MISSING EVIDENCE and returns `assessable:"no"` with four nulls — never a low score.** Scoring silence as low energy manufactures data pointing the wrong way, which is worse than collecting none. This matters most on a **Training Day**: the longest recording, so the likeliest to be transcribed — without the branch, the type with the most delivery to observe would be the one guessing hardest about it.
>   - **The four dimensions are given genuinely separable definitions** (tone = how they came across / energy = liveliness and pace / passion = conviction about DECO / enthusiasm = engagement with THIS person). Four near-synonyms would have produced four numbers that always move together and mean nothing individually.
>   - **The person rated follows the session type** (`deliveryPeopleFor`) — facilitator, interviewer, caller or Regional Manager — never the other party, and the block says so outright. A card calling a facilitator a caller is wrong on the one line naming who is being judged.
>   - **Reconciled across passes like a score, not like prose:** the four numbers are **medianed and rounded** (same reason `reconcileSection` rounds); `assessable` is **DERIVED, never copied** from a run, so a pass claiming `"yes"` over four nulls is corrected to `"no"`; `summary` comes from the last run that had one and goes through `safeNoteText` so a `|` can't shatter the reviewer note; clips reuse `mergeClips`. Defensive throughout — a string, an array, a stringified number or a missing field costs the tone read and nothing else, because throwing would lose three paid passes over an advisory extra.
>   - **IT LIVES IN `outputFormatBlock()`, NOT `promptRules()`, and that was deliberate to contain the blast radius.** All four **rule-set** fingerprints are byte-identical and still pass. The three **whole-prompt** fingerprints necessarily moved — a prompt must change to request a new field — and were re-pinned to V23 with the reason written above them.
>   - **THE REAL COST: the Conversion Call prompt behind Jordan's four measured runs (68/68/64/54 vs a human 65) is no longer byte-identical.** The change is additive and advisory so the effect *should* be small, but "should be" is not "is". **The next Conversion Call grade is the one to watch, and if scores shift this is the first thing to suspect.**
>   - **NOTHING ABOUT IT IS CALIBRATED, and it is the most subjective thing in the app.** No real recording has been graded with it, there is no human baseline for "how enthusiastic was that call", and unlike every scored rubric there is **no Smartsheet to calibrate against**. Treat the first batch as data being collected, not measurement — the same caution recorded against the networking rubric, applying harder here.
>   - **The front end does NOT render it yet, and that is a deliberate stop, not an oversight.** `index.html` was being edited by a live parallel session ("Graded item feedback delivery", the Gmail work in the uncommitted diff), and this file's own rule is to check for live sessions before touching it. The data still lands in **column S** (raw JSON, all runs), so collection starts the moment V23 deploys and the card can catch up; an unknown extra field is ignored by `parseAiResultFromRow` — **verified, so nothing breaks in the gap.** Remaining front-end work: a block on the result card, and `reconcileToneAndEnergy` mirrored for the Grading-Log rebuild path (one more duplicated function on the pile).
>   - **Verified: 994/994 in the Apps Script harness** (the count includes every prior version's tests; ~120 are V23's). Also driven end to end through `doPost`: `tone_and_energy` arrives top-level beside an untouched 8-section object, total 56 = 8×7, and it round-trips out of column S through `firstJsonObject` — the exact path the front end will read. No screenshots and nothing visual inspected, because nothing is on screen yet.
>   - **Deploy: backend ONLY. Paste `Code.gs`, New Version labelled V23, confirm with `?cb=` that the URL answers `"version":"V23"` AND `"toneAndEnergy":true`** — the boolean is the better check, since a cached GET has served a stale version string before. No front-end push is needed or wanted yet.
>
> **V22 IS DEPLOYED — BOTH HALVES ARE LIVE (2026-08-16).** Jordan pasted and cut the version; the front end was pushed straight after. It adds the **STAGE ONE INTERVIEW** rubric as the eighth and LAST graded session type — 9 categories out of 90, pass at 54, flattened exactly like every other recruiting rubric. **Every tab in the app now grades; there are no preview tabs left.** **Verified rather than assumed**, with the cache-buster:
>
> ```json
> {"ok":true,"version":"V22","model":"gemini-3.5-flash","apiKeySet":true,"longAudio":true,
>  "actions":["prepareAudio","checkAudio","transcribe","grade"],"transcription":true,
>  "transcriptWindows":true,"maxAudioSeconds":32518,
>  "gradingPasses":{"Team On-Site Day":3,"Training Day":1,"Conversion Call":3,
>    "Reference Check":3,"Parent Reference Check":3,"Stage 2 Interview":3,
>    "Networking Activity":3,"Stage 1 Interview":3},
>  "sessionTypes":["Team On-Site Day","Training Day","Conversion Call","Reference Check",
>    "Parent Reference Check","Stage 2 Interview","Networking Activity","Stage 1 Interview"]}
> ```
>
> **`sessionTypes` naming all EIGHT is the proof**, not the version string on its own. Backend went first for the usual reason: the front end flips the Stage 1 tab from preview to live, and a V21 backend refuses the `sessionType` *after* the audio is already in Drive.
>
> **NOTHING ABOUT STAGE 1 IS CALIBRATED**, and the same is true of the five other rubrics loaded over 2026-08-14/15. See the audio-only trap note below before touching that prompt — it is the sharpest one in the app.
>
> Things worth knowing before touching it:
>   - **THE AUDIO-ONLY TRAP IS THE WHOLE STORY OF THIS VERSION, and it is the sharpest one since the networking list.** `interviewMediumIntro()` was written for Stage 2 and asserts *"EVERY criterion in this rubric is verbal."* That is true of Stage 2 and **flatly false here**: Stage 1's category 6 (Instrumental / Professional Readiness) names handshake, eye contact, appearance and punctuality. A category with no confident score counts as 0, one zero fails the whole interview under the every-category-6 rule — so a model reading "I cannot see them" as "it did not happen" would have **failed every audio-only Stage One Interview ever recorded**, however well it was run, with nothing on the card explaining why. The fix is `STAGE_ONE_SPEC.carveOut`: eight of nine are verbal, category 6 is the exception, **unavailable ≠ missing**, here are the audible signals it *is* scored on — and, closing the loophole, an interviewer who made **no** readiness assessment at all still scores low. Repeated in the rubric body at category 6 (`MODALITY RULE`) where the number is actually chosen. **Nine assertions cover it across both mediums. Do not "simplify" it back into a shared line.**
>   - **`interviewMediumIntro()` now takes a spec, and that rewrote a constant a LIVE prompt is built from.** So the test is not a presence check: it reconstructs V21's exact Stage 2 intro as an **independent literal** and compares. Came out byte-identical.
>   - **Section keys carry a `stage1_` prefix, and `SECTION_KEY_PREFIXES` in `index.html` gained it in the SAME change.** Stage 1 shares two category names with Stage 2 (behavioural probing, motivation/fit) so the keys had to be namespaced, and the prefix then leaks onto the card unless the front end strips it. **That leak has now shipped twice** (`parent_`, then `stage2_` and `networking_`) and both times only rendering a card caught it — so a card was rendered this time, with all nine headings checked. Clean.
>   - **CERTIFIES from a passing manual grade** (Jordan, asked and answered) → `Certified - Stage 1 Interviews`. **With this, all five recruiting certification columns now certify from a grade**, and the "no rubric loaded means no score to certify off" reason that governed the hold-outs is fully spent. The safety property is untouched and is the only thing making any of it legal: `certifyOnRoster` is reached ONLY from `manualGradePasses`, and **an AI grade still writes nothing to the Roster on any session type.**
>   - **THE STALE-COPY HAZARD FIRED AGAIN, exactly as this file predicted.** The Recruiting Dashboard's on-screen note read *"Four of these certify from a grade; Stage 1 Interview is ticked by hand"* over an app that now ticks it, plus **three** internal comments carrying the same dead reason. All four fixed in this change. **The rule stands and has now caught something twice: adding a session type to `SESSION_CERT_COLUMN` is not done until that note and the comments around `RECRUITING_CERT_COLUMNS` and `SESSION_CERT_COLUMN` have been re-read.**
>   - **Three passes**, by having no entry in `GRADING_RUNS_BY_SESSION_TYPE` — right for an uncalibrated rubric, and an interview is far too short for the cost argument that cut Training Day to one.
>   - **Two real defects found by running it, not by review.** The transcript carve-out read **"you cannot read in the transcript them"** — a phrase slotted into a sentence built for a verb; it is now "a transcript cannot show them" / "an audio recording cannot capture them". And the pre-existing *"an unsupported session type is refused"* test used **Stage 1 Interview** as its example of an unsupported type, so loading the rubric broke it — the second time that assertion has been broken by its own example becoming real (it was `Conversion Call` until V18). It now uses an invented `Stage 3 Interview` with a note saying to change the string rather than delete the test.
>   - **Verified: 729/729 in the Apps Script harness** (the count includes every prior version's tests; ~45 are Stage 1's). Front end driven in a real browser: the tab live, max 90 / bar 54, Pass–Fail wording, "Interviewer being graded", a full result card rendering with **no prefix leak** on any of the nine headings, zero preview tabs left, no page overflow at 1280 or 375, no console errors from the app. **No screenshots — the Browser pane was reporting a 0-width viewport again**, so nothing about appearance was inspected, only measured.
>   - Full rubric and every decision behind it: `docs/stage-1-interview-rubric.md`.
>
> **V19 IS WRITTEN AND IS NOT DEPLOYED (2026-08-15), AND IT WILL RIDE IN ON V20'S PASTE — the same way V17 rode in on V18's.** It adds **both REFERENCE CHECK rubrics** as the fourth and fifth graded session types: `Reference Check` and `Parent Reference Check`, 7 categories out of 70 each, pass at 42, flattened exactly like the Conversion Call. `Code.gs` in this repo now holds V19 **and** V20; one paste ships both, and the version string will read V20. **Confirm with `?cb=` and check `sessionTypes` names all six** — the version alone won't tell you V19's half arrived.
>
> **V21 IS DEPLOYED — AND IT CARRIED V19 AND V20 IN WITH IT. ALL SEVEN SESSION TYPES ARE LIVE ON BOTH HALVES (2026-08-15).** Jordan pasted and cut the version; the front end was pushed straight after. **Read the two notes above as settled** — V19 and V20 were never deployed on their own, they rode in on this paste exactly as V17 rode in on V18's, and the version string reads V21 rather than either of theirs. **Verified rather than assumed**, with the cache-buster, which is the whole reason that rule is written down:
>
> ```json
> {"ok":true,"version":"V21","model":"gemini-3.5-flash","apiKeySet":true,"longAudio":true,
>  "actions":["prepareAudio","checkAudio","transcribe","grade"],"transcription":true,
>  "transcriptWindows":true,"maxAudioSeconds":32518,
>  "gradingPasses":{"Team On-Site Day":3,"Training Day":1,"Conversion Call":3,
>    "Reference Check":3,"Parent Reference Check":3,"Stage 2 Interview":3,
>    "Networking Activity":3},
>  "sessionTypes":["Team On-Site Day","Training Day","Conversion Call","Reference Check",
>    "Parent Reference Check","Stage 2 Interview","Networking Activity"]}
> ```
>
> **`sessionTypes` naming all seven is the proof V19's and V20's halves arrived** — the version string alone could not have told you, which is exactly what the V19 note warned about. ~~**Only Stage 1 Interview is left as a preview tab.**~~ **Superseded by V22 (see above): Stage 1 is loaded, and there are no preview tabs left at all.**
>
> **NOTHING ABOUT ANY OF THIS IS CALIBRATED.** Five session types went live in two days on rubrics nobody has graded a real recording against; the flattening consequences recorded per rubric in `docs/` are all still unmeasured, and the every-section-6 rule is stricter than the source documents on every one of them. The first real Stage 2 or networking grade to come back a Fail is the thing to look at hardest — see the note in the V20 block about its 5%-weighted category becoming a hard gate.
>
> Things worth knowing before touching it:
>   - **It is the first graded type at the TOP of the funnel — the only one that CREATES candidates rather than filtering them.** That is why the tab sits *before* Conversion Call: tab order is process order, and this is the step that feeds the call.
>   - **THE AUDIO-ONLY TRAP IS THE SHARPEST IN THE APP, and the fix must not be "simplified" away.** Stage 2's carve-out says "nothing in this rubric is visual" — **flatly false here.** Category 4 wants names written down and category 6 wants people marked by colour; both are physical acts on paper, both are CRITICAL, and a category with no confident score counts as 0. A model reading "I cannot see the page" as "it did not happen" would **fail every audio-only networking activity that was ever run well.** Jordan's own evidence standard ("if evidence is absent, treat the behavior as not demonstrated") *sharpens* the trap rather than solving it — right about behaviours, wrong about modality. `networkingMediumIntro()` draws the line: **unavailable ≠ not demonstrated**, and 4 and 6 are scored from the narration. A facilitator who runs the colour pass silently still scores low, and that is correct.
>   - **No certification column, by decision.** Jordan was asked directly on 2026-08-15 and chose live grading *without* one, so `SESSION_CERT_COLUMN` has no entry, the auto-certify and un-certify paths are inert by construction, and a passing manual grade signs nobody off. The Recruiting Dashboard still has five columns, not six. Verdict reads **Pass / Fail**.
>   - **The flattening costs more here than on any other type.** The source rubric weights category 8 (Communication, Tone & Non-Pressure) at just **5%** and leaves it non-critical — the lowest weight anywhere in the app — while the flat model makes it as decisive as the two 15% critical categories. A 4 there with 8s elsewhere is a weighted **7.80 PASS** on Jordan's rubric and a **FAIL** in the app.
>   - **Four of the five automatic fails describe how a PERSON was treated**, not how a process was run, so rule 10 additionally requires a quoted moment for each. An unevidenced accusation about somebody's conduct is worse than a missed one.
>   - **`callRuleTwoBullets()` now takes its noun as an argument**, the same refactor V20 made to `callScoringCalibrationBody`, so "facilitator" needed no second copy. The Conversion Call, reference-check and Stage 2 prompts are byte-identical — there is a test pinning the exact literal.
>   - **The transcription prompt got a networking branch too.** Left out it falls through to the field-training intro and labels the voices TRAINER / OPERATOR — exactly the V18.1 bug, one session type along. FACILITATOR / PARTICIPANT.
>   - **Verified: 658/658 in the Apps Script harness** (319 pre-existing all still passing, plus the networking block), and the front end driven in a real browser **with screenshots — the pane was compositing this session.** Checked: the tab live and in process order, `Facilitator being graded` on the form, manual grade capped at 80, pass mark 48, Pass/Fail wording, no cert column and no prerequisites, three passes, the recruiting board picking it up, no page overflow at 1280 or 375, and no console errors from the app.
>   - Full rubric and every decision behind it: `docs/networking-activity-rubric.md`.
>
> **Both halves must go together for these two, and the front-end half is the one that breaks loudly.** It flips the Reference Checks tab from preview to live, and a V18.3 backend answers `sessionType must be...` *after* the audio is already in Drive. **Backend first**, then push `index.html`.
>
> **V18.3 IS DEPLOYED — BOTH HALVES ARE LIVE (Jordan pasted and cut the version on 2026-08-15; front end pushed the same day). IT CARRIED V18.2 IN WITH IT**, so the stricter Conversion Call grading went live in the same paste. Verified rather than assumed: the Web App URL answers `{"version":"V18.3", ..., "gradingPasses":{"Team On-Site Day":3,"Training Day":1,"Conversion Call":3}}`.
>
> **THE VERSION CHECK CAN SERVE A CACHED ANSWER, AND IT DID — read this before diagnosing a "failed" deploy.** The first GET after this deploy returned the **previous** version (`V18.2`) with no `gradingPasses` key at all, which reads exactly like a paste that didn't take or a New Version that was skipped. Same URL one second later with a unique query parameter (`?cb=<guid>`) returned V18.3 correctly. **So always cache-bust the diagnostic**, and never conclude a deploy failed off a single clean GET — the whole point of `doGet` is to answer "did this actually ship?", and a stale answer sends someone to re-paste and re-deploy work that was already live.
>
> What V18.3 does: it grades a **TRAINING DAY IN ONE PASS** instead of three; every other session type is untouched at three. Jordan: *"if the audio for day 2 training is being read 3 times that is probably causing issues ... change it so the audio is just being read once for the training day grader, all other ones would be read and compared 3 times."*
>
> **What it buys, measured off the real 4h33m file: audio grading goes $1.94 → $0.65, and a re-grade $0.65 → $0.22.** Exactly a third, because the recording is sent to Gemini once instead of three times concurrently. It also cuts the load on Google by two thirds, which matters more than it sounds — three simultaneous 410k-token requests are precisely the shape the model sheds first under demand (see the 503 diagnostic below).
>
> **BE HONEST ABOUT WHAT IT DOES NOT FIX, because Jordan's stated reason is only half right.** Since V15 the passes go out **together** via `fetchAll`, so wall-clock was already one call's worth, not three. This change does **not** by itself move Apps Script's 6-minute wall. It cuts cost and concurrent load; it does not make a long recording finish that wasn't finishing.
>
> **What it costs, and this is the real trade:** no median, no cross-check, no "the runs disagreed here" flag, and no corroborated timestamps on the session type with the **most** sections (11) and the longest recordings. Consensus grading exists because single calls produced inconsistent scores — that is exactly the risk being accepted here, and it is Jordan's call to accept it.
>
> Decisions worth not re-litigating:
>   - **`GRADING_RUNS_BY_SESSION_TYPE` + `gradingRunsFor()` are the whole mechanism**, mirrored on the front end as `GRADING_PASSES_BY_SESSION_TYPE` / `gradingPassesFor()` so the price quoted before Submit is the price that gets spent. The backend is still the only thing that decides how many passes fire; a stale front-end copy misquotes a figure, it does not change what runs.
>   - **The "only one pass completed, treat this as shaky" per-section flag is now gated on a pass being MISSING (`expectedRuns > 1`), not merely on there being one.** On a one-pass type nothing went wrong, and flagging all 11 sections would put "Double-check" on every row of every card — which says the same as flagging none of them. **It would also have re-opened a drift this file already warns about twice:** `index.html` rebuilds sections from column S when a session is re-opened from the Grading Log and has no way to know how many passes were *expected*, so a fresh grade would have shown 11 flags and the same session re-opened would have shown none. Verified byte-for-byte: **0 flagged on both paths.**
>   - **What that flag used to say is now said ONCE, in the reviewer notes, on every one-pass grade** — "Graded in ONE pass, not the usual three ... no second opinion behind them: no median, and nothing to flag where two passes would have disagreed." Kept free of ` | `, which is the separator `index.html` splits these notes on.
>   - **The form says it too, before Submit** (`.audio-plan`), because it is half of why the price is a third of what it was and somebody reading a cheaper number deserves to know what was traded for it while it can still be reconsidered. Same reasoning as `renderCertPreview`.
>   - **`runScoresLine` now takes its count off the array instead of hardcoding "3".** It was already capable of printing "3 passes: 8 · —" over a two-pass degraded grade; with one pass in play a literal 3 would simply be false. Still no denominator — see the note on `clipChips`.
>   - **`doGet` publishes `gradingPasses` per session type**, so "did the one-pass change actually deploy?" is answerable from a browser with no Gemini spend — the same job the version string does.
>   - **A one-pass grade can never show a corroborated timestamp** (`CLIP_CORROBORATION_MIN_RUNS = 2`), which is correct and needs no change: one pass citing a moment is the normal case, and an unmarked chip already means "no corroboration", not "doubtful".
>   - **Found while measuring, and fixed in passing:** on a long recording the plan box recommended the transcript route as "safer" while quoting the audio price beside it — on a Training Day that is **$13.62 against $0.65**, so the box was recommending a route costing twenty times the figure next to it without saying so. It now names both numbers. "Safer" is still about *finishing*, not money, and the copy says which.
>   - **339/339 in the Apps Script harness** (312 pre-existing plus 27 new). **Three pre-existing tests genuinely changed behaviour and were updated rather than worked around:** two asserted three passes on a *Training Day* (one through `doPost`, one on the transcript route), and the degraded-honesty block used Training Day as its example of a three-pass type — that block moved to Team On-Site Day, and a new `doPost` Conversion Call test was added so "a Training Day ran one pass" is proven to be about the session type rather than about `doPost` having quietly lost two passes for everybody.
>   - **Verified in a real browser against the actual edited files, both schemes** (no screenshots — the Browser pane was reporting a 0-width viewport again, so nothing about layout was measured). Checked: the pass count for all five type cases including an unknown type and `undefined`; the one-pass grade producing a real total and a real verdict off a single run; the reviewer note splitting into two clean bullets; Team On-Site Day and Conversion Call byte-unchanged at three; `doGet`'s map; the front-end cost model at 4h33m and at 2:22; the plan box on both mediums and both pass counts; the Grading-Log rebuild agreeing with the fresh result; the three-pass spread flag still firing (`9 · 6 · 3` → median 6, flagged); contrast **7.47 light / 10.55 dark** on every plan-box line, all wrapping; no app console errors.
>
> **Previous note, kept because it ships inside this same paste: V18.2 makes the CONVERSION CALL grader stricter and touches nothing else.** Jordan, on two fresh AI grades of 74/80 and 76/80: *"Since the update I need it to be harsher on grading — for context I rated it 64, same audio recording."*
>
> **The evidence is the strongest calibration signal this project has, and it points one way.** The same call, human **64–65**, AI **68, 68, 74, 76** — above the human every time, and drifting *further* up rather than settling. **Read this as correcting the headline of the per-category entry below, not contradicting it:** that entry found the AI marking harder than the human on individual "did they explicitly confirm X" categories, and both can be true at once — it is stingy on a few checklist items and generous on everything else, and the everything-else wins the total. **At the total it has never once come in under the human.**
>
> Two levers, both scoped to Conversion Call by construction (Training Day and Team On-Site Day prompts are byte-identical, with tests on the exact section block in both mediums):
>   - **A SCORING CALIBRATION block in conversion rule 2.** It names the upward bias outright instead of saying "be strict" — the diagnosis written into the prompt is that the model credits a category because the topic *came up* rather than because the evidence standard was met. Then it fixes what the anchors mean on an ordinary call: **a solid, professional, entirely ordinary call is a 6 or a 7 in most categories, not a 9**, and "nothing went wrong" is not worth 8 on its own. Plus a mechanical ceiling tied to the model's own `missed` list — not empty means no 10, two or more real gaps means no more than 7. **Tying it to `missed` is the point:** it binds the score to output already on the card, so the ceiling can be checked rather than trusted.
>   - **`covered` and `missed` are asked for BEFORE `score` in the section JSON.** The model writes fields in the order it is shown them, so scoring first meant committing to a number and only then working out what was missing — and a ceiling cannot bind to a list that does not exist yet. Key order means nothing to `JSON.parse`, so nothing downstream sees it. `justification` also now has to name what kept the score off the next anchor up.
>
> **THE FLOOR IS HELD ON PURPOSE AND THIS IS THE PART NOT TO UNDO.** Strictness applies *above* the pass mark. One category under 6 fails the whole call on its own, so a grader that manufactures sub-6 scores to look rigorous turns a calibration fix into false Fails on real candidates. The prompt says so in as many words, and a test asserts it.
>
> **What it does NOT claim.** The target is one call, and it is the call Jordan made himself — the confound recorded below has not gone anywhere. Nothing is measured against a second call, a weak call, or a call near the 48 pass mark. **The direction is well evidenced; the size of the correction is a judgement.** It may now undershoot. **First thing after deploying: re-grade this same call — it is the only one with a per-category human grade against it — and read the per-category deltas on the Compare tab, not the total.** Two different splits can both sum to 64, and that trap has caught this project once already. Watch specifically for anything landing under 6 that Jordan passed, because that flips a verdict rather than nudging a number.
>
> **312/312 in the Apps Script harness** (284 pre-existing all still passing, 28 new). **One stale assertion fixed in passing:** `doGet reports V18` had been failing since `CODE_GS_VERSION` went to V18.1 — the harness on disk was built before that bump, so the last "284/284" was taken against an older Code.gs. It's now hardcoded to the current version deliberately, with a comment saying why, since a test comparing the constant to itself would guard nothing.
>
> **V18.1 IS DEPLOYED** — confirmed indirectly but firmly: the 2026-08-14 screenshots show evidence timestamps rendering on a transcript-graded session, which is exactly what the `parseClipSeconds` bracket fix restored. The note below is kept for what the fix was.
>
> **V18.1 (2026-08-14).** Two fixes, one of them a bug that had silently disabled evidence timestamps on the ENTIRE transcript route since V16:
>   - **`parseClipSeconds` now strips square brackets.** A transcript writes markers as `[0:01:22]`, and rule 7 told the model to copy the marker *"exactly as written there"* — its own placeholder read `<the [H:MM:SS] marker COPIED from the transcript>`, brackets included. The regex is anchored at both ends, so every clip parsed to `null` and was dropped. **This killed the one thing transcript grading was supposed to do BETTER than audio** (copy an anchored marker instead of estimating one), and it failed silently — a section with all clips dropped is indistinguishable from a section with no clips, and the card then explained it as "graded before evidence timestamps existed". Fixed at both ends: the parser tolerates brackets (which also **recovers rows already written**) and the prompt now asks for the digits without them. **Both copies of `parseClipSeconds` changed** — the front-end one is what rebuilds a session re-opened from the Grading Log.
>   - **The transcription prompt now has a Conversion Call variant.** It was hardcoded to "field-training session" with speaker labels TRAINER / OPERATOR / CUSTOMER, so a recruiting call had its job applicant labelled "the Site Operator / trainee being trained" — nobody on the call. Now CALLER / CANDIDATE. **Missed in V18, which gave the GRADING prompt a conversion variant and not the transcription one.** The front end had to change too: `action:'transcribe'` was not sending `sessionType` at all, so the branch would never have fired — the same pass-through miss V17 made once before, and worth checking by hand every time a prompt starts depending on a new payload field.
>
> Also fixed, front-end only: the no-clips message asserted a cause it cannot know ("graded before evidence timestamps existed") and so confidently mis-explained a brand-new session. It now just says no timestamps came back.
>
> **Deploy the backend first**, then push `index.html`. 284/284 in the harness.
>
> **V18 IS DEPLOYED — AND SO IS V17, IN THE SAME PASTE (2026-08-14). BOTH HALVES ARE LIVE.** Jordan pasted and cut the new version; the front end was pushed straight after. **Verified rather than assumed**, which is what the `doGet` diagnostic is for — the Web App URL answers:
>
> ```json
> {"ok":true,"version":"V18","model":"gemini-3.5-flash","apiKeySet":true,"longAudio":true,
>  "actions":["prepareAudio","checkAudio","transcribe","grade"],"transcription":true,
>  "transcriptWindows":true,"maxAudioSeconds":32518,
>  "sessionTypes":["Team On-Site Day","Training Day","Conversion Call"]}
> ```
>
> **`transcriptWindows:true` and `maxAudioSeconds:32518` are the proof V17 rode along** — the repo copy had been sitting ahead of production on V17 since earlier the same day, and pasting the file for V18 carried it in. So the bounded-window transcription work (8-hour recordings, the adaptive window sizing from V17.1, and the V17.2 constants measured off Jordan's failed 4h33m run) is now live and **has still never completed a real long recording end to end.** That remains the open question it always was; only its deployment status changed.
>
> **The old note here said the order mattered more than usual, and it did:** the front end flips the Conversion Call tab from preview to live, and a V17 backend would have answered `sessionType must be "Team On-Site Day" or "Training Day"` — a live-looking tab that fails on submit *after* the audio is already in Drive, with no graceful fallback of the kind V15/V16 have. Backend went first. Keep that order for any future session type.
>
> **Previous note, kept because the reasoning still applies to the next one: V17 WAS WRITTEN AND NOT DEPLOYED (2026-08-14).** `Code.gs` in this repo is once again ahead of production, and this time it matters: the front-end half is what drives the bounded windows, and it degrades to V16's open-ended behaviour against a V16 backend. **Deploy the backend first** (paste, New Version labelled V17), then push `index.html`. Confirm with the Web App URL — it must answer `"version":"V17"` and `"transcriptWindows":true`. V17 is **8-hour recordings**; see the dedicated item under "Known quirks".

**Confirming which version is actually live — open the Web App URL in a browser, WITH A CACHE-BUSTING QUERY PARAMETER** (`?cb=<anything-unique>`). A plain GET, no grading, no cost, no quota. It returns `{"ok":true,"version":"V14","model":"gemini-3.5-flash","apiKeySet":true,...}`. **The cache-buster is not optional — measured 2026-08-15, the first GET after a confirmed-good deploy returned the PREVIOUS version, missing the key that deploy had added.** That reads exactly like a paste that didn't take, and would have sent somebody to re-deploy work that was already live; the same URL with a unique parameter answered correctly. **Never conclude a deploy failed off one clean GET.** Added 2026-08-14 because "did the New Version step actually happen?" previously could only be answered by spending 3 Gemini calls on a test session and inferring it from the result — and skipping New Version silently leaves the old code running at the same URL, which has caught this project out before. **`CODE_GS_VERSION` has to be bumped in the same edit that changes the header's version label**, or it will confidently report a version that isn't running. The response is public (the Web App is "Anyone" by necessity — see the CORS note below); it exposes a version string, the model name, and a boolean for whether a key is set, never the key itself.

## Rules that matter when changing grading logic

- **BC only.** Everything is scoped to British Columbia. Other provinces are the final phase and explicitly not active work — don't build toward them.
- **Replacements 101 is always graded in BC**, in every territory, never marked N/A. (Confirmed by Jordan; a region-based alternative was considered and rejected.)
- **Certification formulas** must match the Smartsheets exactly:
  - Training Day: total out of 110 across 11 sections; certified = total ≥ 85 **and** every section ≥ 6.
  - Team On-Site Day: total out of 50 across 5 sections; **a recording passes** = total ≥ 38 **and** every section ≥ 6.
  - Conversion Call: total out of 80 across 8 categories; certified = total ≥ 48 **and** every section ≥ 6. **No Smartsheet to match here** — it's the flattened form of the source rubric's weighted 6.0/10, and 48 is both 60% of 80 and exactly 8 × the section pass mark. Certifies from a passing *manual* grade as of 2026-08-14; see the entry under Repo layout.
  - Reference Check **and** Parent Reference Check: total out of 70 across 7 categories each; certified = total ≥ 42 **and** every section ≥ 6 (`Code.gs` V19, 2026-08-15). No Smartsheet to match — same flattening as the Conversion Call, and 42 is both 60% of 70 and exactly 7 × the section pass mark. **Two separate rubrics behind one tab**, so passing one certifies nobody for the other. Both certify from a passing *manual* grade (Jordan's call, 2026-08-15). See `docs/reference-check-rubrics.md`.
  - Stage 2 Interview: total out of 90 across 9 categories; pass = total ≥ 54 **and** every section ≥ 6 (`Code.gs` V20, 2026-08-15). No Smartsheet either — same flattening as the Conversion Call, and 54 is both 60% of 90 and exactly 9 × the section pass mark. **Certifies from a passing *manual* grade** (Jordan, asked and answered 2026-08-15) — 54+ with the sections box ticked ticks `Certified - Stage 2 Interviews` on the **Recruiting Dashboard**, one pass, AI grades still write nothing. See `docs/stage-two-interview-rubric.md`.
  - Networking Activity: total out of 80 across 8 categories; **a recording passes** = total ≥ 48 **and** every category ≥ 6. **No Smartsheet and no certification column** — see the entry under Repo layout. Same 48/80 as the Conversion Call because both rubrics happen to run to eight categories, not because they were copied.
  - Stage 1 Interview: total out of 90 across 9 categories; pass = total ≥ 54 **and** every category ≥ 6 (`Code.gs` V22, 2026-08-15). No Smartsheet — same flattening as the Conversion Call, and 54 is both 60% of 90 and exactly 9 × the category pass mark. Same numbers as Stage 2 because both interview rubrics run to nine categories. **Certifies from a passing *manual* grade** (Jordan, asked and answered 2026-08-15) → `Certified - Stage 1 Interviews` on the **Recruiting Dashboard**, one pass, AI grades still write nothing. **Its category 6 is the audio-only trap** — see `docs/stage-1-interview-rubric.md` section 7 before touching that prompt.
- **A Team On-Site Day certification takes THREE passing recordings, not one** (Jordan, 2026-08-14 — `SESSION_PASSES_REQUIRED`). The per-recording formula above is unchanged and still matches the Smartsheet; the count is a layer on top that the Smartsheet has no concept of, so **the Dashboard certifies LATER than the Smartsheet's own formula would**. That is the intended change, not a drift to reconcile. See the full entry under Repo layout.
- **A section with no confident score counts as 0**, not as "can't determine." `certified` is always a real Yes/No.
- **Consensus grading:** a session is graded by 3 Gemini calls (fired in parallel since V15) that get reconciled (median per section). This costs ~3x tokens — the tradeoff is deliberate, because single calls produced wildly inconsistent scores. Don't "optimize" it back to one call. **EXCEPT on a Training Day, which Jordan cut to ONE pass on 2026-08-15 (V18.3) — see `GRADING_RUNS_BY_SESSION_TYPE`, and don't "restore" it to three.** The count is per session type now; a type not listed there still gets 3.
- **Passes that fail are survivable, but a degraded result must say so (V11).** A 429 or 5xx on one pass is retried (waiting the delay Google suggests, capped so backoff can't eat the 6-minute budget); a *daily* quota 429 is not retried, because waiting can't clear a daily cap. If a pass still fails, grading proceeds with whatever completed rather than throwing away passes already paid for — but the reviewer notes say how many actually ran, and **a single completed pass flags every section for human review**. That last part matters: `reconcileSection` can't detect disagreement it never saw, so one uncorroborated pass would otherwise produce a confident-looking score with nothing behind it.
- ~~Because grading is 3 sequential calls, a long recording can approach Apps Script's 6-minute web app limit. If timeouts appear, either lower `GRADING_RUNS_PER_SESSION` to 2 or restructure as parallel `UrlFetchApp.fetchAll` calls.~~ **Both halves of that are done and the note is kept only so nobody re-does them:** the passes have run in parallel via `fetchAll` since V15, and the count is per session type since V18.3 (Training Day is 1). **So neither lever is left to pull if a long recording still times out** — the remaining one is one pass per execution, and `callGeminiPasses` already isolates the piece that would change.
- **Evidence timestamps (V10).** Every section returns `evidence_clips` — 1–3 `{at, quote}` moments pointing at the audio that drove its score — and the results card turns those into clickable jump points on an in-page player. The point is that a flagged section can be checked in about a minute instead of by re-listening to the whole session. These are an aid to a human's judgement, **not** citations. Don't present them as exact anywhere in the UI.
- **Measured timestamp accuracy — two independent measurements, both on the same short test clip.** The feature works; the open question is length, not accuracy (see below).
  - **2026-08-13, Training Day prompt.** Jordan hand-checked all 5 citations against the audio. Cited 0:07 / 0:28 / 0:43 / 0:55 / 1:43; the matching exchanges start at 0:11 / 0:32 / 0:43 / 0:58 / 1:43. **Off by 0–4 seconds, always slightly early, never late.**
  - **2026-08-14, Team On-Site Day prompt, same audio file** (`Test Audio for AI.m4a` — Jordan confirmed it's the same recording, so this is a genuine second reading of the same ground truth, through a *different* rubric and prompt). Against the anchors established the day before: the three pillars at 0:43 → **cited 0:45**; "go the extra mile" / the pickle line at 1:07 → **cited 1:06**; replacement criteria at 1:43 → **cited 1:42**; the VIP package at 2:06 → **cited 2:06 exactly**. **Off by 0–2 seconds, and this time in both directions rather than always early.**
  - **What this pair actually establishes:** the citations are reproducible across a prompt change (the two runs cite near-identical moments — 0:34/0:35, 0:43/0:45, 0:55/0:56), and the error is small enough in both directions that `CLIP_PREROLL_SECONDS = 3` is the right call and shouldn't be re-tuned off this data. **What it does not establish is anything about a long recording** — both measurements come from the same ~2½ minutes, so drift over a 60-minute session remains completely unmeasured. Don't let "measured twice" get quoted as "validated".
- **HOW TO VERIFY A TIMESTAMP — read this before concluding they're broken.** Check a citation against **where that topic starts**, not against a specific phrase inside it. The prompt tells Gemini to point at where an exchange *begins*, so measuring against a line spoken later in the same exchange makes it look badly late when it isn't. This exact mistake was made on 2026-08-13: two citations looked ~23s off and were written up as an uncorrectable "clock slip", with an elaborate silence-in-the-audio theory attached. Both were correct — 0:43 was checked against the pickle line at 1:07 when it was pointing at "is three things DECO" (the pillars) at 0:43 exactly, and 1:43 was checked against the VIP package at 2:06 when it was pointing at "if they have damage", where the replacement discussion opens. Don't rebuild that theory; measure properly instead.
- **Corrections are the fix-list, not a second Focus Areas (V13, 2026-08-13).** Jordan's point: a Training Day runs over two days and the recording gets submitted while there's still time left with the Site Operator, so the card was missing the one thing a trainer needs in that moment — what to go back over before the session ends. Each entry is `{section, issue, correct, at, priority}`: what was actually said or skipped, what it should be per the rubric/reference data, where to hear it, and `must_fix` vs `should_fix`. Things that matter:
  - **The prompt has to say explicitly that this is not `focus_areas`** (Code.gs rule 9). Focus areas are for the *next* session; corrections are for *this* one, before it's over. Without that line Gemini returns the same list twice.
  - **An entry with no timestamp is an omission, and those are the valuable ones** — something that never came up at all still needs covering before the day ends. The card labels those "never came up" / "Missing" instead of "Heard", rather than just showing no jump point.
  - **An unrecognised or missing `priority` renders with no badge**, not a guessed one. The page must not claim an urgency the AI never assigned.
  - **Half a correction is dropped, not shown.** An entry missing either the issue or the fix can't be acted on, and a to-do row nobody can act on is worse than one fewer row.
  - Capped at 6, `must_fix` first — it's a list somebody works through in the hours that are left, not a report.
- **`CLIP_PREROLL_SECONDS` is 3.** Citations run 0–4s early already, so playback only needs a few seconds of run-up. The original 10 was guesswork and just added dead air before the exchange. The ±15/+30 nudge buttons on the player stay useful regardless — the cited moment is the *start* of a topic, so the specific line a manager wants is usually a little further in.
- **Sections with more than one timestamp (2026-08-13).** A section can carry up to 3 moments, and three bare times (`00:34 02:15 07:41`) gave a manager nothing to choose between without hovering each one. So:
  - **One clip renders exactly as before** — a compact time chip, quote in the tooltip. **Two or more stack**, each with the first 5 words of its quote underneath. The split is deliberate: there's nothing to tell apart when there's only one, and a quote line on all 11 sections would undo the card's 2026-08-13 tightening. Measured at 1280px: a 3-clip section grows the row to ~294px, a 1-clip section stays at 160px.
  - **A moment two or more passes independently landed on says so in words, under the chip: "2 passes point here".** `mergeClips` always counted this (it's how it ranks when a section is over the 3-clip cap) and then dropped it before rendering, so a moment all 3 passes flagged looked identical to one a single pass mentioned. The count says *where to start listening is a better bet*, *not* that the score is more right — the footnote says so.
  - **This shipped first as a compact `×2` / `×3` badge on the chip and that was wrong.** Jordan read `1:42 ×2` on sight as "there are 2 timestamps to review here" — the exact opposite of its meaning, since a corroborated clip is ONE moment several passes agreed on. Any small glyph next to a number invites that reading. **Don't reintroduce a badge here; the count gets plain words or it doesn't ship.** The footnote now leads with the distinction (one moment vs. several) rather than just defining the marker.
  - **The count is never shown as a fraction.** A degraded grade (V11) can run on fewer than 3 passes, so a printed denominator would sometimes be a lie.
  - **This was front-end only — no `Code.gs` change, no redeploy.** Both copies of `mergeClips` already emit `runs`; only rendering changed. The 158px score column is why quotes are cut to 5 words with a 2-line CSS clamp behind them.
  - Still unaddressed: if more than 3 distinct moments survive merging, the extras are dropped with nothing on screen saying so.
- **Still untested at length.** All measurement so far — both hand-checks — is from the *same* short test clip. Whether accuracy holds across a 60-minute session is genuinely unknown — check one citation near the start and one near the end of the first real session, using the verification method above. If accuracy degrades badly over a long recording, the honest response is to stop rendering a clickable time and show only the quote, so a manager searches for words instead of trusting a number that lies.

## Known quirks worth knowing before you debug

- **Corrections have no sheet column, and that's deliberate.** The `corrections` fix-list (V13) rides along inside column S's raw JSON and gets rebuilt by `correctionsFromRuns()` in `index.html`, exactly like evidence clips. It has no column of its own because T/U/V now belong to the follow-up state — a 20th column appended by `Code.gs` would land on top of `Follow-Up Status`. Both sides take corrections from the **last completed run** (they're prose, not scores) and both sort by **priority only**, relying on a stable sort to preserve the AI's own ordering inside each priority. Change one side's ordering and a session's fix-list will reorder itself depending on whether it was freshly graded or re-opened from the log.
- **Column X ("Transcript Link") is Code.gs's, and it is the one extended column the front end does NOT write (V16).** It has to be the backend's: the backend is what knows where the transcript ended up in Drive. It sits past T/U/V/W so it collides with nothing, it's written by a separate `setValue` after `appendRow` (which only ever writes A–S), and it's in its own `try`/`catch` — the grade is already saved by then, and a session whose link failed to write must never be reported as a failed grading run. **`GRADING_LOG_LAST_COLUMN` in `index.html` moved W → X in the same change**, because a delete that stopped at W would leave an orphaned transcript link pointing at a session that no longer exists — the exact failure the W note below already warns about, one column along.
- **Columns T through W and column Y of the Grading Log are the front end's, not Code.gs's** (X is the one exception — see the note directly above). Follow-up state (`Follow-Up Status` / `Follow-Up Note` / `Follow-Up Date`, T/U/V) is written onto the **AI row**; the sections tick (`All Sections 6+`, W) onto the **manual row**; and the human grader's own per-section scores (`Human Section Scores`, Y, from the Compare tab) onto the **AI row** — all by `index.html` alone. Code.gs appends 19 columns (A–S) with `appendRow` and never reads or writes past S, so none of the Follow-Ups tab, the sections tick or the Compare save needed **any backend change or redeploy** — and mustn't be given one by accident. Two things to keep in step: `ensureExtendedHeaders` in Code.gs only writes headers when `Source` is missing (so extra columns don't confuse it), and **deleting a submission clears out to `GRADING_LOG_LAST_COLUMN`, not `A:S`** — stopping short leaves the Follow-Ups tab a ghost row, or the Sheet an orphaned tick or a calibration grade for a recording that's gone.
- **Column S of the Grading Log holds all 3 raw runs concatenated**, separated by `----- next run -----` — not one clean JSON object. `index.html` has a `parseAiResultFromRow()` function that handles both that format and the older single-JSON format. Optional future cleanup: have `Code.gs` also write one reconciled JSON object so the front end doesn't need to rebuild it.
- **Because of that, reconciliation logic is deliberately duplicated in two places** — and has to stay in sync. A freshly graded session gets its reconciled sections from `Code.gs`; the same session re-opened from Recent Submissions gets them rebuilt in `index.html` from those raw runs. `parseClipSeconds` / `formatClipTime` / `mergeClips` / `firstJsonObject` and the `SCORE_SPREAD_FLAG_THRESHOLD` constant exist in both files. Change one, change the other, or a session will look different depending on how it was opened. (This bit before: the front end's rebuild used to collapse every non-unanimous section into a plain "insufficient evidence", so the `†` and Double-check flags silently vanished from any session re-opened from the log. Fixed in V10.)
  - **It bit again on 2026-08-14, and V14 is what caused it** — worth reading before trusting "backend-only" as a category. V14 taught `Code.gs` to salvage a pass where Gemini emitted a complete object and then kept writing, and its own header concluded "front end unchanged; index.html needs no edit for any of this." **The opposite was true: V14 created the need.** `_rawResponseText` keeps the **full** text of the salvaged pass, rambling included, and that is what gets written into column S — so before V14 the sheet only ever held clean runs (the bad one was discarded), and from V14 on it can hold one `JSON.parse` rejects. `parseAiResultFromRow` skipped that chunk, so **`Code.gs` reconciled from 3 runs and `index.html` from 2 on the same session.** Measured on a 8/8/4 spread: the section read **8 and flagged Double-check** when freshly graded, **8 and unflagged** when re-opened; on 8/5/5 it read **5** fresh and **7** re-opened — across the ≥6 every-section rule, so a card could show a section as passing that the grade had failed. The corrections fix-list also came from a different pass, and `run_scores` printed two numbers instead of three. `followUpReasons()` reads those same rebuilt sections, so the Follow-Ups queue's reasons drifted with them. Fixed by mirroring `firstJsonObject()` into `index.html` (front-end only, no redeploy). Two things not to undo:
    - **The salvage is per-chunk, inside the loop — never on the whole of column S.** `firstJsonObject()` applied to a multi-run row returns **run 1** and would be read as the entire result, silently throwing away consensus, the run scores and every flag. That's a worse bug than the one being fixed, and it's a one-line mistake to make. Verified: a clean 3-run row still reconciles as 3 (`9 · 6 · 3` → median 6, flagged).
    - **A genuinely truncated run still drops out.** `firstJsonObject()` returns `''` rather than salvaging a half-object, so real truncation still fails loudly instead of producing a confident-looking score from a fragment.
    - Side benefit, verified: a legacy **single-object** row with trailing text used to return `null` and lose the whole session's section detail. It now recovers, because a row with no separator is simply one chunk.
    - The cleaner long-term fix is for `Code.gs` to store the **salvaged** text in `_rawResponseText` so the sheet never carries a malformed run — but that needs a redeploy, and the mirror has to stay regardless for rows already written.
    - Verified in a real browser against the actual edited file (local static server; the Browser pane wasn't compositing, so no screenshot): the drift closed to byte-identical output, the per-chunk trap, truncation, legacy rows, unparseable junk, plus a full result card rendering from a malformed row with no horizontal overflow and a clean console.
- **Gemini quota and cost — this caused a confusing failure once.** The API key was on Gemini's **free tier: 20 `generate_content` requests per day** for `gemini-3.5-flash`. Consensus grading spends **3 per session**, so that's ~6 sessions a day, and a day of testing exhausts it. When it runs out, the page shows **"AI grading request failed: Failed to fetch"**, which looks like a network or CORS problem and is not — the real error (HTTP 429) is only visible in **Apps Script → Executions**. Always check that log before theorising. Cost on the paid tier: audio bills at 32 tokens/second, so a 60-minute session is ~115k tokens per pass and ~345k across 3; at $1.50/1M in and $9.00/1M out that's **roughly $0.63 a session**. Fixed by enabling billing on the API key's Google Cloud project (AI Studio → API keys → the project → Set up Billing). **Except it evidently wasn't, or didn't stick — the V13 test run on 2026-08-13 hit the daily cap again** ("Only 2 of 3 AI passes completed - Gemini daily quota is used up" in the reviewer notes). Confirm billing is actually active on the project that owns the key before booking any further testing; a half-completed consensus grade is a weaker result than it looks.
  - **It happened a THIRD time on 2026-08-14, and this time the execution log settled what "didn't stick" means.** Jordan enabled billing, the 429 came back anyway, and the full response named the metric outright: `generativelanguage.googleapis.com/generate_content_free_tier_requests`, `quotaId: "GenerateRequestsPerDayPerProjectPerModel-FreeTier"`, `limit: 20`. **That string is the whole diagnosis, and the word that matters is `PerProject`.** Billing was on — just not on the Cloud project that owns this API key. AI Studio auto-creates projects with names like `gen-lang-client-0483927104`, so billing very easily lands on the wrong one and everything looks done. **Before theorising about anything else, read the quotaId: if it still says `-FreeTier`, the key's project is unbilled, full stop.**
  - **A free-tier project that is over its daily cap also returns 503s, not just 429s.** The same session came back with all three passes reporting `Gemini API error (503)` and no quota message at all, which reads as "Google is down" and is not — it's load-shedding on an exhausted free tier. **Don't chase a 503 as a separate problem until the quotaId has been checked.** V15+ retries 503s three times with backoff, so a persistent one across all three passes is a signal in itself.
  - **BILLING IS SETTLED AS OF 2026-08-14 — stop re-diagnosing it.** Jordan's AI Studio API Keys page shows the one key (`...Pxkg`, created 2026-08-11) belongs to project **`deco-training-tracker`** ("DECO Training Tracker") on **Tier 1 · Prepay**, not free tier. The `PerProject` trap above is therefore closed: this key's own project is the billed one. If a 429 with `-FreeTier` in the quotaId ever appears again, something changed — it isn't the old unresolved problem.
  - **THERE IS A THIRD CAUSE OF A 503, AND IT IS GENUINELY GOOGLE'S — model overload (2026-08-14).** Every theory in the two bullets above is about something *we* did wrong, and on 2026-08-14 that framing cost most of an evening: 4h33m grading runs failed with 503 on all three passes, then a 2:22 clip failed the same way, and the size/quota/billing hypotheses were all wrong. **A one-line text probe settled it in one second** — no audio, no size, nothing of ours — returning `{"code":503,"status":"UNAVAILABLE","message":"This model is currently experiencing high demand..."}`. How to tell the three apart, and this is the whole diagnostic:
    - **`UNAVAILABLE` with NO `quotaId` → Google's problem.** Wait; it clears on its own. Nothing to fix.
    - **A `quotaId` → ours.** Read whether it says `-FreeTier` (wrong project billed) or a paid daily cap (wait for midnight Pacific).
    - **404 `NOT_FOUND` → the model name is wrong.** A model that doesn't exist never returns 503, so a 503 is positive evidence the model *is* real and reachable.
    - **Timing was a real signal and was noticed too late:** the day's one success was 5:27am and every failure was early evening. **Peak-hours demand is a live risk for a 4.5-hour recording**, and off-peak is the better slot for a long run regardless of anything else.
    - **A SMALL PROBE PASSING DOES NOT MEAN A LARGE REQUEST WILL GO THROUGH — measured 2026-08-14, 7:17pm Pacific.** This is the one correction to the diagnostic directly above, and it matters because the obvious reading of "run the one-line probe first" is that a green probe clears the model. It doesn't. Under load Google sheds *expensive* requests first and keeps serving cheap ones, so the two answers can disagree at the same moment: `decoDiagnoseGemini()` (7 input tokens, text) returned **200** at 7:09pm, and eight minutes later a request carrying the 4h33m recording with `maxOutputTokens: 512` returned **503 `UNAVAILABLE`, "This model is currently experiencing high demand"** — same key, same model, same minute-scale window. **The size of the request is itself what gets refused.** So: a green `decoDiagnoseGemini` rules out a dead key, a wrong model name and a quota cap, and rules out nothing at all about a long recording. `decoProbeLongAudio()` is what actually answers that question.
    - **`decoProbeLongAudio()` (added to `Code.gs` 2026-08-14) is the long-recording version of that probe**, and it exists because the cheap one is not decisive. It reuses whatever is already in the Gemini Files API — `prepareAudio` puts the recording there and it's kept 48h — so there is **no 131 MB re-upload**; it runs `countTokens` first (free); then fires the smallest possible request against the real file, a few tokens of output, so a failure can't be blamed on how much was asked for. **It costs ~$0.80**, because any question about a 4.5-hour recording ingests the whole thing. Read it as: **200 → the file is fine and the transcription request specifically is what needs tuning; 503 → the request is too big for the capacity available right now.**
    - **The 4h33m file measures 526,773 audio tokens** (`countTokens`, 2026-08-14) — within `gemini-2.5-flash`'s 1,048,576 ceiling with room to spare, and matching the 32-tokens-per-second arithmetic used throughout this file. **Size limits are NOT what blocks this recording.** Don't re-derive this; it's measured.
    - **`decoWatchGemini()` is the WRONG tool for a blocked long recording, and this is a trap.** It probes with a *small* request, which is exactly the thing that keeps succeeding while the big one fails — so it would email "all clear" while the recording still can't be graded. Re-pointing it at `decoProbeLongAudio()` would cost ~$0.80 per check (~$4.80/hour on its 10-minute trigger), which is why it wasn't done. **Retry off-peak instead of watching.**
  - **There are now diagnostic functions in the LIVE Apps Script, and they are NOT in this repo's `Code.gs`** (added 2026-08-14 by pasting into the editor — saving a function does **not** touch the deployed Web App, so this needed no New Version and changed nothing that was running). Fold them into the next `Code.gs` edit rather than assuming the file matches production:
    - **`decoDiagnoseGemini()`** — logs the model in use, calls ListModels, then makes the smallest possible `generateContent` call ("Reply with the single word OK"). **This is the thing to run FIRST on any Gemini failure.** Every previous 503 in this project cost a failed grading run to diagnose; this costs a fraction of a cent and one second. ListModels is in there deliberately: it proves the key authenticates *and* shows which models the key may actually use.
    - **`decoWatchGemini()` / `decoStartWatching()`** — a 10-minute time-based trigger that emails `Session.getActiveUser().getEmail()` the moment the model answers normally, then **deletes its own trigger** so one outage produces one email. Built because the alternative was Jordan sitting there re-running a probe by hand. **`decoStartWatching` has to be RUN once** — pasting and saving arms nothing, and the failure is silent (you wait forever for an email that was never scheduled), so verify a row exists under the Triggers panel.
    - Known-good fallback if a long outage blocks a run: **`gemini-2.5-flash`** is on this key's model list and fits both routes (1,048,576 input tokens vs ~524k for the 4h33m file; 65,536 output, exactly what V16's transcription asks for). It's a Script Property change with **no redeploy**. Grades from it aren't comparable to `gemini-3.5-flash` ones for calibration purposes — column R records which model ran, so they stay tellable apart.
  - **The fastest fix is usually NOT to untangle which project got billed.** Create a new API key inside a project that already has billing, then paste it into Script Properties (`GEMINI_API_KEY`, Apps Script → Project Settings). It's read live — **no redeploy needed.** **But don't read every degraded grade as a quota problem** — the 2026-08-14 Team On-Site Day run also came back "Only 2 of 3 AI passes completed" and had nothing to do with quota; it was the JSON-parse bug below. Read the reason in the reviewer note before concluding anything about billing, and confirm it in Executions.
- **Testing costs real money now.** Use a short clip (2–5 minutes) for anything that only exercises plumbing — whether timestamps render, whether the card parses, whether a deploy took. A 5-minute clip is ~1/12 the cost of an hour-long one and tests the mechanism just as well. Save full-length sessions for actual calibration work, where the grade itself is the point.
- **` | ` is a structural separator, not punctuation.** `notes_for_human_reviewer` is one long string that `index.html` splits on ` | ` to render bullets. Anything appended to it — including error text from a failed pass — must not contain ` | ` internally, or one note shatters into several nonsense bullets. This bit V11 on its first live run, when raw Google error JSON was appended after a ` | raw: ` marker. **It bit again on 2026-08-14, in a path V12's fix didn't cover** — so as of V14 the guard is at the join, not at the message sites: `safeNoteText()` strips ` | ` from anything spliced into the note. Don't remove it on the grounds that the messages are clean now; that was true after V12 as well.
- **Raw API responses belong in Apps Script → Executions, never on the results card.** `callGemini` logs the full failure response there; the message that reaches a manager is a plain sentence. When debugging, that log is the source of truth. **V12 fixed this only for `geminiErrorMessage()` (the HTTP-error path) and left the two JSON-parse throws still appending ` | raw: ` + 500 characters** — which is exactly what landed on Jordan's Team On-Site Day card on 2026-08-14, complete with the note shattering into an extra bullet. All three throw sites in `callGemini` were cleaned in V14. **The lesson is the one above: fixing message text one site at a time has now failed twice.**
- **Gemini can return a complete grading object and then keep writing (V14, 2026-08-14).** A pass came back as `Unexpected non-whitespace character after JSON at position 3721 (line 77 column 1)` — a valid object, then more content starting on a fresh line (a second copy of it, or trailing prose). `JSON.parse` rejects the whole string, so a pass that had actually **succeeded** was thrown away and the grade degraded to 2 of 3. `firstJsonObject()` now takes the first balanced object and ignores the rest. Two things worth keeping: it **counts braces with string-awareness**, because a `{` inside a justification or an escaped `\"` would otherwise truncate the object mid-way; and it **returns `''` for genuinely truncated JSON** rather than salvaging a half-object, so a real truncation still fails loudly. This matters more than "one lost pass" sounds — **a 2-pass grade flags every section for human review (V11) and a 3-pass one doesn't.**
- **One bad transcription pass used to end the whole session, and the message explaining it CRASHED (both fixed 2026-08-15).** Jordan hit this trying to transcribe the 4h33m Training Day file: `The AI stopped part-way through writing out this recording and could not tell where it got to.` Two separate defects, front-end only, no `Code.gs` change and no redeploy:
  - **A pass that stopped with no usable resume point got ZERO retries, while a pass that stopped WITH one got three.** That asymmetry was backwards. `TRANSCRIPT_MAX_ATTEMPTS_PER_WINDOW` (3) exists precisely so a window can be re-tried, and the unresumable case — the more likely one to be a bad pass rather than a real length problem — was the one branch that threw on first occurrence, ending a session whose recording had already been read and paid for several times over. It now retries the same window inside that existing budget, and the cap message is what eventually gives up.
    - **The duplication that costs is bounded by what "cannot resume" MEANS, which is why this is safe.** The backend only says it when the furthest timestamp written is under `TRANSCRIPT_MIN_RESUME_ADVANCE_SECONDS` (30s) past where the pass began — so the pass produced at most half a minute of transcript, and `joinTranscriptChunks` drops repeated opening lines anyway. The other shape is a pass with **no timestamps at all**, which is a preamble or a refusal rather than session content. **Don't generalise this into retrying a pass that wrote real content**; that would append a duplicate stretch that nothing downstream could detect.
    - **`fromSeconds` is deliberately NOT moved on a retry.** There is no trustworthy point to move it to, and guessing one forward would punch a hole in the session — the same asymmetry that governs the resume point everywhere else (early beats late).
  - **The give-up message threw `windows is not defined`.** V17 replaced the pre-computed window array with the cursor-based loop (the plan changes mid-run when a pass runs hot) and this one message was left reading `windows.length` / `windows[w].from`. So the single place whose entire job is to explain a failure clearly **crashed instead**, and it had presumably never fired since V17. Now `plannedWindows.length` / `cursor`. **Worth a general lesson: an error path that is never exercised is not tested, and a `ReferenceError` in one reviews as fine.**
  - **Verified in a real browser against a stubbed endpoint** (the local `_serve.ps1` static server; no screenshots needed — this is all control flow). Checked: an unresumable pass followed by a good one now completes the session and reaches the grade call, retrying at the *same* `fromSeconds`; three unresumable passes in a row produce the intended sentence naming the stretch (`the part starting at 0:00`) with no `ReferenceError` anywhere; and an ordinary resumable truncation still advances to `resumeFromSeconds` exactly as before.
  - **What this does NOT fix: the underlying reason a long recording keeps stopping short.** It buys retries, not capacity. See the V17.2 item for the window-sizing arithmetic, and note that for a file this length **grading from audio is now both cheaper and the shorter route** (~$1.90 against ~$4.27, and one Gemini read of this file measured 59 seconds) — the medium picker's "listen" option is the way through when transcription keeps stalling.
- **Drive reports a just-uploaded recording as 0 bytes for a moment, and that silently broke the first real long session (found and fixed 2026-08-14).** Jordan uploaded the 4h33m file against a fully-deployed V15 and got `AI grading failed: This recording is 131 MB, over the 14 MB limit … Call action:"prepareAudio" first`. Everything looked deployed because everything *was* — the endpoint reported V15, the served front end had `prepareAudio`, the upload succeeded. The cause is the upload shape in `index.html`: `driveCreateFileMetadata` creates an **empty** placeholder and `driveUploadContent` PATCHes the bytes in as a second request, so there is a window where Drive still answers **0 bytes**. `prepareAudioForGrading` picks inline-vs-Files-API purely from `DriveApp.getFileById(id).getSize()`, read 0, returned `mode:'inline'` with no URI — and the grade call, moments later, read the true 131 MB and refused it. Points worth not re-litigating:
  - **Nothing can tell a not-yet-settled 131 MB file from a genuinely short clip by asking Drive alone**, so the page compares against the size it actually uploaded (`audioBytes`, which already existed for the progress estimate). `sizeLooksUnsettled()` is that comparison and is deliberately **conservative** — no `audioBytes`, a non-inline reply, or a pre-V15 backend all report nothing wrong, so it can never invent a failure.
  - **It RETRIES rather than failing** (`PREPARE_AUDIO_ATTEMPTS`, 5 × 5s). A `prepareAudio` that answers `inline` is a size check with nothing uploaded and **no tokens spent**, so asking again is genuinely free — the only cost is the wait, against an alternative that cannot succeed. It gives up with a sentence saying the recording is safe and the grade row saved, and **never reaches the grade call**, so a stuck Drive costs no Gemini spend.
  - **`driveWaitForSize()` closes the window at the source** but deliberately **returns rather than throws** on a timeout: the grade row is already saved by then and a slow Drive must never be reported as a failed submission. The guard above is what actually stops a bad grade — this only has to make it rare.
  - **`INLINE_AUDIO_LIMIT_BYTES` in `index.html` mirrors `GEMINI_INLINE_MAX_AUDIO_BYTES` in `Code.gs`** — one more duplicated constant on the pile (see `mergeClips` et al). A stale copy makes the guard fire early or late; it does **not** change what gets graded, since the backend still enforces the real limit.
  - Front-end only. **No `Code.gs` change, no redeploy.**
  - **Verified in a real browser against the edited file with a stubbed endpoint** (no screenshot — the Browser pane still wasn't compositing). Checked: the seven-case classification table; the bug recovering on retry with the grade call carrying both `geminiFileUri` **and** `audioFileId` (the column-G trap); a 1.4 MB clip completely unchanged at **one** prepare call, empty URI, zero added latency; a long file that prepares first time also at one call; the give-up path at 5 calls / 24s with no grade call issued; the pre-V15 fallback still short-circuiting at one call instead of retrying five times; and a real `prepareAudio` error surfacing as itself rather than being retried away.
- **Apps Script Web App access must be set to "Anyone", not "Anyone within decorepair.com".** The domain-restricted setting breaks CORS when called via `fetch()` from GitHub Pages, even though the endpoint works fine when opened directly in a browser. This cost real debugging time once — don't change it back.
- **Sign-in can't be made permanent.** This is a client-only Google auth flow; tokens last ~1 hour and are silently renewed in the background, but that renewal depends on the browser's own Google session. Truly indefinite sign-in would need a backend holding a refresh token. Don't promise more than that.
- **Being thrown back to the sign-in screen mid-session — rebuilt 2026-08-14.** Jordan: *"This here will randomly get me to sign back in every so often on the same work time (I would not have gotten off the page or closed my laptop)."* The renewal was a **single `setTimeout` roughly an hour out, with one attempt and no second chance** — so any one of four everyday things ended the session, and the next click on anything (a Refresh, a tab switch, a delete) was what discovered it. Decisions worth not re-litigating:
  - **A failed renewal now re-arms itself.** This was the whole bug: `scheduleTokenRefresh` was only ever called *from a successful token callback*, so a renewal that failed scheduled nothing, the token quietly ran out a couple of minutes later, and the session was over. Failures now back off 15s → 30s → 45s → 60s and keep trying. Everything that renews goes through `renewToken()`, which is the one place that re-arms; **don't add a renewal path that calls `requestSilentToken()` directly.**
  - **The wall clock is the authority, not the timer.** `checkTokenHealth()` runs every 30s and also on `visibilitychange` / `focus` / `pageshow` / `online`, and compares `tokenLifeLeftMs()` against a 5-minute lead. A single hour-long `setTimeout` is not something to stake a session on — a background tab has its timers throttled and a sleeping machine doesn't run them at all, so the one renewal that mattered fires *after* the token has already expired. The wake events are the part that covers a laptop lid or a tab left in the background.
  - **The renewal lead went 2 min → 5 min** so a retry has room to succeed before anything the user does can hit an expired token.
  - **`ensureFreshToken()` gets two goes, not one**, 1.2s apart. One silent attempt loses to a network blip or a browser that was asleep a second ago, and losing it used to cost the whole session.
  - **The silent-request safety timeout went 6s → 12s, and `error_callback` is now registered.** Google reports popup-blocked / iframe-closed / network failures *there*, not through `callback` — without it the only thing that ever noticed was the timeout, so every such failure burned the full wait and then looked identical to "this account has no access."
  - **Offline is not signed out.** `navigator.onLine === false` holds the session and the app stays on screen ("Offline — will reconnect on its own", which deliberately does **not** start with "Signed in" so the status dot stays un-green per the rule above); the `online` event renews with no click. Genuinely revoked access still calls `forceSignedOut` exactly as before — that path was checked, not assumed.
  - **The de-dupe gate has two escape hatches, and it needs both.** `silentRenewInFlight` stops the watchdog, the timer, a focus event and an API call from firing four overlapping requests at Google. But **a stuck gate is far worse than no gate** — every later renewal short-circuits onto a promise that will never settle and the session dies with nothing able to revive it. This was hit for real while testing (a tab frozen by the browser mid-request left it standing). So: every resolution path goes through `settleSilentRenew()`, which drops the gate and resolves the waiting promise *together*, and the gate additionally expires on the wall clock after `SILENT_TIMEOUT_MS * 2`. **Don't collapse those two back into one.**
  - **`saveToken` no longer throws when storage is blocked.** It used to throw *inside* Google's callback, one line before the renewal was scheduled — so on a private window or with site data blocked, no renewal was ever armed and the session was guaranteed to end in an hour. There's now an in-memory mirror, so losing storage costs only the across-refresh convenience.
  - **A background renewal that succeeds must not re-run the data loads.** `signedInUi` gates it. Without that, a token arriving after its own safety timeout gave up on it would re-run `loadRoster`/`loadFollowUps`/`loadRecentLog` underneath someone mid-task.
  - Verified in a real browser with a stubbed Google client driving the actual `onGsiLoad` config — 20-odd assertions across: sign-in with storage unavailable, backoff on five consecutive failures, the watchdog quiet at 10 min and renewing at 2 min, three simultaneous wake-ups making one request, wake-from-hidden, expired-token-plus-one-failure recovering silently, offline holding the session and recovering on reconnect, revoked access still locking the app, and the watchdog staying quiet behind the sign-in screen. Front-end only. **No `Code.gs` change, no redeploy.**
  - **What this does not fix:** if the browser's own Google session ends, or third-party cookies are blocked hard enough that the silent flow can't run at all, sign-in is still required. That's the client-only limitation above, and it hasn't moved.
  - **IT KEPT HAPPENING, AND THE 2026-08-14 REBUILD ABOVE COULD NEVER HAVE FIXED IT — the real cause was MULTIPLE GOOGLE ACCOUNTS (found and fixed 2026-08-14, later the same day).** Jordan sent a screenshot of Google's **"Choose an account"** chooser interrupting him — *"This comes up frequently even if I have not stopped working on it"* — listing three signed-in accounts: his own, `recruit@decorepair.com` and `operations@decorepair.com`. Everything in the rebuild above is about a renewal **failing**; this renewal was **succeeding**, and asking a question on the way. Decisions worth not re-litigating:
    - **A renewal with no `login_hint` cannot be silent when more than one account is signed in.** `requestAccessToken({ prompt: '' })` asks Google not to show anything *unless it has to* — and it has to when nothing in the request says **which** account to renew. DECO staff routinely have three. So every ~55 minutes the chooser appeared, nothing had actually gone wrong, and nothing on screen said so; it read as being randomly signed out. **Confirmed against Google's own reference** (`login_hint`: *"when successful, account selection is skipped"*) **and end-to-end in a browser** — the real Google library now builds `…&login_hint=jordan.evans%40decorepair.com&…` where before that parameter was simply absent.
    - **The address comes from Drive's `about` endpoint, so NO new OAuth scope and no fresh consent screen for anybody.** `drive.file` — which this app already holds — is one of the scopes `about.get` accepts. Adding `userinfo.email` would have worked too and would have re-prompted all 18 people on the roster for consent. Don't "simplify" it to a scope.
    - **Renewals are hinted; the Sign in button deliberately is NOT.** Pressing Sign in is a considered click, and hinting it would quietly deny somebody the chance to come back as one of their other accounts. Only renewals must never interrupt. For the same reason the remembered address is dropped **on an explicit sign-out and only there** — `forceSignedOut` keeps it, because a session expiring is not a request to change account.
    - **Everything about the hint is best-effort.** A failed lookup, blocked storage, or a first-ever sign-in leaves it unknown, and an unknown hint is simply omitted — which is exactly the old behaviour. It can make renewals smoother; it must never be able to cost a session.
    - **THE WATCHDOG WAS ALSO MULTIPLYING THE POPUPS, and that was a second real bug.** `checkTokenHealth` runs every 30 seconds and re-fires a renewal whenever the token is inside its 5-minute lead — it respected `silentRenewInFlight`, but **not** the backoff that `renewToken` had just scheduled. So once the chooser went up and the 12-second safety timeout dropped the gate, the watchdog opened another, and another: up to **ten** in one lead window. Both routes into a renewal now read one shared clock, `nextRenewAllowedAt`. **Measured, and it is load-bearing rather than theoretical: 12 watchdog ticks after a timed-out renewal produce 1 request with the gate and 2 without it.**
    - **A renewal that times out with Google not having answered now waits 90s, not 15s** (`RENEW_ON_SCREEN_BACKOFF_MS`). Silence most likely means a window of Google's own is open and waiting on the user, and the old backoff stacked a second one on top of it. Still comfortably inside the 5-minute lead, so nothing is at risk while it waits.
    - **Found by the verification, not by the report, and pre-existing: `onGsiLoad is not defined`.** The `<script>` that loads Google's client calls `onGsiLoad()` from its own `onload`, which was fine only for as long as fetching that script over the network took longer than parsing this ~520 KB page. **Once it is CACHED it can land first**, the call throws, `onload` never fires again, and the token client is never built — the app sits on the sign-in screen with a permanently disabled button, which looks exactly like the missing-OAuth-origin failure recorded below. **Seen for real in the test browser on a repeat visit.** Fixed at both ends: the attribute is guarded (`window.onGsiLoad && onGsiLoad()`), and a catch-up check at the very end of the main script runs it if Google got there first. `onGsiLoad` is idempotent (`gsiInitDone`) so whichever route wins, the other is a no-op. **This also had to be fixed for the hint to work at all** — `rememberedAccount` is loaded inside `onGsiLoad`.
    - Front-end only. **No `Code.gs` change, no redeploy.**
    - **Verified in a real browser against the actual edited file, 20/20 assertions** (a stubbed Google client recording exactly what was sent, plus a stubbed Drive `about`): the first-ever request omitting the hint, every later renewal carrying it, the address learned once and not re-fetched, the Sign in button staying un-hinted, expiry keeping the address where sign-out drops it, the watchdog adding nothing mid-flight, the long backoff, the no-second-popup guard and its load-bearing check, recovery afterwards, the catch-up init path, and `onGsiLoad` being idempotent. Then the real `index.html` against the **real** Google library, confirming `login_hint` in the URL Google actually builds. **No screenshots — the Browser pane was not compositing frames again.**
    - **One harness mistake worth recording, because it produced four false failures that looked like product bugs:** clearing `localStorage` is **not** enough to fake a first-ever visit. `loadStoredToken()` falls back to `tokenMemoryFallback`, the in-memory mirror that exists for blocked-storage cases, so the next init restored a session instead of starting one. Use `clearStoredToken()`.
- The progress bar during AI grading is an **estimate**, not real server progress — Apps Script is one blocking call with no progress feed. The UI says so explicitly. Keep it honest.
- OAuth client ("Training Tracker Web") is **User type: Internal**, so any @decorepair.com account can sign in with no verification screen. Authorized origins include both `https://deco-training-evaluator.github.io` (current) and `https://decomancer128.github.io` (the original personal-account URL). Adding a new domain means adding it here too, or sign-in silently breaks. **The rename has now happened, so `https://development-dashboard.github.io` must be in this list today** — a missing origin doesn't error usefully, the sign-in button just stops doing anything. Whether it was added before the rename went through is unconfirmed; if anyone can't sign in, check this first, and keep the old origins listed for now rather than tidying them out.

## Where things stand

**THE ROADMAP BELOW IS SUPERSEDED — see "The final roadmap (2026-08-14)" directly beneath it.** It is kept because the app still carries its labels (the preview tabs say "Phase 3") and because the *sequence of re-cuts* is itself a slide in the leadership deck.

Roadmap re-cut by Jordan on 2026-08-13 for the leadership PowerPoint, then re-cut again the same day into the version below. The shape changed in two ways: *breadth before authority* (open the tool up to every session type first, then earn the right to decide), and *the AI advises from Phase 2 onward* rather than waiting for a separate "recommendation" phase. Every phase is gated on the one before it actually working, not on a date.

**Phase 1 — done.** Site live. Upload an audio recording and get a reading back from the AI, end to end.

**Phase 2 — in progress (current). Get Training Day dialled in — as a recommendation.** Take real Training Day recordings, grade them with the AI, and put that side by side with the district manager's actual human grade from the "Training Evaluations" Smartsheet. Refine the rubric and prompt until the two line up. Findings go in the "DECO AI Grading — Calibration Decisions Log (Phase 2)" Sheet (`1o4VvjAgdQ6VNPNt4-kEiu_nH5GrFh4LHe4jVo5TRNzc`) — one row per finding, not raw scores. The UI work is already done here: full visual rebuild from DECO's brand book (exact palette `#ef7f23` / `#eeaf66` / `#f0e6d3` / `#a69a83`, real logo, Sora + Caviar Dreams fonts), click-to-expand results in Recent Submissions, readable section cards, a two-click delete, dashboard table fitting one view, numeric manual grade on the rubric's own 110 scale, a dedicated Certified column, the tab renamed "Training" with Team On-Site Day removed (superseded 2026-08-13 — it is now "Training Day", one of six session-type tabs; see Repo layout), and an honest estimated-progress indicator. On 2026-08-13 Jordan reviewed live output and asked for shorter, more digestible written feedback: the V9 prompt caps justifications at one sentence and strong/weak/focus entries at "label: one clause" (2–4 entries per list) while still requiring every covered/missed item to appear, and the results card bolds those labels, one-lines the footnotes, and breaks reviewer notes into bullets. Also on 2026-08-13, **evidence timestamps (V10)**: the card was flagging sections as doubtful without saying where to check them, so the flags went unchecked — now each section carries the AI's own "Listen at" jump points, a flagged section also shows what all 3 passes scored it (`9 · 6 · 3` reads very differently from `8 · 8 · 5`), and the recording plays in the page instead of only linking out to Drive. This is the piece that makes calibration tractable: when the AI and the Smartsheet disagree, you can hear what the AI heard instead of guessing why. **The output is explicitly a recommendation, not a verdict** — the district manager doing the grading uses it to inform their call, they don't defer to it. Every phase up to 5 keeps that framing; don't let the UI drift into sounding authoritative.

**Phase 3 — Open it up to the other avenues.** Same upload-audio-get-feedback loop, extended past Training Day to: **Team On-Site Day**, **Conversion Call**, **Reference Checks**, **Stage 1 Interview**, **Stage 2 Interview**. Still feedback, still not deciding. Two structural notes:
- **Reference Checks is one tab, not two — AND IT IS NOW LIVE (2026-08-15, `Code.gs` V19).** The candidate reference check and the *parent* reference check share a tab, with a picker inside it for which one you're grading (Jordan's call, 2026-08-13 — a second tab would be clutter). They are two separate rubrics with two separate `sessionType` strings; the `variants` array on that `SESSION_TABS` entry holds them, and it is the only tab in the app that covers two session types. Full entry under Repo layout.
- **Team On-Site Day is already wired** — `Code.gs` has always carried its rubric (50 points, 5 sections) and it has a live tab, so a recording can be graded today. What it hasn't had is calibration against the real human grades in the "Site Visit Evaluations" Smartsheet. Treat any Team On-Site Day score as uncalibrated until that happens.
- ~~**Only STAGE 1 INTERVIEW is still a preview tab.**~~ **SUPERSEDED — Stage 1 went live in `Code.gs` V22 on 2026-08-15 and THERE ARE NO PREVIEW TABS LEFT.** Every session type in the app now grades. `renderPreviewTabs()` and `buildMockGradingForm()` still exist and now iterate an empty list — harmless, and worth keeping until it's clear no new session type is coming.
- ~~**The recruiting side is explicitly POST-PROJECT (Jordan, 2026-08-13).**~~ **SUPERSEDED, AND COMPLETELY — Jordan reversed this himself over 2026-08-14/15 by handing over the rubrics one at a time and asking for each to be wired up.** Conversion Call went live first (V18), then both reference checks (V19), Stage 2 Interview (V20), the Networking Activity (V21) and finally Stage 1 Interview (V22). His framing on 2026-08-15: *"I am going to give you all the rubriks for the remaining stuff that needs to be activated."* **ALL of the recruiting-side session types are now live — the reversal is complete and there is nothing left of this note to act on.** The old note is kept only so nobody re-derives it from an older paragraph and stops work that Jordan explicitly asked for. What still stands from it: a tab must not be flipped to `'live'` without its rubric being loaded into `Code.gs` and redeployed first.
- **SUPERSEDED SAME DAY — the networking activity WENT LIVE (Jordan, 2026-08-15, `Code.gs` V21).** He reversed the note below within hours of making it and deployed the networking rubric, so **every tab in the app now grades**: Training Day, Team On-Site Day and the full recruiting funnel including networking. The subjectivity caution below is still worth reading before trusting a networking score, but it is no longer a reason the tab is absent. **Practical ceiling as of this date: audio over roughly 1.5 hours does not complete**, so a full Training Day recording still cannot be graded end to end.
- ~~Two more session types exist and are deliberately NOT going in the app: the networking conversion call, and the networking activity itself (Jordan, 2026-08-15).~~ His reasoning at the time: they are **more subjective** than the session types already loaded, though still gradable. The plan was to **track them on the Dashboard later** rather than grade them now. Two things follow. First, don't add them to `SESSION_TABS` — even as preview tabs — without asking; the preview tabs currently on screen all represent types with a real rubric waiting, and these don't. Second, if they ever are built, **the subjectivity is the design problem, not the plumbing** — a rubric whose criteria a human would score inconsistently cannot be calibrated against a human, which is the whole method this project runs on. Establish the human baseline first.

**Phase 4 — Continuous feedback, coaching, and fine-tuning.** The tool becomes something the team uses constantly rather than at certification moments: ongoing feedback and coaching across all those session types, with the volume of real examples that generates feeding back into fine-tuning the grading. This is where accuracy gets earned at scale — Phase 5 isn't reachable without it. **The Follow-Ups tab (built 2026-08-13) is the first piece of this** — it's what turns one-off gradings into a queue that gets worked, rather than findings that evaporate once the results card is closed.

**Phase 5 — The AI starts making the decisions.** Once it's fine-tuned, it makes the actual call: certifying someone on a Training Day, and the equivalent decision on the other session types. Used for both certifications and continuous development. **This is the only phase where the AI decides anything** — everything before it is advisory, and that gate is the point of the whole roadmap.

**Bonus phase (may not happen) — grade the other side of the conversation.** So far everything grades the person *running* the session. This would grade the person on the receiving end: the **trainee** (needs-a-Day-3 / cut / ready-for-the-field), the **interviewee**, and the **employee being evaluated on a Team On-Site Day**. **Recommendation only — never the decision, at any level of accuracy**, unlike Phase 5. Rubrics still need writing, and Jordan hasn't committed to building it.

**Final phase — company-wide.** Expand across DECO, beyond BC.

## The final roadmap (2026-08-14) — THIS IS THE CURRENT ONE

Jordan re-cut the phases a fourth time, and this one is different in kind rather than in detail: **it is ordered by SIZE OF RECORDING, smallest first.** Every earlier version started with Training Day because it's the most valuable session type; this one starts with the Conversion Call because it's the *shortest*, and works up to Training Day last. The reasoning, in Jordan's words: *"I should have started with the smaller recordings first."*

Why it matters for anyone working on this: **the old roadmaps put the hardest engineering problem in Phase 1 and the actual question in Phase 2.** A 4h33m Training Day recording forced the Files API upload, transcribe-first grading, and bounded transcription windows (V15–V17.2) — none of which had anything to do with whether the AI grades like a district manager. A 5–15 minute conversion call answers the grading question with none of that machinery. The work isn't wasted (it's built, and it permanently unlocks every length), it just arrived before the thing it was supporting.

**Phase 1 — done.** A live website where you can upload an audio recording and get some kind of feedback back.

**Phase 2 — Smallest activity first: the Conversion Call (5–15 min). Grade it CONSISTENTLY.** Proof of concept on the shortest recording DECO has. The bar here is *repeatability*, not correctness — the same call graded twice should produce the same result. **This split is Jordan's sharpest call and it mirrors what actually happened technically:** consensus grading (3 passes, reconciled, disagreements flagged) fixed consistency and had nothing to say about accuracy. An inconsistent grader cannot be calibrated, so this has to come first.

**Phase 3 — Grade it ACCURATELY.** Consistently produce the grade *Jordan himself would have given*. This is the human-comparison work: AI grade beside the real human grade, refine, repeat. Only reachable once Phase 2 holds.

**Phase 4 — Work UP the size ladder.** Next biggest session type, then the next: reference check → interview → … → **Training Day last, because it's the biggest**. Each step repeats Phases 2 and 3 (consistent, then accurate) on a longer recording.

**Phase 5 — Grade the other side of the conversation.** Rate the *trainee*, the *candidate* in interviews, and the person on the receiving end of a conversion call. **Recommendation only — never the decision.**

**Final phase — launch across the company.**

**What is NOT in this cut, and don't re-add it silently:** the earlier roadmaps' explicit "the AI starts making the decisions" phase. Certification authority is not named as a phase here. The **advisory-only rule still stands** — an AI grade must never write to the Roster (see "Auto-certifying from a manual grade"), and every earlier note in this file that says "that's Phase 5" means *the old Phase 5*, i.e. "not yet, and not without a decision from Jordan." **Ask before building anything that lets the AI decide.**

## Open items

- **First real calibration run was planned for 2026-08-14 (Jordan) and DID NOT HAPPEN — it was blocked by recording length, not by anything about the grading.** Jordan brought the real file (`Matt Training August.13th.m4a`, 4h33m) and it could not have been graded: see the LONG RECORDINGS item under "Known quirks". V15 and V16 both shipped and **are deployed**; **V17 (bounded windows, 8-hour recordings) is written and is NOT.** So: every AI score in this app remains uncalibrated, and that is still the single biggest open question about the project. Sequence from here is (1) **deploy V17** — paste `Code.gs`, cut a New Version labelled V17, confirm the Web App URL answers `"version":"V17"`, then push `index.html`, backend first — (2) grade this recording **with the transcribe-first route**, which is the cheaper bet and the one that leaves the audio untouched if it fails, (3) compare against Matt's human grade in the "Training Evaluations" Smartsheet, (4) log *findings* — one row per thing learned, not raw scores — and note the outcome here.
  - **Under V17 the 4h33m file is 2 windows rather than 1, so watch for two things the shorter route can't show:** whether the join between them reads cleanly (`joinTranscriptChunks` should have dropped any repeated boundary line), and whether timestamps in the SECOND window are still anchored to the start of the whole recording rather than to the start of that window. The prompt says so explicitly and a test covers the instruction, but nothing has checked what the model actually does with it.
  - **While doing it, read the transcript.** It's a `.txt` in the audio folder and it's the first chance to see what the AI actually heard across 4.5 hours — whether speaker labelling holds up over that length, whether the delivery notes are useful or noise, and whether the "no speech" collapsing is throwing away anything that mattered. That's worth as much as the score comparison, and it can't be checked any other way.
  - **Grade one citation near the start and one near the end of this file while you're at it.** It's the first recording long enough to answer the timestamp-drift question below, and that answer is worth as much as the score comparison.
- **AI grading consistency is still not fully solved.** Consensus grading improved it but didn't eliminate drift. Treat any AI result near a pass/fail threshold as directional until real calibration data exists.
- Sections that are inherently visual (uniform, tent setup, tool organization, drill technique) can't be scored from audio alone. Expect `insufficient_evidence` there — that's a medium limitation, not a prompt bug.
- **Evidence timestamp accuracy is settled on a short clip and completely unmeasured on a long one.** Two hand-checks (2026-08-13 and 2026-08-14, detailed above) put the citations within 0–4s and 0–2s respectively — but **both were the same ~2½-minute file**, so nothing is known about a 60-minute session. The specific risk isn't a couple of seconds of slip; it's whether cited times stay anchored *at all* deep into a long recording. First full-length session should check one citation near the start and one near the end, using the verification method above. If they land in the wrong part of the session entirely, the honest response is to stop rendering a clickable time and show only the quote, so a manager searches for words instead of trusting a number that lies. `CLIP_PREROLL_SECONDS = 3` is correct for the short-clip data and shouldn't be re-tuned until there's long-recording data to tune against.
- **Corrections (V13) are deployed and confirmed working live — 2026-08-13.** Jordan deployed and graded a 2-minute test clip; the block rendered with 4 `must_fix` entries and, importantly, they were **genuinely distinct from focus areas** rather than the same list twice, which was the failure this prompt guards against. Caveat on what that run did *not* prove: every entry came back as an omission ("never came up", no timestamp), which is the correct answer for a clip where nothing was covered — so the clickable-timestamp path inside a correction was still unexercised.
  - **That gap is now closed — 2026-08-14, Team On-Site Day, the 2:44 clip.** The card came back with **both** correction shapes side by side: one with a real jump chip (`0:35` — "stated the discount ladder as 50-40-30-20 without mentioning the invoice minimum for the $20 offer") and one omission ("never came up" / **Missing**, for the last-resort retention ladder and premium upsell). So the timestamped path, the omission path, and the visual distinction between them all render live.
  - **Both corrections were also factually right**, checked against `docs/bc-sales-policy.md`: the $20 plan offer really is gated on an invoice ≥ $115 (line 23), the premium upsell really is +$50 on top of Basic (line 68), and the retention ladder really does start at $20 (lines 51–53). That's the first evidence that corrections are grounded in the reference data rather than plausible-sounding — worth re-checking on a session with subtler errors, since a discount ladder is one of the most explicitly stated numbers in the prompt.
- **Langley glass shop phone: (825) 962-7747** — given by Jordan 2026-08-13. Written in as a *soft* fact (a rep saying a different number is noted, not penalized) because 825 is an Alberta area code and BC numbers are 604/778/236 — possibly a call-routing number, possibly needs a re-check. Harden it once confirmed. Now in `docs/bc-shop-info.md`, `docs/DECO-AI-Grading-Setup-Guide.md`, `docs/gemini-grading-prompt.md`, and the `Code.gs` reference data (line ~481). **Live in grading as of a fresh V13 deploy on 2026-08-13** — the number was already in the saved Apps Script code, so the redeploy was a version cut only, no paste. The one open question left is the number itself, not its plumbing.
  - **The prompt doc was only half-updated on 2026-08-13, and it went unnoticed for a day.** `docs/gemini-grading-prompt.md` had the number in the section-11 rubric line and the open-questions list, but its **BC REFERENCE DATA block still read `Phone: [NOT YET PROVIDED — ... do not penalize ...]`** — the exact block that mirrors `Code.gs` line 481. Fixed 2026-08-13. Lesson for next time a policy number changes: the reference-data block and the rubric line are two separate places in that file, and grepping for the number finds the wrong one first.
  - **`docs/` and `apps-script/` are in `.gitignore`** (deliberately — the repo is public and serves the live site, so the prompt, rubrics, and pricing ladders stay local-only). That means **ripgrep/Grep silently skips them by default**, and a search for a rubric value will come back clean while the value is sitting right there in an untracked file. Search those two folders explicitly. It also means neither folder is version-controlled — an edit there has no history and no backup.
- **Transcribe-then-grade: UN-PARKED AND BUILT as V16 on 2026-08-14.** Jordan: *"I was told that the audio is gonna be too much to grade, and I need it to be transcribed first, and then the transcription ran through AI to save money. Is this possible? And if so, can you work on it?"* The scoping below (from 2026-08-13, when it was parked) is kept because most of its reasoning held up — but three of its conclusions were **wrong or have moved**, and those are what matter:
  - **"The real win would be near-free re-grading" was right, and it's the reason to do this.** Measured against Google's current published rates for the actual 4h33m file: **$2.45 graded from audio, $1.37 through a transcript, and $0.26 for every re-grade after that.** The first-grade saving (~44%) is real but modest; the re-grade being **1/9 the price** is what makes Phase 2 calibration affordable, since calibration *is* "grade this session again against a tweaked prompt".
  - **"It does NOT solve long recordings" is no longer true, and this was the reasoning error.** That note assumed transcription would be one very long call into the same 6-minute wall. It is one call — but it *replaces three*. Direct grading needs three passes that each ingest 524k audio tokens inside one execution; transcript grading is one audio call, then three passes over ~36k tokens of **text** that come back in seconds. The risky call goes from three to one, and the transcript is saved, so it never has to be risked twice.
  - **"Chunk it" is NOT available, and this is the constraint that shapes the design.** Gemini has **no way to bill part of an audio file** — `videoMetadata` start/end offsets are a *video* feature, and asking for a time range in the prompt still ingests and charges for the whole file (confirmed from Google's audio-understanding docs, 2026-08-14). So a 4-window transcription of this recording would cost **4 × $0.79 in audio tokens alone — more than grading it directly.** One call is both cheapest and simplest. **Don't "optimise" this into parallel windows; it's strictly worse.** Windows exist only as the resume path, driven one per execution from the browser, for a recording too long to finish in one answer.
  - **What it costs is real and unchanged:** a transcript can't carry tone, and the rubric penalises rushed delivery. Mitigated, not solved — see the V16 item below.
  - Original 2026-08-13 scoping, kept for the reasoning that still stands:
  - **The real win would be near-free re-grading, not cost.** An hour is ~115k tokens as audio, ~12k as text — so a re-run against a tweaked prompt drops from ~$0.63 to ~$0.16. That's what would make Phase 2 calibration experiments affordable. A *fresh* session only falls from ~$0.63 to ~$0.44, which isn't worth the build on its own. Second win: all 3 passes reading one fixed transcript removes "the model heard it differently this time" as a source of the score drift noted above.
  - **What it costs:** the rubric scores things text can't carry — the 5-tier scale penalises *rushed* delivery, and confidence/pacing don't survive transcription. It also needs speaker labelling to know the RM from the Site Operator from the customer, which gets messy when people talk over each other on a live site. And the evidence timestamps would have to be carried out of the transcription step rather than coming free.
  - **It does NOT solve long recordings.** Transcribing 8 hours is still one very long call into the same 6-minute Apps Script wall. That needs chunking and a job queue either way.
  - If it's ever built, build it as a transcript stored *alongside* the audio, not replacing it, so grading can still use both. **(This is what V16 does — the transcript is a `.txt` in the Drive folder next to the recording, and the audio is untouched.)**
- **TRANSCRIBE-FIRST GRADING — V16, built and tested 2026-08-14, DEPLOYED (both halves, confirmed live) but NOT YET RUN ON A REAL RECORDING.** Decisions worth not re-litigating:
  - **The medium is chosen by LENGTH, with a visible override.** Under 30 minutes the AI listens (the saving is pennies — measured $0.115 vs $0.106 on the 2:22 clip — and listening is the only way tone reaches the grade); over 30 minutes it transcribes first. A picker on the form (`#g-medium`: Choose for me / transcribe / listen) overrides it either way. **Duration is read in the BROWSER** off the chosen file (`readAudioDuration`, an `<audio>` element and `loadedmetadata`; never throws, resolves 0 on failure), which is also what makes the cost estimate real rather than size-guessed. With no duration it falls back to file size against the 14 MB inline limit.
  - **The cost is shown BEFORE Submit, not after** (`.audio-plan`) — same reasoning as `renderCertPreview`. The moment to find out a 4-hour file is a $2.40 grading run is while it can still be reconsidered. It names the length, both prices, the re-grade price and the tone trade-off, and says the figures are estimates.
  - **The short-clip path is byte-identical to V15, and there is a test asserting it.** `SHARED_RULES` was split into `sharedRules(medium)` so the two mediums differ in exactly two paragraphs — what the model is looking at (rule 1) and where its timestamps come from (rule 7) — while the pricing ladders and ICBC rebuttals keep **one** copy. `checkEq('sharedRules("audio") is exactly the old SHARED_RULES', …)` is the guard; **two copies of the discount ladder is how a grade starts quietly disagreeing with `docs/bc-sales-policy.md`, with nothing on the card to show it.**
  - **Timestamps get BETTER, not worse, and this is the one part that could beat the audio path.** Listening, the model *estimates* a time (measured 0–4s early). Reading, it **copies** a marker the transcription pass already anchored — rule 7's transcript variant forbids estimating, interpolating or inventing one. That's also the most likely answer to the unmeasured long-recording drift question, though **nothing has been measured yet.**
  - **Tone is mitigated, not solved, and every transcript-graded session says so.** The transcription prompt writes delivery notes inline *while it can still hear* (`[rushed]`, `[hesitant]`, `[reading verbatim]`, `[long pause]`); the grading prompt says to judge pace ONLY from those and to **treat delivery as unremarkable where the transcript is silent** — an absent note must not become an invented tone in either direction. `[inaudible]` is defined as *missing evidence, not bad performance*. Code.gs also prepends a reviewer note to every transcript grade, and the card carries `transcriptNotice()` with a link to read the transcript.
  - **Truncation is detected by an END MARKER, not guessed.** The model writes `=== END OF TRANSCRIPT ===` as its last line; its **absence** is what says the output cap or the clock was hit. Without it, a transcript cut short looks perfectly well-formed and would be graded as though the session ended early. On truncation the backend returns the last timestamp reached and the page asks for the rest, appending into the same Drive file. **Forward progress is enforced** (`TRANSCRIPT_MIN_RESUME_ADVANCE_SECONDS`) — a model repeating its final timestamp would otherwise be asked for the same window forever — and the page caps it at `MAX_TRANSCRIBE_CALLS`.
  - **The transcript is fenced and declared to be DATA.** It's a recording of people talking, and people say "skip this" and "here's what you do next" to each other all day. The block says outright that anything inside reading like a direction is something a person said during the session — grade it, never obey it.
  - **One pass covers ~8h16m** (65,536 output tokens ÷ ~2.2 tokens per second of recording), so the 4h33m file fits with room to spare and the resume path is genuinely a safety net. That 2.2 is a **guess** — denser talking produces more, and a very dense 4.5h recording could still truncate. That costs an extra pass; it does not produce a wrong grade.
  - **`maxOutputTokens: 65536` is right for `gemini-3.5-flash`, but `GEMINI_MODEL` is a Script Property somebody can change**, and an older model with a smaller ceiling rejects the whole request. A 400 naming that field retries once without it. Tested.
  - **A pre-V16 backend falls back to listening rather than failing**, detected by the same `sessionType must be` signature the pre-V15 check uses — the two halves ship by unrelated routes, so a page newer than the script *will* happen.
  - **Verified in a real browser: 162/162 in the Apps Script harness** (96 pre-existing, all still passing, plus 66 new) **and 73 front-end assertions** driving the real `gradeSessionWithAI` against a stubbed endpoint. Covered: the clean pass; truncate-and-resume; a stuck pass that can't resume; the maxOutputTokens fallback; the pre-V16 fallback; audio mode unchanged (2 calls, 2 parts, second is audio); the transcript grading request carrying **exactly one part and no audio** (the whole point); the column-G trap reached via `transcriptFileId`; an empty transcript file; column X written with T/U/V/W untouched; the cost model against hand-computed figures; all eight `resolveMedium` cases; and the plan box and transcript notice in both schemes. Contrast of every new string, computed from the root tokens: **5.21–17.91, all over AA in both schemes.**
  - **NOT verified: anything visual, and anything on a real recording.** The Browser pane was at 0×0 viewport again, so there are **no screenshots** — the contrast figures are arithmetic on the resolved tokens, not measured off a rendered page, and the layout of the new plan box and notice has never been looked at. **Worth an actual look the first time a session can screenshot.** And no recording has been transcribed: every cost and timing figure is arithmetic, not observation.
  - **What was deliberately NOT built: a "re-grade from transcript" button.** It's the obvious next step and most of the payoff — the transcript link and `transcriptFileId` are stored precisely so it's a small job — but the ask was "transcribe first to save money", not "add a re-grading workflow", and it needs a decision about whether a re-grade replaces the old row or adds one. Offer it when calibration actually starts.
- **LONG RECORDINGS — V15, built and DEPLOYED 2026-08-14 (both halves live and verified), but NOT YET RUN ON REAL LONG AUDIO.** This supersedes most of the old "an 8-hour recording can't be graded" note, which is kept at the bottom of this item because its arithmetic is still right.
  - **What forced it.** Jordan produced the first real Training Day recording — `Matt Training August.13th.m4a`, **4h33m (16,381s), 131 MB, 62 kbps mono**, in `…/DECO 2026/Recordings/Field Manager Meeting/` — and said he was about to upload it. It would have failed, and the failure would have looked like a network problem. **Don't let anyone upload a full session against a pre-V15 backend.**
  - **The four limits, all confirmed from the vendors' own docs rather than memory** (worth re-reading before any "we can't grade that" conclusion, because three of the four are *ours*, not Gemini's):

    | Limit | Value |
    |---|---|
    | Gemini inline (`inlineData`) request | **20 MB total**, prompt included |
    | Apps Script `UrlFetchApp` POST | **50 MB per call** |
    | Apps Script execution | **6 min, PER EXECUTION** |
    | Gemini Files API | **2 GB per file, kept 48 h** |
    | Gemini audio per prompt | **9.5 hours** |

    Base64 inflates by 4/3, so 131 MB became **~183 MB per request, sent three times** — 9× over the Gemini cap and 3.7× over the Apps Script one. **Gemini was never the constraint;** 4h33m is well inside 9.5 hours.
  - **Three changes, and they're independent.** (1) Audio now reaches Gemini through the **Files API** — uploaded once, referenced by URI, so a grading request is a few KB. (2) **`prepareAudio` and `grade` are separate calls**, because the 6-minute limit is per *execution*, so splitting them buys two budgets — by far the cheapest thing that bought time. (3) The **3 consensus passes now run in parallel** via `UrlFetchApp.fetchAll`, so wall-clock is one call rather than the sum of three. (3) only became sane because of (1).
  - **Decisions worth not re-litigating:**
    - **Short clips deliberately keep the OLD inline path**, chosen by *size* (`GEMINI_INLINE_MAX_AUDIO_BYTES`, 14 MB), not by session type. It works, it's one round trip instead of a dozen, and it means this change **cannot regress the 2:22 test clip that every measurement in this project so far rests on.**
    - **Chunks are pulled from Drive with HTTP `Range` requests, never `DriveApp.getBlob().getBytes()`.** The latter materialises the whole recording in script memory, which is where a 131 MB file dies with "Exceeded memory limit" — a failure that looks nothing like a size problem. `driveByteRange()` keeps peak memory at one chunk. A test asserts `getBlob()` is called **zero** times on the long path.
    - **`GEMINI_UPLOAD_CHUNK_BYTES` is 8 MB because it must be a multiple of 256 KB** (the resumable protocol requires that of every chunk but the last) **and** well under 50 MB. Don't round it to 10 MB.
    - **`audioFileId` must STILL be sent alongside `geminiFileUri`, and V15 now throws if it isn't.** Column G (Audio File Link) is built from it, and column G is what `fetchSubmissions()` groups a session's manual and AI rows by and what the in-page player is built from. Without it the grade saves perfectly and writes `.../file/d/undefined/view` — **a session that looks fine on the card and is quietly broken in the sheet.** This was found by the test harness, not by review.
    - **`index.html` falls back to the old single-call flow if the backend is pre-V15**, detected by V14's `sessionType must be` reply to an unknown action. The two halves deploy by completely different routes (a git push vs. a manual paste), so there *will* be a window where the page is new and the script is old; the fallback means that window costs long-recording support rather than breaking grading for whoever is mid-session.
    - **A still-`PROCESSING` upload is handed back, not thrown away.** `waitForGeminiFileActive` returns the last state instead of failing, and the front end re-asks with `action:'checkAudio'` — a free metadata read that gets a fresh 6-minute budget each time.
    - The progress bar is now **phase-based with each phase owning a slice** (upload 0–28%, unpacking 28–35%, grading 35–92%) so the handover always moves forward and never reads as a reset, and the estimate is **derived from file size** rather than the old flat 180s, which came from a 2-minute clip. Verified monotonic: `0 → 0.3 → 35 → 35.2 → 35.5 → 100`.
    - **An Apps Script timeout is now explained in words.** When an execution is killed the endpoint answers with an HTML error page, so the page used to say `Unexpected token '<'`. `describeGradingTransportFailure()` turns that into a sentence naming the 6-minute limit — but **only past 4 minutes elapsed**, since the same HTML early on means a script error, not a timeout.
  - **THE HONEST OPEN QUESTION, and it is the whole ballgame: nobody knows whether ONE Gemini pass on 4h33m of audio returns inside 6 minutes.** Everything above removes the size walls; this one is unmeasured and cannot be settled without running it. If it times out, the next step is **one pass per execution** (three browser calls, reconciled at the end) — `callGeminiPasses` already isolates exactly the piece that would need to change. **Do not record long recordings as "solved" until a full-length session has actually completed.**
    - **V16 changes the shape of this question rather than answering it.** Transcript-first grading means the long recording is read **once**, by one call, instead of three times by three — so there is one risky call instead of three, and if it succeeds the transcript is saved and the risk never has to be taken again for that session. It is still unmeasured. **The V16 route is the one to try first on the real file**, because a failure there costs one transcription and leaves the audio untouched, where a failed 3-pass audio grade costs three.
  - **Cost if it runs: ~$2.50 per grading of this file** (16,381s × 32 tokens/s = 524k per pass, ×3 = 1.57M input at $1.50/1M, plus a little output). Also: **while the Files API URI is alive (48 h), re-grading costs no re-upload** — re-running a session against a tweaked prompt for the price of the tokens alone, which is most of the win the parked transcribe-first idea was going to buy.
  - **The `.mp4` recordings in that same folder are VIDEO** (Admin/BC Wide/Field Manager meetings, some 1–3 GB). Video bills at a far higher token rate than 32/s audio. Nothing has been tested against one; don't assume this work covers them.
  - **There is a local test harness for `Code.gs`, in the gitignored `apps-script/` folder.** `_stubs.js` fakes every Apps Script service (`UrlFetchApp` incl. `fetchAll`, `DriveApp`, `Utilities`, `PropertiesService`, `ScriptApp`, `SpreadsheetApp`, `ContentService`); `_tests.js` holds **96 assertions**; `_build-harness.ps1` concatenates stubs + Code.gs + tests into `_harness.html`; `_serve.ps1` is a PowerShell `HttpListener` static server on port 8765 (**this machine has no node and no real python** — the Store stubs only). To run it: `.\apps-script\_build-harness.ps1`, start `_serve.ps1` in the background, create `.claude/launch.json` with `{"name":"static","url":"http://localhost:8765","port":8765}` (the Browser pane refuses a localhost port that isn't in there, and it renders anything outside the project folder as a dead static snapshot), then `preview_start` by name and open `/apps-script/_harness.html`. **Delete `launch.json` afterwards** — it is not gitignored. Two things the harness proved that review had missed: the column-G trap above, and that the 131 MB file needs exactly **17 chunks** with contiguous, non-overlapping ranges covering the file once.
  - **Superseded by V17 (2026-08-14) — an 8-hour recording is now handled, and the ceiling is now a measured number rather than a vague "different problem".** See the V17 item directly below. The arithmetic in the paragraph that used to sit here was right about cost and wrong about nothing being possible; what it lacked was the observation that the 6-minute wall is made of OUTPUT, which is the thing a bounded window actually shrinks.

- **8-HOUR RECORDINGS — V17, built and tested 2026-08-14, NOT YET DEPLOYED and NOT YET RUN ON A REAL RECORDING.** Jordan: *"can you make the code work for 8+ hour recordings."* Most of what was needed already existed — V16 built the `fromSeconds`/`toSeconds` plumbing and then never drove it. Decisions worth not re-litigating:
  - **THERE IS A HARD CEILING AT ~9h01m AND IT IS NOT OURS.** Gemini bills audio at 32 tokens/second and `gemini-3.5-flash` reads 1,048,576 input tokens per request, so 32,518 seconds is the absolute most that can reach the model in one piece. Google's own documented audio cap (9.5h) is *looser*, so this is the one that binds. **Past it a recording cannot be graded at all and must be split into two files submitted as two sessions** — and no windowing trick avoids that, because a window still sends the whole file. `MAX_AUDIO_SECONDS_PER_REQUEST` (Code.gs) and `MAX_GRADABLE_SECONDS` (index.html) are that number; one more duplicated constant on the pile. Jordan was asked and confirmed 8–9h is the real range, so multi-part upload was scoped OUT.
  - **Re-confirmed from Google's docs, because the whole design turns on it: there is still no way to bill part of an audio file.** Asking for a time range in the prompt ingests and charges for the entire file. So every window re-reads everything, and that cost is real and is in the estimate.
  - **The 6-minute wall is made of OUTPUT, not input — that is the insight the V16 note missed.** V16 assumed windows only help with the 65,536-token output cap and concluded they were pure loss. But an 8-hour transcript is ~63k output tokens in ONE execution, which is minutes of generation on top of reading a 1M-token file. A bounded window shrinks the generation, and each window gets its own fresh 6-minute budget. That is what makes 8 hours finishable.
  - **Windows are sized by OUTPUT TOKENS (30,000), never by clock time, and only used when needed.** `transcriptWindowCount()` returns the SMALLEST count that keeps each call safe, so a 2:22 clip and a 3-hour session still run as **one open-ended pass — byte-identical to V16** (verified). A 4h33m session is 2 windows, 8h is 3, 9h is 3. **If a window ever overruns 6 minutes, `TRANSCRIPT_WINDOW_TARGET_TOKENS` is the one number to turn down.** How fast the model writes is a guess until a real long session runs.
  - **The last window is sent open-ended (`toSeconds: 0`), never bounded by the duration the browser read.** A container that reports its length slightly short would otherwise stop the transcript a few seconds early with nothing downstream able to detect it. Running long costs nothing; stopping short loses evidence — the same asymmetry that governs the resume point.
  - **AUDIO NOW GOES FIRST IN A TRANSCRIPTION REQUEST, prompt second, and that is the cost story.** Implicit caching (default on for 2.5+ models) only matches on a shared PREFIX, so with the prompt leading it could never fire — the varying part came first. Audio first makes the ~1M identical tokens the prefix, and a hit bills at **$0.15/M against $1.50/M**. Never promised and never the headline figure, but it was impossible by construction before. **Deliberately NOT applied to the grading passes** in `buildGeminiRequestOptions`: those are three *identical* requests fired in parallel, so nothing is warm and the order buys nothing there, while changing it would disturb the short-clip path every measurement in this project rests on. Two tests pin the grading order in place.
  - **At 8 hours the transcript route COSTS MORE than listening ($5.10 vs $4.24) and is still correct.** Three windows re-reading the audio eventually outweigh three straight listens. It stays the right route because listening means three calls that each swallow the whole recording inside one 6-minute execution — the thing that cannot finish. **The plan box says this out loud** rather than quoting a saving it isn't making; the old copy would have read as nonsense next to its own numbers. The re-grade price ($0.38 vs $4.24) is untouched and is still the Phase 2 win.
  - **Two caps on the transcription loop, because it is the one loop in this app that spends money per iteration** (~$1.40 a pass at 8h whether it achieves anything or not). `TRANSCRIPT_MAX_ATTEMPTS_PER_WINDOW = 3` is the tight one and gives the useful error — it names the stretch that keeps stopping short — and `MAX_TRANSCRIBE_CALLS = 12` is the backstop. Both are checked BEFORE the call, never after: by the time a pass answers it has already been charged for. Measured: a pathological model that never finishes now costs 3 passes (~$4) instead of 12 (~$16), and **grades nothing**.
  - **`joinTranscriptChunks()` drops verbatim repeats at a window boundary.** A resumed pass restarts ON its last timestamp (early beats late), so that line genuinely arrives twice — and a grader cannot tell a stitching artefact from a trainer repeating themselves, which the delivery rubric scores. Exact matches only, tail only, capped, so the worst case is a duplicate surviving rather than real session being eaten.
  - **The progress bar got a ratchet, and it was a real bug, not a precaution.** A window that truncates and resumes calls `setPhase` without advancing the window index, which walked the bar backwards — on precisely the long recordings where somebody is already watching it nervously. Clamped in `renderProgress` against a high-water mark so every future call site is covered too.
  - **Verified in a real browser, both schemes, with screenshots** — the Browser pane WAS compositing this session. **191/191 in the Apps Script harness** (162 pre-existing all still passing, plus 29 new). Front end driven against a stubbed endpoint: the 8h window sequence (`0→9600`, `9600→19200`, `19200→open`), a truncate-and-resume staying *inside* window 1 (`3600→9600`) with the bar never stepping backwards across 7 real samples, the 4h33m file at 2 windows, the 2:22 clip at one open-ended window, the audio route completely untouched, the runaway cap, and the pre-V16 fallback. **The submit-time ceiling guard issues ZERO outbound calls on a 10h file** (Sheets and Drive counted, not just the grader), lets 8h through, and deliberately lets an unreadable duration through. Contrast of every new string: **5.21–17.91, all over AA in both schemes.**
  - **Two mistakes worth recording, both caught only by running it.** The backend computed `windowFromSeconds`/`windowToSeconds` and `doPost` didn't pass them through — invisible to review, caught by an assertion. And the first progress check reported `barMonotonic: true` off **zero samples**, because the stub resolved instantly; a vacuous pass that would have hidden the ratchet bug. **A monotonicity check needs latency in the stub or it proves nothing.**
  - **THE WINDOW SIZE NOW MEASURES ITSELF — V17.1, added the same day on Jordan's question** (*"is there a way to make sure that if it hits 5:59 to hit submit for it? ... as well as any way to make sure it is under 6:00 minutes?"*). Points worth not re-litigating:
    - **THERE IS NO WAY TO SALVAGE A PASS AT 5:59, and the reason is worth keeping written down because it is the obvious idea.** Writing the transcript is ONE blocking `UrlFetchApp` call. Gemini does not stream it back; the whole answer arrives at the end or not at all. At 5:59 the script is holding **nothing**, so "saving what it has" would write an empty file. A killed pass loses its own work and its own cost, never the session — every finished pass is saved to Drive before the next starts.
    - **A pre-flight "is there time left?" check would also be pointless here**, and was considered and dropped: each transcription pass is its own POST, so it is its own execution with its own fresh 6 minutes and the Gemini call is essentially all of it. There is nothing else in that execution to have used the budget up.
    - **CONFIRMED FROM GOOGLE'S QUOTAS PAGE: a Workspace account gets the SAME 6 minutes** as a consumer one. There is no bigger-budget tier to move to; don't go looking for one again.
    - **So the only real lever is not asking for more than fits, and the size of the ask is now measured rather than guessed.** `transcribeAudio` returns `elapsedMs`; a pass over `TRANSCRIBE_SLOW_PASS_SECONDS` (270s — 4m30s, leaving 90s of headroom) shrinks every REMAINING window by `TRANSCRIBE_SHRINK_FACTOR` (0.55), with a 20-minute floor so a slow model can't drive the span towards zero. **One slow pass now costs one extra retry instead of a failed session.**
    - **270s deliberately, not tighter.** Every shrink means more passes and every pass re-reads the whole recording, so over-reacting to a pass that was comfortably fine is itself expensive. Measured: a 3m40s pass leaves the 3-window plan alone; a 5m00s first pass takes an 8-hour session from 3 windows to 5.
    - **The loop is cursor-based now, not a pre-computed array**, because the plan changes mid-run. `transcribeWindowTotal` is re-estimated after each window, so the label goes "Pass 1 of 3" → "Pass 2 of 5". The progress ratchet is what makes a growing total safe — the bar pauses rather than jumping backwards. **Verified: zero backward steps across 10 samples while the plan grew 3 → 5.**
    - **Worst case is bounded but it is not cheap: `MAX_TRANSCRIBE_CALLS` (12) × a full audio read ≈ $16.50 on an 8-hour session** if every pass keeps running hot. That is the price of finishing instead of failing, and the on-screen label says the passes went up. If it ever needs tightening, lower the cap rather than the shrink floor.
  - **THE FIRST REAL RUN FAILED, AND IT PRODUCED THE NUMBERS THIS WHOLE THING WAS MISSING — V17.2, 2026-08-14, 8:45pm.** Jordan ran the 4h33m file against V17.1. Upload fine (2:20), windowing fine (correctly "Pass 1 of 2"), then `Timeout: …generateContent` at ~9:45 elapsed. **NOT a 503** — the request went out and never came back. **Assume it was billed** (~$0.61): unlike a refusal, a timeout means Gemini almost certainly read the audio.
    - **The cause was one wrong constant.** Windows were sized assuming the model writes ~200 tokens/second. Pass 1 was asked for 2h16m ≈ 18,000 tokens and didn't finish, putting the true rate **under 48/s**. Everything else about the design held; that single guess was the failure.
    - **`decoProbeLongAudio()` then split the two candidate causes apart, and this is why that function exists.** Reading the 4h33m file: **59 seconds, HTTP 200.** So **reading is cheap and WRITING is the wall** — which is what makes smaller windows the right fix. Had it been the other way round, every window would re-pay the read and no amount of splitting would have helped; the answer would have been splitting the audio into separate files. **Run this probe before re-tuning anything here.**
    - **AUDIO IS 25 TOKENS/SECOND, NOT 32 — measured, and the docs disagree.** `countTokens` predicted 526,773 for the 4h33m file; `usageMetadata.promptTokenCount` actually billed **409,541**, exactly 25.0/second. usageMetadata is what gets charged, so **every cost figure in this app was 22% too high** before this. `AUDIO_TOKENS_PER_SECOND` is now 25. Note `MAX_GRADABLE_SECONDS` deliberately still divides by **32** — being conservative about a ceiling is the safe direction, and 9h01m still clears the 8–9h Jordan asked for.
    - **Windows are now sized from a TIME BUDGET, not a token count:** `(240s budget − 13s per hour of reading) × 35 tokens/s of writing`. All three numbers come from that run. Result: the Matt file goes from 2 passes to **6** (~46 min each, 3× smaller than the pass that died), 8h to ~15.
    - **THE COST WENT UP AND IT IS NOT SMALL.** Matt: **$2.15 → $4.27**. 8h: **$5.10 → $16.07**, because every one of 15 passes re-reads the whole recording. **If the implicit cache is hitting, those become $1.50 and $3.43** — a 4–5× difference that decides whether 8-hour sessions are viable at all.
    - **Whether caching actually fires is UNKNOWN and is now instrumented rather than hoped for.** Audio-first ordering makes a hit *possible*, not certain. Every transcription pass now logs `promptTokenCount` / `cachedContentTokenCount` to both the Apps Script log and the browser console. **The first successful long run answers it.** If cached is 0%, explicit context caching (create once, reference per window, ~$3.31 all-in on 8h) is the next build and it is worth doing.
    - **Also seen in the probe: `thoughtsTokenCount: 117` on a "reply OK" request.** The model is thinking even for trivial work, and thinking tokens are billed as output *and* cost wall-clock. Transcription needs no reasoning, so **turning thinking down for the transcription call is an untested but plausible way to buy back a lot of the write budget** — which would mean fewer passes and less re-reading. Worth measuring before building.
  - **WHAT THIS DOES NOT PROMISE:** no long recording has been transcribed **end to end** yet. The write rate is bounded rather than measured precisely, and adaptation only helps from pass two onward — a first pass that overruns still fails and still costs its read.
- One reference-data gap left, non-blocking: the complex-chip price ceiling ($125) is an unconfirmed assumption.
- **The Stage 2 Interview rubric grades facts that `docs/` does not contain, and this is the one real content gap in V20 (2026-08-15).** Category 2 grades DECO's growth figures and site counts, and category 7 grades compensation, the end-of-contract sales bonus and contract dates — the source rubric says to check all of them against "current approved materials supplied to the AI", and no such materials exist in this project. **Handled honestly rather than papered over:** the prompt states outright that those figures are not supplied, tells the model not to mark a number right or wrong, has it grade the *shape* of what was said instead (was advancement framed as earned, was an end date mentioned, was pay described as performance-based), and routes any specific figure into `notes_for_human_reviewer` for a human to check. So a Stage 2 grade is real, but **its factual-accuracy checking is partial by construction.** Closing it means Jordan supplying current figures for a `docs/deco-recruiting-facts.md`, which would then have to be added to `STAGE_TWO_PROCESS_REFERENCE` in `Code.gs` — and would also be worth pointing the Conversion Call prompt at, since it has the same blind spot and has never said so.
- **Team access: DONE (Jordan confirmed 2026-08-14).** The Roster/Grading Log Sheets and the Audio Uploads Drive folder are shared with the rest of the team, so the app now works end to end for someone other than Jordan. This was the last access blocker; if a teammate hits a permissions error, it's a per-file share that got missed, not the design.
- **Roster certification columns are auto-ticked from a passing *manual* grade (2026-08-13), never from an AI grade.** See "Auto-certifying from a manual grade" above. Auto-flipping from AI results is deliberately not built — that's Phase 5.

## Working preferences

- **Verify UI changes with a real browser** before saying they're done — render the page headless, screenshot it, check for console errors. This has caught real regressions on this project.
- **Don't overpromise.** Where a limitation is real (sign-in longevity, progress accuracy, audio-only grading), say so plainly in both the code and the conversation rather than papering over it.
- **Resist scope creep.** Leadership explicitly rewards judgment about what *not* to build. Two live-monitoring features were deliberately scoped and dropped once it turned out the existing workflow already covered the need.
- When something is edited outside a Claude session, note it here so this file stays a true picture of the project.
